# 食序管家 v0.7.4.23 更新说明

产品版本：`0.7.4.23`

技术包版本：`0.8.23`

## 为什么开发这一版

v0.7.4.22 已经修复明确摄入、饮水写后查询、自然称重、历史餐食“碳水”误判和同会话纠正等入口问题。真实 OpenClaw UAT 随后发现两个更窄但会破坏可信度的问题：冷会话问“刚才记上了吗？”时会在 Meal、Pantry、事务、文件甚至其他业务工具之间长时间探测；查询候选同时返回 UTC 与上海本地时间时，模型可能选择 UTC，把凌晨 03:36 显示成前一天 19:36。

这两个问题都不是账本数据错误，也不需要重写 Meal、库存或时间窗。本版只收窄操作状态查询的公共路由，并减少模型可见的互相冲突时间证据。

## 本版修复

- 纯“刚才记上了吗？”识别为最近饮食操作状态查询，只允许一次 `diet_transaction get_recent`。
- 同一轮的 Meal、Pantry、Report 等其他 Diet 路由会被拒绝；第一次最近操作读取完成后，第二次 Diet 读取也会被拒绝，失败同样不会触发写入重放。
- 私有路由提示明确禁止扫描业务文件、Meal、Pantry、Report 或其他业务工具；查不到证据时应说明不确定或只问一个短问题。
- 当读结果同时含 `occurred_at_local` 与 `occurred_at`，或同时含 `measured_at_local` 与 `measured_at` 时，模型可见文本只保留本地字段，并隐藏同对象的审计时间。
- 当查询范围同时含 `start_local/end_local` 与 `start_utc/end_utc` 时，模型可见文本只保留本地范围。若工具只有 UTC，则继续保留该唯一时间证据。
- 原始结构化 `details` 不变，数据库仍保存 UTC，UI、审计、合同和外部调用方仍可取得完整字段。

## 授权与兼容边界

- “没记就帮我补记”等当前消息明确授权的条件补记不是纯状态查询，继续进入原有写入判断。
- 普通餐食、库存、饮水、体重、报告和时间范围查询不经过本路由；本版没有把所有只读请求压成事务查询。
- 非 Diet 插件不由本项目越权拦截；Skill 运行契约负责禁止转向文件或其他业务工具，Diet 宿主钩子只硬拦本项目的错误路由。
- 不新增工具 action、数据库字段或 migration，不改变 Meal/SQLite 事务、回执生成器、营养与库存计算。

## 明确保持不变

- 明确摄入继续直接记录；一个玉米继续显示自然计数和 `可食部（玉米粒）约90克（估算）`，玉米芯不计入营养。
- 普通餐食继续使用六项双行进度回执；纯水继续只显示饮水确认和饮水双行。
- 模糊开放份量、包装换算、库存扣减与撤销返库、过期推荐硬过滤、否定/计划/他人隔离均不修改。
- 不处理 OpenClaw WebUI“新会话”按钮问题。
- v0.7.4.22 及更早目录、包、哈希和记录保持不可变。

## 更新与回退

本版没有新增 migration，继续使用 migrations 001–021，schema 与 v0.7.4.19 相同。更新前必须停止实例，并在外部 `dataDir` 之外创建和校验**升级前冷备份**；在线 `diet_system backup` 只用于同版本恢复，不能替代升级前冷备份。只安装已校验的 `personal-diet-pantry-0.7.4.23-installable.tgz`。如果安装、自检或真实 UAT 失败，保持数据不变并重新安装已校验的 v0.7.4.19 包恢复技术运行。

## 重点口语化验收

1. 冷会话问“刚才记上了吗？”：最多一次最近操作读取，不得调用 Meal、Pantry、Report、文件扫描或重放写入。
2. 先记录一个临时玉米，再在新会话查询候选：显示上海本地时间，不得把 `03:xx` 显示成前一天 `19:xx`；随后清理临时记录。
3. “刚才玉米记上了吗？没记就帮我补记”：不得被纯状态查询路由吞掉，仍按条件补记规则判断。
4. “吃了个玉米”后同会话说“其实可食部是80克”：继续直接纠正；新会话无可信句柄时不得直接删除。
5. “刚喝了149毫升水，帮我记一下并告诉我今天一共喝了多少”：继续写入后查询，纯水回执保持简洁；随后撤销。
6. “刚称了106.66公斤”：继续直接记录；随后“撤销刚才”只撤销该体重。
7. 一句话同时写餐食、饮水和体重：继续在首次写入前整体阻断。

## 候选制品契约

本地候选目录顶层必须恰好包含：

- `personal-diet-pantry-0.7.4.23-source.tar.gz`
- `personal-diet-pantry-0.7.4.23-installable.tgz`
- `release-manifest.json`
- `TEST-SUMMARY-v0.7.4.23.zh-CN.md`
- `SHA256SUMS`
- 本地审阅留痕目录 `GitHub文档`

当前仍是开发候选。只有总约束保护矩阵、全量自动化、可重复构建、安装包审计、停机冷备份、软路由安装、真实口语化 UAT、测试数据清理和数据库核验全部通过，才能标记为可用；本项目不会自动推送 GitHub、创建 Tag 或 Release。
