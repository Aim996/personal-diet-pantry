# 食序管家 v0.7.3.2 更新说明

## 本版目标

v0.7.3.2 修复高频包装库存闭环：OpenClaw 可以先搜索库存，再用返回的搜索句柄记录“喝了 1盒”，由插件完成包装换算、营养缩放、FEFO 扣减和可撤销写入。技术包版本为 `0.7.5`，用户文档和导出 manifest 使用产品版本 `0.7.3.2`。

## 核心修复

- **可信搜索句柄**：句柄绑定商品身份、基础单位、包装规格、营养快照和相关批次版本；批次变化后旧句柄失效，新句柄仅使用小写十六进制字符。
- **展示单位换算**：当库存已持久化确定的包装规格时，餐食最小参数只需 `raw_name + normalized_name + amount + unit + inventory_match_handle`；`1盒` 会在服务端换算为基础量。
- **部分营养**：标签已知字段正常缩放，标签没有提供的未知字段保持 `null`，不会为了通过写入而补造零值或模型估算；餐食和当天汇总保留部分营养状态。
- **包装入库边界**：完整包装三元组 `display_quantity + display_unit + base_quantity_per_display_unit` 配合基础 `unit` 可推导基础数量；显式基础数量冲突时返回字段级错误。
- **修正时间稳定**：餐食修正未指定新时间时继承原 `occurred_at`，只更新修改时间；失败修正不会改变原餐食或库存。
- **Skill 调用纪律**：移除“裸数字直接写体重”的危险规则；缺少明确体重语义时不写入，并把静态路由检查改名为 lint。
- **行为轨迹门禁**：新增脱敏行为轨迹，验证一次库存搜索、一次餐食记录、唯一最终回复，以及写入前、写入后和撤销后的数据库断言。

## 数据迁移与兼容

本版没有新增 migration，继续使用 migration 001–021，schema 与 v0.7.3.1 相同。升级可复用同一个专用 `dataDir`；导入继续接受 0.6.7、0.7.0、0.7.1、0.7.2、0.7.3、0.7.3.1，并新增接受 0.7.3.2 的可移植导出。

本源码、构建和发布流程不会自动部署、停止、重启或修改任何 OpenClaw、Telegram、Docker、路由器或生产实例，也不会接触真实个人数据库。

## 验证范围

发布门禁覆盖 Python 与 TypeScript 全量测试、Skill 静态 lint、包装豆浆行为轨迹、安装包独立启动、既有数据库升级、发布审计、敏感信息扫描、依赖审计和可重复构建。具体通过、跳过和哈希只记录在当次机器生成的测试摘要与 manifest 中。

## 发布目录

正式发布目录顶层恰好包含：

- `personal-diet-pantry-0.7.3.2-source.tar.gz`
- `personal-diet-pantry-0.7.3.2-installable.tgz`
- `release-manifest.json`
- `TEST-SUMMARY-v0.7.3.2.zh-CN.md`
- `SHA256SUMS`
- `GitHub文档/`

源码包只用于审阅和复现；OpenClaw 插件安装器只能接收可安装包。

## 升级与回退

升级前停止目标实例，使用随可信源码提供的 helper 创建并校验包含已提交 WAL 数据的升级前冷备份，同时保留 `personal-diet-pantry-0.7.3.1-installable.tgz`。安装 v0.7.3.2 后保留原 `dataDir`，运行 `self_check`，再以只读查询核对库存、餐食和营养状态。

在线 `diet_system backup` 仅用于同版本恢复，不能替代升级前冷备份。若新版本安装或自检失败，停止实例后可直接安装 v0.7.3.1 可安装包并复用同一 `dataDir`；若数据库或环境受损，再恢复已校验的冷备份。不得手工修改 `schema_migrations`。

## 延后项目

以下问题不在本轮偷偷扩展：剩余目标与日期的后端确定性计算、时间线因果解释、疑似重复餐确认、统一建议约束、类别索引、通用数量单位和性能 SLO 进入 v0.7.3.3；主体级记忆隔离、Skill 进一步压缩、公共发行兼容元数据及营养种子安装向导进入 v0.8.0。
