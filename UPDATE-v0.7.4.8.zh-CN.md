# 食序管家 v0.7.4.8 更新说明

产品版本：`0.7.4.8`
技术包版本：`0.8.8`

## 本版目标

本版没有扩展账本边界，只补齐 v0.7.4.7 已经承诺、但真实 OpenClaw 调用链没有稳定兑现的三处行为：
可食部估算必须进入正式餐食回执，用户给出精确克重后必须去掉旧估算标记，纯饮水必须继续只显示饮水。

## 用户可见变化

- “吃了个玉米”仍然直接记录，并保留用户说的 `1个`；当调用链已经提供可食部证据时，正式记录和
  回执稳定显示 `1个｜可食部（玉米粒）约90克（估算）`，不会只剩一个没有重量口径的“1个”。
- 系统不会只凭“玉米”这个名字猜玉米芯、玉米粒或可食比例；可食部名称和重量必须来自当前调用的
  明确证据。没有安全证据时，继续使用原有数量表达，不编造部位。
- 用户紧接着明确纠正“其实是80克”时，原子更新仍保留 `1个` 和已经确认的可食部对象，同时改为
  `1个｜可食部（玉米粒）80克`；精确用户输入不再错误保留“估算”字样。
- 纯饮水成功回执只显示饮水确认和饮水两行进度。即使调用方误传了热量或蛋白质指标，饮水渲染器
  也不会把六项餐食进度混进来。

## 实现边界

- 份量表达在餐食草稿进入账本前做确定性规范化，继续复用既有 `portion_expression` 和
  `portion_evidence_json`，不增加字段或 migration。
- 规范化只处理已经给出自然计数、消费重量和可食部证据的项目；不维护“食物名称到可食部”的隐式
  规则表，不把模型常识当作账本证据。
- 精确纠正仍绑定当前会话最后一次成功餐食凭证，并沿用既有更新事务、营养重算和唯一目标保护。
- 纯饮水变化只在回执渲染层过滤为 `water` 指标，不改变饮水写入、目标、撤销或餐食饮料逻辑。

## 保持不变

- 明确、已发生、自然数量清楚的常见食物继续同轮直接记录，不增加多余确认。
- “一点、一些、几口”等真正开放份量继续先展示有界估算并确认，确认前零业务写入。
- 餐食回执继续使用热量、蛋白、脂肪、碳水、纤维、饮水六项固定顺序，每项固定两行。
- 查询零写入、跨轮授权隔离、两次失败熔断、过期食品推荐硬过滤、`Asia/Shanghai` 通用时间窗、
  库存事务和 SQLite 唯一事实来源保持不变。
- 本版没有新增 migration，继续使用 migrations 001–021，schema 与 v0.7.4.7 相同。
- 本地构建和实例安装不会自动推送 GitHub、创建 Tag/Release。

## 自动验证与真实验收

- 自动测试总数和构建环境以候选目录中的 `TEST-SUMMARY-v0.7.4.8.zh-CN.md` 与
  `release-manifest.json` 为准。
- 发布前必须在隔离数据或可回滚数据上真实验证：玉米直接记录、80 克纠正、真正模糊份量的零写入
  确认、纯饮水短回执、餐食六项两行、查询零写入、撤销/删除和记录数量不漂移。

## 升级与回退

更新前停止目标实例并创建、校验升级前冷备份；安装时保留外部 `dataDir`。更新后核对产品版本
`0.7.4.8`、技术包版本 `0.8.8`、七类工具、自检和关键记录数量。若候选失败且数据库未变化，
停止实例并重新安装已校验的 v0.7.4.7 可安装包；在线 `diet_system backup` 只用于同版本恢复，
不能替代升级前冷备份。

候选目录顶层只包含：

- `personal-diet-pantry-0.7.4.8-source.tar.gz`
- `personal-diet-pantry-0.7.4.8-installable.tgz`
- `release-manifest.json`
- `TEST-SUMMARY-v0.7.4.8.zh-CN.md`
- `SHA256SUMS`
- `GitHub文档/`
