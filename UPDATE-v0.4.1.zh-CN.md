# 食序管家 v0.4.1 更新说明

这是 v0.4.0 的小型可靠性修复，不更新 OpenClaw 系统，也不改变现有
SQLite 数据结构。

## 修复内容

- 库存查询兼容模型常用的 `food_name` 输入，并自动转换为正式查询字段。
- 查询未指定商品时自动退回紧凑结果，避免详细库存整表进入上下文。
- 补充重量元数据时，缺少 `source_text` 会自动生成安全来源说明。
- 关联营养表时，缺少 `linked_at` 会使用当前会话时间。
- 自动移除模型误带入重量更新请求的商品名称字段。

这些兼容处理针对线上会话中出现的 `INVALID_INPUT` 红色工具错误。正式
SQLite 写入仍通过预览与提交事务完成，不会绕过原有校验。

## 数据与回退

v0.4.1 没有新增迁移，数据库仍为 schema 10。升级前后的 SQLite 备份和
v0.3.0 源码备份均保留在插件数据目录的 `backups` 文件夹中。需要回退时，
可恢复源码备份并使用对应的已验证 SQLite 备份。
