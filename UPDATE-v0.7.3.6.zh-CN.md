# 食序管家 v0.7.3.6 更新说明

## 本版目标

v0.7.3.6 面向真实家庭饮食账本的稳定性：统一时间解释，补齐查询与库存关系证据，
让模糊份量先形成可检查的估算确认，并为每条 Skill 路线设置调用预算和失败止损。
技术包版本为 `0.7.9`，产品版本为 `0.7.3.6`。

## 受保护行为

受保护的六项两行进度回执保持不变：热量、蛋白、脂肪、碳水、纤维、饮水固定顺序，
每项固定两行、10 格进度条、真实超标百分比与本次增量；成功写入后不添加
`📊 今日进度：` 标题，也不为了重建回执额外查询报告。每个变更批次仍须先完整阅读
`docs/PRODUCT-BEHAVIOR-INVARIANTS.zh-CN.md`，未经用户明确授权不得弱化已登记行为。

## 时间与查询

- 正式数据继续以 UTC 时间存储；公共结果同时提供按用户 IANA 时区生成的当地时间投影。
- 默认配置使用 `Asia/Shanghai`，但实现不把某个固定偏移写死；其他 IANA 时区和夏令时
  边界按相同规则处理。
- 新增通用时间范围描述符：日/周/月等日历窗口、滚动时长、明确本地起止范围和兼容的
  完整日期。跨午夜、跨月和相对时间都使用同一个范围模型。
- “昨晚”只是通用时间范围的一条例子；餐食查询覆盖该区间内的正餐、加餐、营养饮料、
  补剂等 meal-domain 摄入，不再被固定缩窄成晚餐标签。
- 查询结果返回实际范围与完整性；读取失败不再被解释成“没有记录”。

## 估算确认

- 模糊份量不再擅自落成单点事实。可合理限定时，插件返回建议值、上下界、单位、策略与
  证据说明；不能合理限定时只询问一个必要事实。
- 同一顿包含多个模糊项目时，只生成一次联合预览；预览对餐食、库存和其他业务表零写入。
- 只有用户下一条纯确认才用原样句柄提交一次。新增数量、来源、目标、修正或否定会使旧预览
  失效；已确认的个人份量规则可以直接解析而不重复询问。

## 报告完整性与库存关系

- 临期报告完整返回仍有库存的全部已过期批次，并区分过期、紧急、临近与正常状态；报告
  带范围、数量和 `complete` 元数据，整个操作保持只读。
- 熟食、加工库存与原料之间只有正式 cooking transaction 生成的 relation 才可支持
  “已经从原料扣过”等结论。名称相似、数量巧合或模型推断不再作为库存关系证据。
- 新的库存变化仍只由成功提交结果里的 `inventory_effects` 证明。

## Skill 路由与调用预算

Skill 采用统一流程：事件状态 → 独立事实解析 → 有界路线 → 读取/预览或确认写入 →
一次终态回复。否定、计划、假设和取消先于时间与份量解析短路。

调用预算为：简单读写 1 次、定向搜索加操作 2 次、修正/撤销 2 次、估算预览最多 2 次、
确认提交 1 次；只有工具明确返回的 cursor 才能延长同一读取路线。相同错误指纹不重试，
两个不同失败也会终止路线。Diet 能力失败后不得转 Exec、SQL、SQLite、报告或文件遍历兜底。
偏好查询失败不会猜测忌口，也不会向用户暴露内部维护命令。

## 数据迁移与兼容

本版没有新增 migration，继续使用 migration 001–021，schema 与 v0.7.3.5 相同。
导入接受 0.6.7、0.7.0、0.7.1、0.7.2、0.7.3、0.7.3.1 至 0.7.3.6 的可移植导出。
升级继续使用原专用 `dataDir`，不会自动改写历史事实、库存关系或估算。

## 验证与已知边界

本版新增策略注册表、跨日/夏令时、三领域当地时间、临期报告完整性、正式库存关系、
联合估算、调用熔断与脱敏轨迹测试。受保护回执测试继续作为发布门禁。

宿主流式重复和“停止生成后下一轮续写”属于 OpenClaw 宿主/流式层诊断范围；本版通过
“一个用户消息只生成一次终态业务回复”收紧 Skill，但不在没有宿主证据时修改插件业务事实
或宣称已经根治宿主流式重复。该项延后到安装后的宿主集成测试继续诊断。

## 发布制品

正式发布目录顶层恰好包含：

- `personal-diet-pantry-0.7.3.6-source.tar.gz`
- `personal-diet-pantry-0.7.3.6-installable.tgz`
- `release-manifest.json`
- `TEST-SUMMARY-v0.7.3.6.zh-CN.md`
- `SHA256SUMS`
- `GitHub文档`

源码包只用于审阅与复现；OpenClaw 安装器只能使用可安装包。

## 升级与回退

本项目不会自动部署、上传、安装、停止或重启 OpenClaw。实施任务只生成本地制品。
升级前停止目标实例，创建并校验包含已提交 WAL 数据的一致 SQLite 升级前冷备份，
同时保留 `personal-diet-pantry-0.7.3.5-installable.tgz`（v0.7.3.5 可安装包）。

本版没有 schema 变化；需要回退时，可停止实例后安装 v0.7.3.5 可安装包并继续使用同一
`dataDir`。在线 `diet_system backup` 仍只用于同版本恢复，不能替代升级前冷备份。
