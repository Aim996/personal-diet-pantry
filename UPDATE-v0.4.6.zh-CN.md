# 食序管家 v0.4.6 更新说明

`0.4.6` 基于 `0.4.5` 修复饮食记录中的库存别名误扣和确认流程卡死。

## 本次修复

### 鲜食与加工食品分离

- 用户说“番茄”时，优先匹配库存中的鲜食“西红柿”；
- “番茄”不会再因为字面包含关系自动扣减“番茄罐头”；
- 用户明确说“番茄罐头”时仍可正常匹配；
- 罐头、酱、汁、干、粉、腌、冻干、浓缩等加工形态必须与用户原话一致。

### 泛称食材

- “蘑菇”在库存只有一种合理菌菇时，可匹配“香菇”等唯一子类；
- 同时存在香菇、平菇等多个候选时，要求选择对象，不猜测扣减；
- “两口”等口语量由模型结合食物形态估算，不写死固定重量；
- 没有安全库存对象时允许记录饮食，并明确“已记录，未扣库存”。

### 饮食确认闭环

`diet_meal commit_record` 新增可选的 `confirmed` 布尔参数。低置信度预览返回
`LOW_CONFIDENCE` 后，用户回复“可以、嗯、记一下、确认”等短确认语，Agent 可
使用原 handle 和 `confirmed: true` 完成同一笔记录。

确认只解除低置信度门槛，不能绕过：

- 库存对象歧义；
- 库存不足；
- handle 过期或已失效；
- 数据库完整性错误；
- 请求参数错误。

重复提交同一笔已完成预览不会重复写入。

### 回复与数据库状态一致

失败后只能按真实结果回复：

- 已记录；
- 已记录，未扣库存；
- 未记录；
- 需要确认对象；
- 需要补充数量。

工具没有成功时，不再使用“我手动记一下”等容易让人误以为已经落库的话术。

## 数据库兼容性

本版本不新增数据库迁移，不更改现有 SQLite 表结构，并保留现有 SQLite 数据。
升级不会自动修正历史误扣；需要纠正历史数据时，请明确执行撤销或库存调整。

## 升级

1. 先停止目标 OpenClaw 测试实例；
2. 备份实例当前数据目录；
3. 用 `0.4.6` 独立归档替换 `0.4.5` Skill/plugin 文件；
4. 保留原有 SQLite 数据目录和映射；
5. 启动实例并运行一次 `diet_system self_check`；
6. 使用本文末尾的测试词进行人工验证。

## 回滚

1. 停止实例；
2. 恢复已留存的 `0.4.5` 源码或发布包；
3. 继续使用升级前备份的数据目录；
4. 启动后执行一次只读自检。

由于 `0.4.6` 不新增数据库迁移，回滚不需要改写 `schema_migrations`。

## 建议人工测试词

```text
刚啃了个番茄
刚吃了个番茄罐头
刚在外面喝了杯美式
可以，记一下
刚夹了两口蘑菇
刚吃了根火腿肠
刚喝了一大口牛奶
```

重点观察：番茄只扣西红柿；外食不扣库存；短确认后只写一次；蘑菇只有唯一
候选时自动扣减；失败回复不声称已经记录。
