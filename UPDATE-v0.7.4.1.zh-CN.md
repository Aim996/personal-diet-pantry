# 食序管家 v0.7.4.1 更新说明

产品版本：`0.7.4.1`  
技术包版本：`0.8.1`  
上一产品版本：`0.7.4.0`

## 本版目标

本版不增加新的日常饮食功能，而是把已经可用的食序管家整理成普通 OpenClaw 用户能看懂、能安全安装、能验收、能回滚的 GitHub 项目。受保护的六项两行进度回执、七类工具、40 个公开动作、时间/份量/库存/纠错/过期边界和 SQLite 事实来源均保持不变。

## 用户可见变化

- 首页改为先说明适用人群、核心能力、真实回执、最快开始和安全边界。
- 新增 MIT 许可证、变更记录、安全报告和贡献说明。
- 新增简短安装、升级、AI 操作提示词及维护者发布入口，并继续链接既有详细安装手册。
- 候选制品、CI 和 Release 流程使用同一组版本与文件名契约；公开发布仍需人工批准。

## 兼容性与数据

- 本版没有新增 migration，继续使用 migrations 001–021，schema 与 v0.7.4.0 相同。
- 安装或更新只替换 Skill/插件包并保留外部 `dataDir`，不会修改用户数据。
- v0.7.4.1 导出可被当前导入器识别，旧版导出兼容项未删除。
- 更新前仍需停止目标实例并创建、校验升级前冷备份；在线 `diet_system backup` 只用于同版本恢复，不能替代升级前冷备份。
- 若更新失败，可停止实例并重新安装 v0.7.4.0 包；数据受损时以升级前冷备份作为恢复边界。

## 当前发布状态

本文件描述的是本地候选与 GitHub 审阅内容。仓库暂时保持 Private，尚未创建 Git Tag 或 GitHub Release，也不会自动部署到任何 OpenClaw 实例。只有完整门禁通过、人工审阅批准且目标标签不存在时，才允许进入正式发布步骤。

本地候选目录顶层只包含 `personal-diet-pantry-0.7.4.1-source.tar.gz`、`personal-diet-pantry-0.7.4.1-installable.tgz`、`release-manifest.json`、`TEST-SUMMARY-v0.7.4.1.zh-CN.md`、`SHA256SUMS` 和 `GitHub文档/`。其中 `GitHub文档/` 仅作本地审阅，不作为第六个 GitHub Release 附件。
