# 食序管家 v0.7.4.7 更新说明

产品版本：`0.7.4.7`  
技术包版本：`0.8.7`

## 本版目标

本版来自 v0.7.4.6 在真实 OpenClaw 会话中的定向复测，只处理三条已经证明会阻断正常使用的
主链路：明确餐食的可食部回执、紧接上一条记录的自然纠正，以及唯一目标的删除。没有重做账本
架构，也没有扩修已经通过的饮水、时间窗、模糊份量和过期过滤行为。

## 用户可见变化

- 用户说“吃了个玉米”时保留 `1个`，不再擅自改成 `1根`。
- 玉米、带骨肉、带壳食物等存在不可食部分时，回执明确区分整物总重与可食部。没有给重量的
  玉米可以直接记录为 `玉米 1个｜可食部（玉米粒）约90克（估算）`；营养只按可食部计算。
- 用户明确说的是“带芯/带皮/带骨/连壳总重”时，总重作为原始证据保存；由比例换算出的可食部
  重量继续标记为估算，不把不可食部分算进营养。
- 用户紧接着说“其实是80克”时，只要上一条成功记录仍有唯一有效凭证，就直接原子更新并返回
  新回执，不再陷入重复确认或授权拒绝。
- 已经通过查询唯一定位的餐食可以直接删除；调用方不必再重复构造一段仅用于审计的说明文字。

## 实现边界

- 完整份量表达复用既有 `portion_evidence_json` 持久化并由统一回执渲染器读取，不新增字段或
  migration；旧记录缺少该表达时继续走原有自然数量与克重回退格式。
- `其实是` 被纳入定向更新动作识别，但更新仍必须绑定有效餐食凭证；它不会获得创建、删除或
  跨域写入权限。
- 删除接口只把 `source_text` 从公共必填参数改为可选。目标句柄/精确选择器仍然必需；缺少审计
  文字时由服务端根据已选记录生成，无法唯一定位时仍拒绝写入。

## 自动验证

- TypeScript：148 项通过。
- Python：575 项通过，4 项按平台或可选条件跳过。
- Skill 路由评估、类型生成一致性、敏感内容扫描、发布审计、可安装包桥接、升级 E2E、无源码
  安装和生产依赖审计均通过。

## 保持不变

- 明确、已发生、自然数量清楚的常见食物直接记录；“一点、一些、几口”等开放份量仍先展示
  有界估算，确认前零业务写入。
- 纯水继续只显示饮水确认和饮水进度，不展开六项营养进度。
- 餐食回执继续使用六项、固定顺序、每项两行的受保护模板。
- 查询零写入、跨轮授权隔离、连续两次失败熔断、过期食品推荐硬过滤和 `Asia/Shanghai` 通用
  时间窗保持不变。
- SQLite 仍是唯一事实来源；本版没有新增 migration，继续使用 migrations 001–021，schema
  与 v0.7.4.6 相同。
- 本地构建和实例安装不会自动推送 GitHub、创建 Tag/Release；远端发布仍需单独授权。

## 升级与回退

更新前停止目标实例并创建、校验升级前冷备份；安装时保留外部 `dataDir`。更新后核对产品版本
`0.7.4.7`、技术包版本 `0.8.7`、七类工具、自检和关键记录数量。若候选失败且数据库未变化，
停止实例并重新安装已校验的 v0.7.4.6 可安装包；在线 `diet_system backup` 只用于同版本恢复，
不能替代升级前冷备份。

候选目录顶层只包含：

- `personal-diet-pantry-0.7.4.7-source.tar.gz`
- `personal-diet-pantry-0.7.4.7-installable.tgz`
- `release-manifest.json`
- `TEST-SUMMARY-v0.7.4.7.zh-CN.md`
- `SHA256SUMS`
- `GitHub文档/`
