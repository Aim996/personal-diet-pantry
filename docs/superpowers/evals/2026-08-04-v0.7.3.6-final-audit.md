# 食序管家 v0.7.3.6 最终实施审计

日期：2026-08-04
产品版本：`0.7.3.6`
技术包版本：`0.7.9`

## 实施结论

本轮已完成设计确认范围内的实现与本地候选包验证，未上传、未安装、未停止或重启
OpenClaw。v0.7.3.5 目录未作为编辑目标；v0.7.3.6 使用独立版本目录。

完成范围：

- 声明式策略注册表以及时间、份量、库存关系、报告分类规则；
- UTC 存储与 IANA 用户时区本地投影；
- 泛化的日历、滚动、跨日和显式本地时间范围；
- 过期报告完整性元数据与加工品/原料关系证据；
- 模糊份量的可解释范围估算、联合预览和确认后提交；
- Skill 意图路由、调用预算、失败指纹去重、两次失败熔断及禁止 Exec/文件兜底；
- 跨版本约束新增时间范围、估算确认和工具证据边界三组长期保护项；
- 产品版本、技术版本、安装文档、更新说明、构建脚本和导入兼容列表同步升级。

数据库 migration 保持 `001` 至 `021`，本版没有 schema 迁移。

## 最终验证证据

- Python 主回归（隔离安装用例单独运行）：`534 passed, 4 skipped`；
- 隔离安装及无源码启动 smoke：`1 passed`；
- TypeScript/Vitest：`19` 个测试文件，`77 passed`；
- 新增长期约束、进度回执和版本合同：`35 passed`；
- 发布/构建合同：`75 passed, 4 skipped`；
- 版本合同：`24 passed`；
- Skill 结构校验：`pass`，无 finding；
- 生成工具契约检查：`pass`；
- 敏感内容扫描：`pass`，无 finding；
- 发布审计：`pass`，TypeScript/Python 的 `75` 个公共动作完全一致；
- 新鲜上下文前向评估：跨日查询、否定模糊摄入、双项目联合估算、失败终止、
  库存关系和偏好查询失败六类陌生表达全部符合设计。

一次使用较长 pytest 临时根目录时，三个深层隐私擦除测试触发 Windows 传统路径长度
限制；改用短临时根目录后相关测试 `3 passed`，随后全量得到上述 `534 passed, 4 skipped`。

## Skill 与候选包

- 主 `SKILL.md`：`379` 行，`19,444` 字节；
- 安装候选包：`dist-package/personal-diet-pantry-0.7.3.6-installable.tgz`；
- 大小：`283,522` 字节；
- SHA-256：`7D85D38A48279F5F99DB1597DBB276BC71A1BC379A0D5AC227DDEF19DC12E48C`；
- 包成员：`137`；
- 包内版本：产品 `0.7.3.6`、技术包 `0.7.9`；
- 包内 migration：`21` 个，最后一个为
  `021_package_semantics_and_product_operations.sql`；
- 包内容白名单与敏感文件拒绝检查：`pass`。

## 正式发布边界

`scripts/build_release.py` 的正式双构建、可复现性和源码快照流程按设计拒绝了当前工作区：
根 Git 工作树不是 clean 状态，且 v0.7.3.5/v0.7.3.6 当前均由 Git 报告为未跟踪目录。
因此本轮只交付已验证的本地安装候选包，没有伪造 clean-commit 来源、正式源码归档、
release manifest 或正式发布目录。待维护者明确整理并提交工作树后，才能运行正式发布构建。

## 未在本轮宣称修复

OpenClaw 宿主流式层的整段重复，以及用户中止生成后下一轮续写，需要安装后的宿主集成
证据。本版只确保 Skill 对一次用户消息从单一终态工具结果生成一次业务回复，不在缺少宿主
证据时宣称已经根治宿主流式问题。
