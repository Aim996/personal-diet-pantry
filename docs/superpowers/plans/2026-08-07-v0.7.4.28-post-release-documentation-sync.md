# v0.7.4.28 Post-Release Documentation Sync Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Synchronize the public repository and local project record with the already completed v0.7.4.28 GitHub Release without changing the immutable version, release assets, runtime behavior, or product/package versions.

**Architecture:** `docs/releases/v0.7.4.28.zh-CN.md` is the detailed public evidence record. README, CONTEXT, workflow, and maintainer release documentation contain only short current-state summaries and link to that record. A separate file at the project root preserves local operational history and is not committed or uploaded as a Release asset.

**Tech Stack:** Markdown, Git, GitHub CLI, PowerShell, Python sensitive-content scanner.

## Global Constraints

- Do not modify `E:\codx\食序管家\0.7.4.28`, tag `v0.7.4.28`, or any existing GitHub Release asset.
- Keep product version `0.7.4.28`, technical package version `0.8.28`, and migrations `001–021` unchanged.
- Do not edit runtime code, Skill files, package metadata, migrations, tests, or user-visible product behavior.
- Do not create `0.7.4.29`, a new tag, a new Release, or an OpenClaw deployment.
- Record GitHub Actions run `31161584233` as an account billing lock that stopped before any workflow step; never describe it as passing CI.
- Treat GitHub Release metadata, the peeled tag commit, `release-manifest.json`, and `SHA256SUMS` as the evidence sources in that order.
- Use branch `codex/v0.7.4.28-post-release-docs`; stage only named documentation files.

---

### Task 1: Add the Canonical Public Release Record

**Files:**
- Create: `docs/releases/v0.7.4.28.zh-CN.md`

**Interfaces:**
- Consumes: GitHub Release `v0.7.4.28`, commit `b649fd1b38f1b7a6f6eef31117ad855ddb35a641`, `E:\codx\食序管家\0.7.4.28\release-manifest.json`, and `E:\codx\食序管家\0.7.4.28\SHA256SUMS`.
- Produces: the single detailed public evidence page linked by all shorter entry documents.

- [ ] **Step 1: Re-query live Release facts before writing**

Run:

```powershell
gh release view v0.7.4.28 --repo Aim996/personal-diet-pantry --json tagName,name,isDraft,isPrerelease,publishedAt,targetCommitish,url,assets
git ls-remote origin 'refs/tags/v0.7.4.28^{}'
Get-Content -Encoding utf8 'E:\codx\食序管家\0.7.4.28\release-manifest.json'
Get-Content -Encoding utf8 'E:\codx\食序管家\0.7.4.28\SHA256SUMS'
```

Expected: public, non-draft, non-prerelease Release; five assets; tag and manifest commit both `b649fd1b38f1b7a6f6eef31117ad855ddb35a641`.

- [ ] **Step 2: Write the evidence record**

Create a concise Chinese page with these sections and exact facts:

```markdown
# v0.7.4.28 公开发布实录

## 发布身份
## 正式资产与 SHA-256
## 本地发布门禁
## GitHub Actions 外部阻塞与人工回退
## 远端回下载复核
## 安装、部署与数据边界
## 后续维护规则
```

Include Python `606/610` with 4 skipped, TypeScript `238/238`, Skill routing `43/43`, install E2E `4/4`, 21 migrations, zero production dependency vulnerabilities, zero sensitive findings, and both reproducibility flags true. Include all four hashes from `SHA256SUMS`; describe `SHA256SUMS` itself as the fifth uploaded asset, not as self-covered.

- [ ] **Step 3: Check the new record for placeholders and malformed whitespace**

Run:

```powershell
rg -n 'TBD|TODO|待定|稍后补|占位' docs/releases/v0.7.4.28.zh-CN.md
git diff --check -- docs/releases/v0.7.4.28.zh-CN.md
```

Expected: no placeholder matches and no diff-check errors.

- [ ] **Step 4: Commit the public record independently**

```powershell
git add -- docs/releases/v0.7.4.28.zh-CN.md
git commit -m "docs: record v0.7.4.28 public release"
```

### Task 2: Update Public Entry Points and Current Context

**Files:**
- Modify: `README.md`
- Modify: `README.en.md`
- Modify: `CONTEXT.md`

**Interfaces:**
- Consumes: `docs/releases/v0.7.4.28.zh-CN.md` from Task 1.
- Produces: short current-status entry points without duplicating the full evidence table.

- [ ] **Step 1: Add the release-record link to the Chinese README**

In `当前版本与状态`, add one bullet stating that the published assets were downloaded back and matched the local candidate byte-for-byte, linking to `docs/releases/v0.7.4.28.zh-CN.md`. Add the same link under `文档导航`. Do not copy hashes into README.

- [ ] **Step 2: Add the equivalent English entry**

Under `Current status`, add one sentence linking to the Chinese evidence record and saying the five public assets were downloaded and hash-matched. Do not claim GitHub Actions passed.

- [ ] **Step 3: Turn CONTEXT into current maintainer context**

Change the title from the stale `v0.7.3.5` label to `食序管家 v0.7.4.28 项目上下文`. Add a `当前正式发布` section before the existing domain language definitions containing product/package versions, Release URL, final tagged commit, 21 migrations, the five-asset contract, and a link to the release record. Preserve all existing domain definitions verbatim.

- [ ] **Step 4: Verify links and stale titles**

Run:

```powershell
rg -n 'v0\.7\.3\.5|docs/releases/v0\.7\.4\.28\.zh-CN\.md|b649fd1b38f1b7a6f6eef31117ad855ddb35a641' README.md README.en.md CONTEXT.md
Test-Path -LiteralPath docs/releases/v0.7.4.28.zh-CN.md
git diff --check -- README.md README.en.md CONTEXT.md
```

Expected: no stale CONTEXT title, the record link is present in all three entry files, and the target exists.

- [ ] **Step 5: Commit entry-point updates**

```powershell
git add -- README.md README.en.md CONTEXT.md
git commit -m "docs: link verified v0.7.4.28 release status"
```

### Task 3: Update Workflow and Maintainer Release History

**Files:**
- Modify: `GITHUB-WORKFLOW.zh-CN.md`
- Modify: `docs/RELEASING.md`

**Interfaces:**
- Consumes: the canonical release record and the actual PR/tag/Release history.
- Produces: an accurate handoff snapshot and a reusable manual-fallback audit rule.

- [ ] **Step 1: Replace the stale workflow snapshot**

Update the dated snapshot to 2026-08-07 with: public repository visibility, default branch `main`, documentation branch `codex/v0.7.4.28-post-release-docs`, product/package versions `0.7.4.28 / 0.8.28`, migrations `001–021`, formal Release URL, tagged commit `b649fd1...`, and no automatic deployment. Add `docs/releases/v0.7.4.28.zh-CN.md` to the single-source table.

Replace the sentence requiring a product version for every snapshot correction with the narrower rule: governance facts may be corrected in a documentation-only commit when they do not change code, package metadata, installation artifacts, or user-visible behavior; completed version directories and Releases remain immutable.

- [ ] **Step 2: Add the completed-release evidence section to the maintainer guide**

Append `## v0.7.4.28 已完成发布证据` to `docs/RELEASING.md`, with the Release URL, final commit, five-asset count, local gate totals, Actions run URL and zero-step billing-lock wording, manual release authorization, and remote download hash-match result. Link to the canonical release record rather than duplicating all four hashes.

- [ ] **Step 3: Verify terminology and stale status removal**

Run:

```powershell
rg -n 'Private|0\.7\.4\.16|尚未创建|v0\.7\.4\.28 已完成发布证据|billing|账单|docs/releases/v0\.7\.4\.28' GITHUB-WORKFLOW.zh-CN.md docs/RELEASING.md
git diff --check -- GITHUB-WORKFLOW.zh-CN.md docs/RELEASING.md
```

Expected: the current snapshot contains no stale private/v0.7.4.16/not-created facts; historical examples elsewhere may remain if explicitly historical.

- [ ] **Step 4: Commit workflow and maintainer history**

```powershell
git add -- GITHUB-WORKFLOW.zh-CN.md docs/RELEASING.md
git commit -m "docs: update post-release maintenance state"
```

### Task 4: Add the Local Project Release Trail

**Files:**
- Create outside Git: `E:\codx\食序管家\GitHub公开发布留痕-2026-08-07-v0.7.4.28.md`

**Interfaces:**
- Consumes: public record, PR #1, PR #2, tagged commit, Release metadata, manifest, and hashes.
- Produces: the local cross-version handoff record required by project governance.

- [ ] **Step 1: Confirm the target does not exist**

```powershell
Test-Path -LiteralPath 'E:\codx\食序管家\GitHub公开发布留痕-2026-08-07-v0.7.4.28.md'
```

Expected: `False`. Stop instead of overwriting if it exists.

- [ ] **Step 2: Write the local trail**

Include repository/visibility, PR #1 and #2 URLs, final commit/tag/Release, local candidate path, five assets and four covered hashes, test/reproducibility/sensitive-scan results, Actions billing lock, no-deployment/no-user-data statement, immutable-version rule, and next-maintainer checklist. Do not include credentials, host secrets, API keys, user records, or private OpenClaw state.

- [ ] **Step 3: Validate the local trail**

```powershell
rg -n 'sk-[A-Za-z0-9]|root:|192\.168\.|API Key|密码' 'E:\codx\食序管家\GitHub公开发布留痕-2026-08-07-v0.7.4.28.md'
Get-Content -Encoding utf8 'E:\codx\食序管家\GitHub公开发布留痕-2026-08-07-v0.7.4.28.md' | Select-Object -First 20
```

Expected: no sensitive-pattern matches; header and release identity render correctly.

### Task 5: Verify Scope, Publish the Documentation PR, and Merge

**Files:**
- Verify all files from Tasks 1–4.
- Do not modify any additional repository file.

**Interfaces:**
- Consumes: completed documentation commits and local trail.
- Produces: merged public documentation with no new tag or Release.

- [ ] **Step 1: Verify repository scope and immutable release state**

Run:

```powershell
git status --short
git diff origin/main...HEAD --name-only
git diff origin/main...HEAD --check
git diff origin/main...HEAD -- package.json openclaw.plugin.json pyproject.toml src diet_mcp skills migrations tests
gh release view v0.7.4.28 --repo Aim996/personal-diet-pantry --json tagName,isDraft,isPrerelease,url,assets
git ls-remote origin 'refs/tags/v0.7.4.28^{}'
```

Expected: only the design, plan, release record, README files, CONTEXT, workflow, and maintainer release guide changed; runtime diff is empty; Release still has five assets and the tag still peels to `b649fd1...`.

- [ ] **Step 2: Run documentation-sensitive gates**

```powershell
python scripts/scan_sensitive_content.py .
python -m pytest -q tests/test_version_contract.py tests/test_release_ref.py tests/test_release_audit.py tests/test_public_repository_surface.py tests/test_public_behavior_contract.py tests/test_build_release.py
```

Expected: zero sensitive findings and all selected version, public-surface, behavior-contract, audit, reference, and release-builder tests pass.

- [ ] **Step 3: Push the explicit documentation branch**

```powershell
git push -u origin codex/v0.7.4.28-post-release-docs
```

- [ ] **Step 4: Create a ready documentation PR**

Create a PR targeting `main` whose body states: governance-only documentation sync; no product/package version change; no runtime, Release asset, tag, deployment, or user-data change; local checks and the known zero-step Actions billing lock.

- [ ] **Step 5: Review and merge**

Verify the PR file list and diff through `gh pr view`/`gh pr diff`. If GitHub Actions again stops before all steps because of the documented billing lock, record it as external infrastructure evidence. Merge only when the local gates pass and the PR contains no out-of-scope files.

- [ ] **Step 6: Final public verification**

```powershell
git fetch origin
git show origin/main:docs/releases/v0.7.4.28.zh-CN.md
gh release view v0.7.4.28 --repo Aim996/personal-diet-pantry --json tagName,isDraft,isPrerelease,url,assets
git ls-remote origin 'refs/tags/v0.7.4.28^{}'
```

Expected: the release record is on `origin/main`; the existing Release remains public with exactly five assets; tag commit remains unchanged.
