# 食序管家 v0.6.1.5.1 自然口语自动触发实施计划

> **执行要求：** 基于 v0.6.1.5 完整提交
> `9cadffec81505645efb5ea228e21b1943434eb55`，逐项测试先行。
> 不修改软路由、线上 Skill、Telegram 配置、线上数据库或用户数据。

**产品版本：** `0.6.1.5.1`  
**内部包版本：** `0.6.2-dev.6`  
**目标：** 让普通中文口语、省略句和合理裸数字可靠命中完整
`personal-diet-pantry` Skill，同时保持七工具、55 动作、15 个迁移及全部
v0.6.1.5 业务语义不变。

**实现策略：** 触发问题首先通过 frontmatter 修复；正文只增加一个短入口契约，
并原位复用已有 Workflow、工具路由、写入决策表和体重规则。用静态契约、
正反例路由矩阵、干净构建测试和可复现发布门禁共同验收。

---

## 任务 1：冻结产品版本和发布版本映射

**先修改测试：**

- `tests/test_version_contract.py`
- `src-tests/version-contract.test.ts`
- `tests/test_build_release.py`
- `tests/integration/test_installable_e2e.py`

**RED：**

1. 要求产品版本为 `0.6.1.5.1`。
2. 要求 npm/Python/OpenClaw 内部包版本为 `0.6.2-dev.6`。
3. 要求 `package.json` 保存机器可读产品版本。
4. 要求发布清单同时包含 `product_version` 和 `version`。
5. 运行定向测试，确认仍返回 v0.6.1.5 / `0.6.2-dev.5` 而失败。

**GREEN：**

1. 同步 `package.json`、`package-lock.json`、`pyproject.toml`、
   `openclaw.plugin.json` 和 Python `__version__`。
2. 在包元数据和发布构建器中加入产品版本 `0.6.1.5.1`。
3. 保持内部包版本是合法 SemVer/PEP 440 版本。
4. 重跑定向版本测试。

## 任务 2：冻结自然口语发现契约

**先修改/新建测试：**

- 新建 `tests/contracts/test_natural_language_trigger_skill_contract.py`
- 修改 `tests/test_skill_package.py`

**RED：**

1. 解析 Skill frontmatter 并要求只有 `name`、`description`。
2. 要求 description：
   - 以 `Use when` 开头；
   - 覆盖餐食、营养饮品、纯水、做饭剩菜、库存临期、体重、报告、
     目标偏好和撤销重做；
   - 覆盖合理裸数字和任何 `diet_*` 任务；
   - 明确无需点名 Skill；
   - 排除文案/翻译、泛知识、图片、代码/示例和未发生事件。
3. 要求正文入口契约位于 `## Readiness` 之前。
4. 要求入口契约说明：
   - 按完整意图识别；
   - 接受中文口语、省略句和不完整语法；
   - 明确发生的行为继续走现有直接写入规则；
   - 否定、计划和假设不写入；
   - 加载后必须执行 Skill 全文；
   - 不因 Telegram/WebUI 渠道不同而改变业务规则。
5. 要求七个工具全部保留。
6. 要求 `SKILL.md` 行数不超过基线 724。
7. 运行测试，确认旧英文 description 和缺失入口契约导致失败。

**GREEN：**

1. 更新 frontmatter description。
2. 在标题和 Readiness 之间加入最小入口契约。
3. 合并正文中的重复说明，控制总行数。
4. 不改变任何工具 action、字段、数据库规则或回复公式。
5. 重跑定向 Skill 契约与 Skill validator。

## 任务 3：冻结正反例和七领域路由

**先修改/新建测试：**

- 扩展 `tests/contracts/test_natural_language_trigger_skill_contract.py`
- 必要时新增 `contracts/natural-language-trigger-cases.json`

**RED：**

1. 用结构化正例矩阵覆盖：
   - `105`、`105 空腹` → `diet_weight`；
   - `刚喝了 500ml 水` → `diet_water`；
   - 豆浆、早餐、外食 → `diet_meal`；
   - 做饭、吃掉、剩菜冷藏 → `diet_meal record_cooking`；
   - 坏苹果丢弃、库存、临期 → `diet_pantry`；
   - 记错、撤销、重做 → `diet_transaction`；
   - 进度、洞察、日报、周报、月报 → `diet_report`；
   - 初始化、自检、备份、恢复、目标、偏好 → `diet_system`。
2. 用反例矩阵覆盖文案、百科、图片、代码示例、订单号和未发生事件。
3. 要求 Skill 明确处理“更强语义优先”，避免 `105 个鸡蛋` 被当体重。
4. 要求已有裸数字默认 kg 和系统时间规则保持不变。
5. 运行测试，确认旧 Skill 不能完整满足矩阵。

**GREEN：**

1. 只补齐入口层缺失的领域词和优先级说明。
2. 复用现有 write decision table，不再复制第二张有冲突的决策表。
3. 重跑自然口语、体重、饮食、饮水和库存 Skill 契约。

## 任务 4：修复干净工作区测试顺序

**先修改测试：**

- 新建或扩展 CI 脚本契约测试；
- 目标文件 `ci/verify.ps1`。

**RED：**

1. 在没有预生成 `dist/index.js` 的干净副本中执行 TypeScript 包内容测试。
2. 证明当前顺序先运行 `npm test`、后运行 `npm run build`，使
   `src-tests/package-contents.test.ts` 因缺少 `dist/index.js` 失败。
3. 静态要求 `npm run build` 出现在 `npm test` 之前。

**GREEN：**

1. 把 TypeScript build 移到 TypeScript tests 之前。
2. 不放宽安装包内容断言，不提交预生成产物作为绕过。
3. 在干净目录运行 build → test 并确认通过。

## 任务 5：更新发布器和用户文档

**先修改测试：**

- `tests/test_build_release.py`
- `tests/test_release_audit.py`
- 文档静态契约测试。

**RED：**

1. 要求 GitHub 文档集包含本设计和本计划。
2. 要求发布器使用 `0.6.2-dev.6` 文件名，并在清单中声明产品版本
   `0.6.1.5.1`。
3. 要求更新说明明确：
   - v0.6.1.5.1 是完整 v0.6.1.5 的增量；
   - 7 工具、55 动作、15 迁移不变；
   - 数据结构和现有运行数据不变；
   - 本构建未部署软路由。

**GREEN：**

1. 新建 `UPDATE-v0.6.2-dev.6.zh-CN.md`。
2. 更新 README、RELEASE、发布器文档白名单和版本链接。
3. 保持详细开发文档只进入源码包/GitHub 文档，不进入运行时 Skill。

## 任务 6：全量验证

按顺序运行并保存机器报告：

1. 自然口语、体重、版本、发布和 CI 定向测试。
2. 全量 Python pytest。
3. TypeScript build。
4. 全量 Vitest。
5. Python compileall。
6. Skill validator。
7. release audit。
8. npm pack dry-run 与安装包端到端测试。
9. 生产依赖审计和已接受开发依赖风险漂移检查。
10. 从干净工作区再次执行完整 `ci/verify.ps1`。

任何一项失败都不能进入正式构建。

## 任务 7：生成 v0.6.1.5.1 发布物

1. 提交全部源码、测试和文档，使发布构建工作区干净。
2. 使用发布器各构建两次源码包和可安装包。
3. 比较两次成员列表及 SHA-256。
4. 生成：
   - `personal-diet-pantry-0.6.2-dev.6-source.tar.gz`
   - `personal-diet-pantry-0.6.2-dev.6-installable.tgz`
   - `release-manifest.json`
   - 当时版本专用的哈希清单（文件名以该版本构建实现为准，不沿用后续版本命名）
   - 机器测试摘要
   - GitHub 文档集
5. 检查归档不含 `.sqlite`、数据库备份、令牌、密钥、日志和运行数据。

## 任务 8：写入项目根目录成果

目标目录：

```text
C:\path\to\personal-diet-pantry\0.6.1.5.1-自然口语自动触发\
```

写入：

- `01-v0.6.1.5.1设计与执行计划.md`
- `02-v0.6.1.5.1开发完成报告.md`
- `发布包\` 下的全部验证发布物

同时更新：

```text
C:\path\to\personal-diet-pantry\食序管家总的后续迭代路线图.md
```

路线图只在全部门禁通过后把 v0.6.1.5.1 标为完成，并记录提交、测试数、
迁移数、发布包哈希和“不含部署”的事实。

## 完成门槛

- 产品版本只写 v0.6.1.5.1；
- 内部包版本只写 `0.6.2-dev.6`，并明确其技术用途；
- 正反例发现契约全部通过；
- 七工具、55 动作、15 迁移保持不变；
- `SKILL.md` 不超过 724 行；
- 全部 Python/TypeScript/构建/Skill/审计/安装/升级门禁退出码为 0；
- 两类归档均可复现且哈希已记录；
- E 盘版本目录和总路线图已经更新；
- 软路由、Telegram 配置、线上数据库和用户数据均未修改。

