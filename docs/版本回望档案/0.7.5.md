# 0.7.5 版本回望档案

## 当前状态

- 产品版本：`0.7.5`
- 技术包版本：`0.9.0`
- 状态：本地候选实现与自动化门禁已完成，尚未安装或发布
- 源码基线：v0.7.4.28 内容等价提交 `6a61e4d268740e52ddb9a2f65008b17e43820ef4`
- 回退基线：v0.7.4.28 代码与升级前冷备份

## 为什么值得修改

v0.7.4.x 的局部补丁已经让明确餐食、称重和普通入库进入不必要确认或直接失败。继续增加正则和确认句柄的边际收益为负。本版只保留经过验证的账本、事务和查询底座，重写口语写入决策与入库默认值。

## 允许范围

- CORE-01、CORE-02、CORE-04、CORE-07、SAFE-05 的实现收口；
- 普通入库取消日期必填，新增位置/到期估算及来源；
- 产品/技术版本、迁移 022、Skill 和对应文档测试。

## 相邻保护项

CORE-03、CORE-05、CORE-06、CORE-08、CORE-09、CORE-10、SAFE-01～SAFE-07、GOV-01、GOV-02、UI-01 均不得弱化。

## Good Flags

| Flag | 当前状态 | 证据 |
|---|---|---|
| GF-001 | 通过 | 普通餐食仍使用六项、每项双行的正式回执合同 |
| GF-002 | 通过 | 纯水仍只输出饮水双行，不展开热量和宏量营养 |
| GF-003 | 通过 | “刚啃了根玉米”直接写入；自然单位与可食部估算规则保留 |
| GF-004 | 通过 | 紧接上一笔的明确重量纠正直接更新，不重新预览或确认 |
| GF-005 | 通过 | 在独立 `0.7.5` worktree 开发，v0.7.4.28 未修改 |

## 证据与已知缺陷

开发前真实 UAT 已复现：玉米确认循环、体重单位/确认循环、入库强制到期日和默认位置错误；纯水直接写与短回执正常。实施时先加入真实口语红灯用例，再完成以下绿灯：

- TypeScript：24 个文件、268 项通过；
- Python：617 项通过、4 项跳过；
- Skill 合同：37 项通过；
- 迁移 022：5 项通过，旧库存行保留并标记 `legacy_unknown`；
- 明确库存进食固定为一次 `diet_pantry search` 加一次 `diet_meal record`，禁止二次库存探测和额外确认；
- `SKILL.md` 为 19,987 字节，仍低于 OpenClaw 的 20,000 字节加载上限。

这些是本地自动化证据，不等同于软路由实机 UAT。候选包构建、安装和真实口语验收仍需分别留证。

## 继承边界

值得继承的是 SQLite 单一事实源、营养证据、Meal/Pantry 事务、撤销返库、查询和正式回执渲染。不得整体照搬 v0.7.4.x 的活动预览、确认句柄和分散式口语路由。
