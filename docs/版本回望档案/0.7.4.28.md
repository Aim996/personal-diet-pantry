# 0.7.4.28 版本回望档案

## 版本身份

- 产品版本：`0.7.4.28`
- 技术包版本：`0.8.28`
- 基线：公开 `main` 提交 `4341e93f9721c54b080c8eb90ae2c8604a3dead1`（0.7.4.27 / 0.8.27）
- 变更类型：公开分发、安装文档与正式 Release
- migration：无，保持 `001–021`
- 生产部署：不在本轮范围

## 为什么值得修改

公开仓库已存在，但首页仍描述为 Private/发布准备中，且远端没有实际 Release。普通用户和其他智能体无法取得可验证安装包，现有安装提示词也无法完成。修复这项事实断裂能直接让项目从“可阅读源码”变成“可安全安装”，不需要触碰运行时业务行为。

## 允许变化

- 当前产品/技术版本与资产文件名。
- README、安装、更新、排障、AI 提示词、Release Notes 和维护者发布说明。
- Release workflow 的当前版本参数。
- 对应版本/文档/构建/发布合同测试。
- 正式 Git Tag 与 GitHub Release。

## 禁止变化与影响矩阵

| 约束 | 本版处理 | 验证方式 |
|---|---|---|
| CORE-01～CORE-10 | 不修改 | 既有核心 Python/Vitest 门禁与全量测试 |
| SAFE-01～SAFE-07 | 不修改 | 授权、事务、删除、时间、数据安全合同测试 |
| GOV-01 | 遵守：新开 0.7.4.28，0.7.4.27 不动 | 版本合同、目录存在性与 Git diff |
| GOV-02 | 遵守：所有用户保留项原样 | 产品行为不变量与 Skill 合同测试 |
| UI-01 | 不修改：纯饮水仅显示饮水反馈 | 既有纯水回执测试 |
| GF-001 | 保留 | 全量回归 |
| GF-002 | 保留 | 全量回归 |
| GF-003 | 保留 | 全量回归 |
| GF-004 | 保留 | 全量回归 |
| GF-005 | 保留 | 全量回归 |

## 发布验证结果

- 发布合同红灯：新版本测试首次得到 Python `36 failed, 54 passed, 3 skipped`，Vitest `2 failed`；失败均指向实现仍为 0.7.4.27/0.8.27、缺少新文档或未生成 dist，证明测试能够拦截旧实现。
- 发布合同绿灯：版本/公开文档 `35 passed`；构建器与隔离安装 `54 passed, 4 skipped`；TypeScript 版本与包成员合同通过。
- 完整 `ci/verify.ps1`：通过。
- 核心受保护行为门禁：`35 passed`；Skill 路由 `43/43`，safety/overall 均为 `1.000`。
- Python 全量：`605 passed, 4 skipped, 0 failed`（609 项）。
- Vitest 全量：`238 passed, 0 failed`（45 个 suites）。
- 敏感信息扫描：通过，`0 findings`。
- Release audit：通过；产品 0.7.4.28、技术包 0.8.28、七域 75 个公开动作的 TypeScript/Python 合同一致。
- 安装 E2E：`4 passed`。
- 生产依赖审计：`0 vulnerabilities`；开发依赖风险接受合同通过。
- 最终发布环境：Python 3.12.13；Node.js 22.23.1；npm 10.9.8。Node 便携包来自 Node.js 官方发行目录，SHA-256 为 `7df0bc9375723f4a86b3aa1b7cc73342423d9677a8df4538aca31a049e309c29`。
- 可复现构建：由最终干净提交上的 `scripts/build_release.py` 再次执行并写入 `release-manifest.json`；只有两个归档均为 reproducible 才允许创建 Tag/Release。
- 最终 main commit：以 annotated tag `v0.7.4.28` 指向和 `release-manifest.json.commit_sha` 为准。
- Tag：`v0.7.4.28`。
- Release URL：<https://github.com/Aim996/personal-diet-pantry/releases/tag/v0.7.4.28>。
- GitHub Actions 状态：账户计费锁定，工作流不能启动；本项明确为“未运行”，不得记为通过。正式 Release 依赖上述本地完整门禁和可复现构建证据人工创建。
