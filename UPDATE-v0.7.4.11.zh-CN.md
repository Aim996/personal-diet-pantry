# 食序管家 v0.7.4.11 更新说明

产品版本：`0.7.4.11`  
技术包版本：`0.8.11`

## 本版解决什么

0.7.4.10 已把“其实是80克”从连续工具失败修成一次原子更新，但真实 OpenClaw 回执仍错误显示 `约80克（估算）`。0.7.4.11 只修复这项精度证据：

- 更新继续使用安全 `meal_handle` 或唯一 selector，并提交完整的新 `items`。
- 本次用户纠错句作为 `source_text` 必须随更新传入，它是新的份量证据和事务审计原因；未变化的时间、餐次和地点仍由服务端保留。
- “其实是80克”会把旧的 `约90克（估算）` 或误传的 `约80克（估算）` 归一为 `80克`。
- “其实大概80克”仍保留近似和估算语义，不会被系统改成精确事实。
- 新增主流程继续显示 `1个｜可食部（玉米粒）约90克（估算）`；只在用户明确纠正后移除估算标记。

## 明确没有改变

- 没有新增 migration，也没有新增工具、action、数据库表或通用 Patch；继续使用 migrations 001–021。
- 普通餐食仍固定六项、每项两行；纯水仍只显示饮水双行。
- 模糊份量零写入、只读隔离、库存联动、相对时间、过期推荐硬过滤、删除安全和 WebUI 均不在本轮修改范围。

## 更新与回退

本版与 0.7.4.10 使用同一数据库 schema。更新前保留经过校验的升级前冷备份；在线 `diet_system backup` 只用于同版本恢复。安装、自检或口语验收失败时停止实例并重装已验证的 v0.7.4.7 包，数据库异常时再恢复冷备。不得覆盖 0.7.4.10 的提交、候选目录、包或校验值。

## 候选制品合同

- `personal-diet-pantry-0.7.4.11-source.tar.gz`
- `personal-diet-pantry-0.7.4.11-installable.tgz`
- `release-manifest.json`
- `TEST-SUMMARY-v0.7.4.11.zh-CN.md`
- `SHA256SUMS`
- 本地审阅留痕目录 `GitHub文档`

## 发布状态

当前内容仍是候选。自动化、可复现构建、软路由安装、广泛真实 UAT 和测试数据清理全部通过前，不得标记完成。仓库脚本不会自动推送 GitHub、创建 Tag/Release。
