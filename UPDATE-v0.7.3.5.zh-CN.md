# 食序管家 v0.7.3.5 更新说明

## 本版目标

v0.7.3.5 增加约束文件必读和用户指定保留功能的登记门禁。每个变更批次开始前必须完整
阅读约束文件；用户明确要求长期保留的功能必须在实现前登记，未经用户针对该功能作出明确
修改许可，后续不得修改。本版技术包版本为 `0.7.8`，产品版本为 `0.7.3.5`。

## 更新前必须阅读约束

- 每次设计、审查、编辑、测试、打包或部署开始前，都必须完整阅读当前版本的
  `docs/PRODUCT-BEHAVIOR-INVARIANTS.zh-CN.md`。
- 不能用记忆、摘要、旧版本文档或他人转述代替本次阅读。
- 阅读后才能确定本轮允许修改的边界和需要继承的受保护行为。

## 用户指定保留功能

- 用户明确说某项功能、格式、流程或行为“需要保留”“不得删除”或具有同等明确语义时，
  必须把它视为长期保护指令。
- 该功能必须在同一变更批次、开始实现前登记进约束文件，并同步运行时规则、测试和用户示例。
- 不能只把承诺留在聊天、计划或开发者记忆中，也不能推迟到以后再登记。

## 修改受保护行为的授权

- 未经用户明确授权，不得删除、弱化、改序、缩减、替换或改变受保护行为的语义。
- “整体优化、重构、简化、提升体验”等宽泛要求不构成修改授权。
- 有效授权必须点名受保护功能、具体修改内容和允许范围；授权不得扩张、复用或推定。
- 授权范围不清楚时保持原样并询问，不得先修改后确认。

## 版本迭代

本次规则变更没有覆盖 v0.7.3.4，而是创建 v0.7.3.5，并同步递增技术版本。既有版本目录
和制品继续保持不可变；后续新需求将在新的变更批次中创建下一版本。

## 数据迁移与兼容

本版没有新增 migration，继续使用 migration 001–021，schema 与 v0.7.3.4 相同。
导入继续接受 0.6.7、0.7.0、0.7.1、0.7.2、0.7.3、0.7.3.1、0.7.3.2、0.7.3.3、
0.7.3.4，并接受 v0.7.3.5 可移植导出。升级继续使用原专用 `dataDir`。

## 发布制品

正式发布目录顶层恰好包含：

- `personal-diet-pantry-0.7.3.5-source.tar.gz`
- `personal-diet-pantry-0.7.3.5-installable.tgz`
- `release-manifest.json`
- `TEST-SUMMARY-v0.7.3.5.zh-CN.md`
- `SHA256SUMS`
- `GitHub文档`

## 升级与回退

本项目不会自动部署、停止或重启 OpenClaw。升级前停止目标实例，创建并校验包含已提交
WAL 数据的一致 SQLite 升级前冷备份，同时保留 v0.7.3.4 可安装包。安装后运行
`diet_system self_check`，再验证受保护的六项进度回执和库存效果。

本版没有 schema 变化；需要回退时，可以停止实例后安装 v0.7.3.4 并继续使用同一
`dataDir`。在线 `diet_system backup` 仍只用于同版本恢复。
