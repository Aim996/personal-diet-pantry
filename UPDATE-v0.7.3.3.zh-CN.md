# 食序管家 v0.7.3.3 更新说明

## 本版目标

v0.7.3.3 是一次回复层兼容修复。它恢复已经确认的写入后六项营养进度回执，阻止
“简洁回复”规则把固定两行格式压缩成单行摘要，并把该行为纳入跨版本约束和自动化门禁。
技术包版本为 `0.7.6`，面向用户和导出 manifest 的产品版本为 `0.7.3.3`。

## 恢复的进度回执

- 成功写入且目标已确认时，固定显示热量、蛋白、脂肪、碳水、纤维、饮水六项。
- 每项固定两行，进度条固定 10 格；超目标百分比保持真实，进度条本身封顶。
- 第二行显示本次工具实际返回的增量与增量百分比；小于 1% 的正增量使用 `+<1%`，
  零增量省略，增量不使用括号。
- 纤维未知保持“未知”，饮水保持“当前 ml / 目标 L”。
- 成功回执不再追加 `📊 今日进度：` 摘要标题、主动评价、剩余额度或饮食建议。

## 最新数据来源

餐食或饮水写入后的回复只读取最终成功工具结果里的 `daily_progress` 与
`inventory_effects`。Skill 不复用上一轮进度快照，也不为了重建回执额外调用
`diet_report progress`，因此不会出现“已经记录新食物，但总量仍是记录前数值”的回执。

## 长期约束

新增 `docs/PRODUCT-BEHAVIOR-INVARIANTS.zh-CN.md`，记录已经由用户确认、后续不得静默
删除或弱化的行为。进度回执的精确规则继续位于
`skills/personal-diet-pantry/references/water-and-progress.md`，并由
`tests/test_skill_progressive_disclosure.py` 强制检查。普通成功回执的 2–3 行偏好已明确
排除必需的六项结构化进度块。

## 数据迁移与兼容

本版没有新增 migration，继续使用 migration 001–021，schema 与 v0.7.3.2 相同。
v0.7.3.3 导入继续接受 0.6.7、0.7.0、0.7.1、0.7.2、0.7.3、0.7.3.1、0.7.3.2，
并接受 v0.7.3.3 可移植导出。升级保留原专用 `dataDir`，不重建或改写个人数据库。

## 发布制品

正式发布目录顶层恰好包含：

- `personal-diet-pantry-0.7.3.3-source.tar.gz`
- `personal-diet-pantry-0.7.3.3-installable.tgz`
- `release-manifest.json`
- `TEST-SUMMARY-v0.7.3.3.zh-CN.md`
- `SHA256SUMS`
- `GitHub文档`

安装器只接收 `installable.tgz`；源码归档仅用于审阅与复现。

## 升级与回退

本项目不会自动部署、停止或重启 OpenClaw。升级前应停止目标实例，创建并校验包含
已提交 WAL 数据的一致 SQLite 冷备份，同时保留 v0.7.3.2 可安装包。安装 v0.7.3.3
后运行 `diet_system self_check`，再以一条测试餐食或饮水记录核对六项回执和库存效果。

如需回退，可停止实例后安装 v0.7.3.2 并继续使用同一 `dataDir`；升级前冷备份仍用于
应对安装损坏、磁盘或环境故障。在线 `diet_system backup` 只用于同版本恢复。

## 非目标

- 不改变营养计算、库存扣减、做饭、撤销/重做或数据库 schema。
- 不新增远程部署、多用户、凭据管理或自动图片识别。
- 不把本次格式修复解释为医疗建议或营养数值准确性的额外保证。
