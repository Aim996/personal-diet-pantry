# 食序管家 v0.7.4.6 更新说明

产品版本：`0.7.4.6`  
技术包版本：`0.8.6`

## 本版目标

本版只解决真实 OpenClaw 会话中已经复现的明确进食主链路问题：用户已经说“吃了个玉米”或
“吃了根火腿肠”时，不再因为常见重量需要估算而强制预览确认；记录完成后，用户补充“80克”
时可以精确修正刚才那一条。修改前完整读取项目约束并对 v0.7.4.5 做了多场景 UAT；纯饮水、
模糊开放份量、查询隔离、失败熔断、时区查询与过期食品过滤等已经通过的行为不扩修。

## 用户可见变化

- “吃了个玉米”“吃了根火腿肠”等明确、已发生且自然数量清楚的常见食物直接记录，不再仅因
  标准重量和营养属于估算而要求确认。
- 回执第一行同时保留用户的自然单位与克重，例如
  `已记录！火腿肠 1根 50克（估算）｜84.8 kcal`；其后继续使用受保护的六项两行进度格式。
- 用户紧接着说“其实80克”时，只要刚写入记录的目标凭证仍有效且唯一，就直接原子更新，返回
  `已更新！火腿肠 1根 80克｜135.68 kcal` 和更新后的完整进度，不再先查一次、再确认一次。
- “吃了点花生”“吃了一些菜”“吃了几口”等数量本身开放的表达仍展示重量范围和营养估算，
  确认前零业务写入；它们没有被改成静默直写。
- 纯水继续只显示饮水确认与饮水进度，不展开热量、蛋白、脂肪、碳水和纤维。

## 工具与账本修复

- 工具现在能同时表达自然计数 `1根` 与标准份量估算 `50g`。新注册的
  `portion.standard_count_weight` 仅用于“自然数量已清楚、克重来自标准份量”的情况，
  不用于“一点、一些、几口”等开放份量。
- 份量估算的范围、单位、证据类型和策略键保存在既有营养证据中；刷新、查询和回执可以继续
  区分估算重量与用户后来确认的精确重量，同时不新增 migration、不改变旧事务行结构。
- 成功的餐食新增与修正会返回内部不透明记录凭证；它只用于后续精确修改/删除，不在用户回复中
  展示，也不允许脱离当前授权把别的记录当作目标。
- 公共更新不再一律强制 `_preview_only`。带有效记录凭证、字段完整的明确修正直接原子提交；
  没有唯一凭证、目标有歧义或关键事实缺失时仍先查询/澄清。
- 若更新参数把完整餐食字段平铺在顶层，桥接层会把可识别的五个餐食字段一次归入 `draft`；
  同时提供嵌套与平铺字段则视为冲突并拒绝，不做覆盖猜测。
- 当 `nutrition_facts` 与 `nutrition_estimate` 结构和值完全一致时，桥接层保守保留
  `nutrition_estimate`；只要任一字段不同仍返回明确错误。标签/用户精确数据只用 facts，
  标准食物或常见份量数据只用 estimate，营养按 `per_100g`、`per_100ml`、
  `per_serving` 或 `consumed_total` 恰好缩放一次。

## 保持不变

- 七类公共工具和 40 个日常公开动作不变。
- 查询消息绝对零写入；停止、否定、取消和上一轮任务不能在后续复活。
- 同一宿主轮次连续两次终止性 Diet 失败后立即熔断，不扫描源码、不切换到 Exec 或文件兜底。
- 过期库存永远不进入推荐候选；已经发生的过期摄入仍按事实记录为 consume，不伪装成丢弃。
- 相对时间继续统一按 `Asia/Shanghai` 的通用时间窗解析；相同内容的两次进食不会擅自去重。
- 纯饮水短回执和餐食六项两行回执均为受保护行为。
- SQLite 仍是唯一正式事实来源；本版没有新增 migration，继续使用 migrations 001–021，
  schema 与 v0.7.4.5 相同。

## 验证结果

- TypeScript：146 项通过。
- Python：573 项通过，4 项按平台/可选条件跳过。
- 已覆盖明确计数直写、份量估算证据跨读取保留、成功写入返回凭证、50克改80克的单轮原子修正、
  冲突营养拒绝、平铺 update 归一、旧事务撤销/重做、打包白名单、桥接和无源码安装。
- 软路由真实 OpenClaw 口语化验收与测试数据清理由本地候选安装流程单独留痕；本地构建本身不会
  修改用户数据。不会自动推送 GitHub、创建 Tag/Release；远端发布必须另行授权。

## 升级与回退

更新前停止目标实例并创建、校验升级前冷备份；安装时保留外部 `dataDir`。更新后核对产品版本
`0.7.4.6`、技术包版本 `0.8.6`、七类工具、自检和关键记录数量。若候选失败且数据库未变化，
停止实例并重新安装已校验的 v0.7.4.5 可安装包；在线 `diet_system backup` 只用于同版本恢复，
不能替代升级前冷备份。

候选目录顶层只包含：

- `personal-diet-pantry-0.7.4.6-source.tar.gz`
- `personal-diet-pantry-0.7.4.6-installable.tgz`
- `release-manifest.json`
- `TEST-SUMMARY-v0.7.4.6.zh-CN.md`
- `SHA256SUMS`
- `GitHub文档/`
