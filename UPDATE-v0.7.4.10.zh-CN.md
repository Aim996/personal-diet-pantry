# 食序管家 v0.7.4.10 更新说明

产品版本：`0.7.4.10`  
技术包版本：`0.8.10`

## 本版解决什么

0.7.4.9 已让“吃了个玉米”恢复一次直记，但软路由真实会话紧接着说“其实是80克”时，更新接口依次要求模型补 `source_text`、`meal_type` 等没有变化的旧字段，最后触发熔断。用户表述、目标记录和新重量都已明确，失败根因是接口仍把一次局部事实纠正当成必须由调用方重发的整条餐食。

新增成功回执继续明确保留 `1个｜可食部（玉米粒）约90克（估算）`；本版只让紧接着的精确重量纠正可靠落到同一条记录。

0.7.4.10 只收紧这条核心纠错链路：

- 先用 `meal_handle` 或唯一 selector 安全选中一条活动餐食并校验版本。
- 调用方提交完整的新 `items` 集合；未提供的发生时间、餐次、原始事实文本和地点由服务端从被选中记录保留。
- 调用方显式提供的新餐次、文本、地点或时间仍优先，不会被旧值覆盖。
- stale handle、歧义 selector、已删除记录和并发变更继续拒绝，不会因为补全而放宽目标身份。

## 明确没有改变

- 没有新增 migration，也没有新增公开工具、action 或数据库表；继续使用 migrations 001–021。
- 没有引入通用 JSON Patch；多项餐食更新仍提交完整 `items`，避免猜测局部目标。
- 明确摄入仍直接记录；真正模糊的份量仍先展示有界估算并确认，确认前零业务写入。
- 普通餐食仍固定显示六项、每项两行；纯水仍只显示饮水双行。
- 库存、时间范围、过期推荐硬过滤、删除安全、只读权限隔离和 WebUI 均不在本轮修改范围。

## 更新与回退

本版与 0.7.4.9 使用同一数据库 schema。更新前停止实例并保留经过校验的升级前冷备份；在线 `diet_system backup` 只用于同版本恢复，不能替代升级前冷备份。安装只使用 `personal-diet-pantry-0.7.4.10-installable.tgz`。安装、自检或真实口语验收失败时停止实例并重装已验证的 v0.7.4.7 包；数据库异常时再从对应冷备恢复。不得覆盖 0.7.4.9 的候选目录、包或校验值。

## 候选制品合同

正式候选目录顶层包含：

- `personal-diet-pantry-0.7.4.10-source.tar.gz`
- `personal-diet-pantry-0.7.4.10-installable.tgz`
- `release-manifest.json`
- `TEST-SUMMARY-v0.7.4.10.zh-CN.md`
- `SHA256SUMS`
- 本地审阅留痕目录 `GitHub文档`

## 发布状态

当前仓库内容仍是候选。只有自动化全矩阵、可复现构建、安装包审计、软路由安装、多场景真实 OpenClaw UAT 和测试数据清理全部通过后，才可把 0.7.4.10 标记为完成或上传 GitHub。仓库脚本不会自动推送 GitHub、创建 Tag/Release。
