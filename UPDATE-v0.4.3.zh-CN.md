# 食序管家 v0.4.3 更新说明

v0.4.3 是面向 OpenClaw 的稳定性与数据准确性修复版本。它延续 v0.4.2
的饮食记录、库存扣减、剩菜流转、模糊匹配、营养表和进度条合同，不引入
新的数据库迁移。

## 本次修复

- 库存新增和预览省略 `added_at`、`source_text` 时，由插件安全补齐系统
  字段；`added_at` 始终来自插件可信当前时间，不接受公开 schema 禁止的
  `context.now` 控制。通过记录句柄删除饮水时也不再要求模型补造
  `source_text`。
- `INVALID_INPUT` 可返回安全的字段级错误诊断。Skill 只修正工具指出的
  字段，且字段级错误最多修正一次；无可重试诊断时立即停止，避免盲目
  更换数量、单位或动作。`food_name`、`quantity`、`unit` 的缺失现在可从
  正常工具执行路径获得这一诊断；Python 服务内部仍严格要求完整字段。
- 明确包装规格必须精确计算：`package_count × quantity_per_package`
  决定正式库存总量。例如 `6 × 330ml` 精确保存为 `1980ml`，不再用
  `2000ml` 等估算值覆盖。包装字段不完整时统一返回可执行的
  `INVALID_INPUT(package_specification)`；计算完成后三个包装字段会被
  删除，不传给 Python，也不进入预览或工作流记录。
- 精确数量写入现有 SQLite `REAL` 前会执行十进制往返校验。`1980`、`0.3`
  等常见值可准确入库；超大整数或高精度小数若会静默舍入，则返回可恢复
  的 `INVALID_INPUT(quantity)`，且不写入批次、移动、事务或预览。
- JavaScript 已无法安全表达的 `quantity`、`package_count` 或
  `quantity_per_package` 会在插件边界、调用 Python 前被拒绝。此类精确值
  必须直接使用原始十进制字符串，不能把已经舍入的 `number` 再转成字符串。
- 完整商品名称保存在 `food_name`，`normalized_name` 只用于模糊匹配，
  包装营养表继续关联到对应库存批次。
- Skill 文档以 UTF-8 保存，确认无常见乱码并补充中文硬规则，同时保留
  成功后不查询完整库存、不显示内部诊断等既有回复约束。

## 兼容性

- 可从 v0.4.2 直接升级，OpenClaw 最低兼容版本仍为 `2026.5.17`。
- SQLite 表结构不变；现有库存、营养表、饮食记录、事务和个人偏好不会
  被重写或删除。
- 新的包装规格字段仅用于工具调用层的精确换算，最终仍只写入现有数量、
  单位和重量字段；本版本不新增数据库迁移。
- 精确十进制字符串属于现有数量字段的兼容输入形式，不改变 SQLite 列或
  已存数据。
- 安装后继续使用原来的数据目录，无需重新初始化或导入数据。

## 升级方式

1. 升级前使用食序管家的备份功能创建并校验一份 SQLite 备份。
2. 用 v0.4.3 包替换旧插件，保持原有 `dataDir` 配置不变。
3. 安装依赖并构建插件，然后重启 OpenClaw。
4. 调用 `diet_system self_check`，确认数据库、六个工具和 Skill 均可用后
   再进行写入。

如需回退，可恢复升级前备份并重新安装 v0.4.2；由于本版本没有数据库迁移，
无需重建库存或营养数据。
