# 食序管家 v0.4.4 更新说明

v0.4.4 为库存增加完整的保质期写入、旧数据补全和剩余时长展示，同时保留
现有 SQLite 数据。此前的 v0.4.3 稳定修复版已经单独留存，本次发布不会
覆盖其留存副本。

## 主要更新

- 新入库、预览入库、剩菜和熟食库存都必须写入具体 `expires_at`。
- 日期来源按“用户明确日期、包装或营养标签日期、模型估算”依次选择。
  信息不足时由模型结合食品状态和储存位置给出偏保守的具体估算，不重复
  询问用户。
- 旧库存可使用 `missing_expiry_only` 分页查找，再通过
  `preview_update_metadata` 和 `commit_update_metadata` 仅补充到期时间。
- 库存查询新增简短的剩余时长，例如“剩余5天”“剩余18小时”
  “已过期5小时”或“待补保质期”。
- 过期食品不会自动记为已吃、自动清空或丢弃；库存变动仍以用户明确意图
  为准。
- 普通饮食回复继续只显示本次库存变化，不返回完整库存，减少无关输出和
  模型消耗。

## 数据兼容

本版本沿用 SQLite 已有的 `pantry_batches.expires_at` 列，不新增保质期
数据表，也不重建数据库。升级时：

- 保留现有 SQLite 数据、饮食记录、营养关联、库存数量和个人偏好；
- 无需清理旧数据库；
- 无需重建或重新导入库存；
- 缺少日期的旧批次按需补全，不在每顿饭后扫描全库。

建议升级前照常创建一份经过校验的备份，并保持原来的 `dataDir` 配置。
升级后运行 `diet_system self_check`，确认六个工具和数据库均可用。

## 版本留存

v0.4.3 已单独留存，可作为稳定修复基线。v0.4.4 使用新的独立版本目录和
发布包；本更新步骤本身不删除或修改 v0.4.3 的留存内容。
