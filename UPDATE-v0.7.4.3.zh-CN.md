# 食序管家 v0.7.4.3 更新说明

产品版本：`0.7.4.3`
技术包版本：`0.8.3`

## 本版目标

本版是一次安全止损迭代，只处理真实测试中会损坏账本可信度的四类问题：查询触发写入、
复合句部分提交、Meal 参数映射失控、体重删除误删。没有把历史体重补记、剩菜保质期推断
或完整跨域事务系统塞进本轮，也没有为了版本号扩大功能面。

## 当前轮次写入闸门

- 插件从 OpenClaw 宿主的当前消息和 `runId` 建立写入权限，不再只相信模型生成的工具参数。
- “刚才记上了吗”等纯查询只能读取；查询发现缺记录也不能自行补写。
- 单域消息只授权用户当前表达的具体动作，例如“吃了玉米”只允许新增餐食，不能借同域权限
  删除或修改旧餐食；疑问句即使包含“删除、撤销、记一下”等词也仍按查询处理。
- 同一句同时要求餐食、饮水、体重、库存或资料中的两个以上域写入时，在第一次业务写入前
  整体阻断并保持零写入，避免“体重和水成功、餐食失败”的部分提交。
- 模糊份量的预览仍可进行，因为它不产生业务写入；提交必须在后续纯确认轮次使用有效句柄。
- 上一轮已停止、取消或失败的任务不会为新一轮提供写入权限；同一轮累计两次终止性失败后
  对全部 Diet 调用熔断。

## Meal 参数归一化

常见中文餐次和地点在调用 Python 业务层前确定性转换，例如：

- 午餐 → `lunch`
- 加餐 → `snack`
- 家里 → `home`
- 外卖 → `takeout`

无法映射的值直接返回带 `field / reason / expected` 的 `INVALID_INPUT`，不再把中文枚举连续
提交给底层，也不允许失败后扫描源码或 `node_modules` 寻找调用方式。

## 体重删除确认

体重删除改为两阶段工作流：第一次用选中的记录句柄只生成零业务写入预览，公开精确体重、
UTC 测量时间、用户本地时间和时区；第二次只有纯确认携带一次性 `commit_handle` 才会删除。
提交前重新核对记录版本，错误、过期、已消费或普通查询句柄都不能删除记录。

## 保留行为

- 七个公共工具入口和 40 个日常公开动作没有变化。
- 六项、每项两行的成功进度回执保持不变。
- 库存生命周期、纯水换算、餐食库存联动、撤销/重做和既有时区范围规则保持不变。
- 普通“吃了个玉米”、饮水、体重、入库、模糊份量预览和带句柄纠错均有回归测试，避免安全
  闸门把正常单域流程一并封死。

## 数据迁移与兼容

本版没有新增 migration，继续使用 migrations 001–021，schema 与 v0.7.4.2 相同。体重删除
确认复用既有工作流表及已允许的引用类型，没有手工修改 SQLite，也没有数据搬迁。

导入器新增识别 v0.7.4.3 导出，同时继续接受所有既有受支持版本。需要回退时，保留
v0.7.4.2 可安装包和升级前冷备份；在线 `diet_system backup` 仍只用于同版本恢复，不能替代
升级前冷备份。

## 发布边界

本地候选目录顶层固定包含 `personal-diet-pantry-0.7.4.3-source.tar.gz`、
`personal-diet-pantry-0.7.4.3-installable.tgz`、`release-manifest.json`、
`TEST-SUMMARY-v0.7.4.3.zh-CN.md`、`SHA256SUMS` 和审阅用 `GitHub文档/`；正式 Release
只上传前五个文件。

本轮只生成本地候选，不会自动安装到路由器、停止或重启 OpenClaw，也不会自动推送 GitHub、
创建 Tag、Release 或合并分支，不会修改用户数据。仓库尚未创建 Git Tag 或 GitHub Release。
只有完整门禁通过且用户另行明确要求，才进入安装或远端发布。
