# 食序管家 v0.7.3 更新说明

## 本版目标

v0.7.3 把 v0.7.2 已有的定向库存搜索扩展成可直接执行的日常闭环：保留用户原始包装单位、按商品跨批次扣减、正确处理本地日历到期日，并让已经做好的剩菜复用原营养快照直接记录。工具响应同时增加稳定结果语义，减少模型在失败后重复试错。本次发布只生成可安装包和离线镜像，不会自动部署或修改 OpenClaw、软路由及现有 `dataDir`。

## 包装语义持久化

入库现在可同时保存基础数量与用户看到的包装数量：`display_quantity`、`display_unit`、`base_quantity_per_display_unit` 和可选的 `package_hierarchy` 都进入同一个 `diet.sqlite`。例如“两盒、每盒 180g”不会在后续展示中退化成只有 360g；剩余包装数由正式基础库存确定性换算，而不是由模型临时猜测。

## 商品级扣减与 FEFO

`diet_pantry search` 在唯一商品匹配时签发 `inventory_match_handle`。已使用库存调用 `deduct`，已丢弃库存调用 `discard`；工具会在一个事务内按配置的已开封优先、临期优先顺序跨物理批次扣减。相同商品的多个批次不再被当成商品歧义，库存不足时整笔操作失败且不产生部分扣减。

## 日历到期日

入库、库存元数据和剩菜都可直接提交本地 `expiry_date`（`YYYY-MM-DD`）。系统在用户配置时区内解析该日期的当天结束时间，同时保留日历日期供展示，避免模型手工拼接时区偏移造成提前或延后一天。只有明确的时刻才使用 `expires_at`。

## 熟食剩菜直接记录

做饭产生的剩菜批次会保留不可变的熟食营养快照。以后说“把冰箱那盒剩菜吃了”时，先定向搜索取得 `prepared_food_handle`，再调用 `diet_meal record_prepared`：只扣熟食批次，复用已保存的营养，不重新扣原材料。撤销会恢复熟食批次，原材料库存保持不变。

## 结果与失败恢复

所有公共响应保留兼容字段 `ok`，并新增一个明确 `outcome`：

- `write_committed`：正式写入已提交；
- `preview_ready`：预览或确认信息已准备，但业务数据尚未写入；
- `read_completed`：只读请求已完成；
- `no_op`：目标状态已经满足，本次没有新增事务；
- `failed`：请求失败，不得声称数据已经改变。

可修复的 `INVALID_INPUT` 会返回安全的字段、原因、期望格式与是否可重试。相同会话、相同参数、相同确定性失败在五分钟内可复用，避免原样重复工具调用；数据库忙、句柄过期、超时和结果不确定不会进入该缓存，仍可按状态证据自救。

## Skill 路由

主 `SKILL.md` 只增加四条紧凑路线，具体参数继续按需读取参考文件：带包装入库、商品级使用/丢弃、熟食剩菜直接记录和本地日历到期日。36 条自然语言行为用例全部覆盖通过，主 Skill 仍保持单层导航和体积预算，避免 DeepSeek 为一个简单请求读取全部参考文档。

## 数据迁移与兼容

首次由 v0.7.3 打开数据库时会应用 migration 021：为库存批次增加包装语义字段，并扩展商品操作与熟食引用所需的受信工作流类型。已有 migration 020 搜索索引、营养档案、餐食、饮水、体重、库存、撤销/重做和报告数据均保留。升级在原 `dataDir` 上原地迁移；不要手工修改 SQLite 或 `schema_migrations`。

## 验证结果

发布门禁会从干净提交重新生成工具契约，执行完整 Python 与 TypeScript 测试、TypeScript 构建、Skill 校验、发布审计和安装包内独立启动测试。实际通过数、跳过数、提交号和 SHA-256 只写入构建生成的 `TEST-SUMMARY-v0.7.3.zh-CN.md`、`release-manifest.json` 与 `SHA256SUMS`，本文不预报数字。

正式发布目录顶层恰好包含 `personal-diet-pantry-0.7.3-source.tar.gz`、`personal-diet-pantry-0.7.3-installable.tgz`、`release-manifest.json`、`TEST-SUMMARY-v0.7.3.zh-CN.md`、`SHA256SUMS` 和 `GitHub文档/` 六项。源码包用于审阅，只有 installable 包可交给插件安装器。

## 升级与回退

升级前先停止目标实例，并用随源码发布的 `scripts/cold_backup.py` 创建、校验一份实例停止状态下的一致 SQLite 升级前数据库备份；同时保留 v0.7.2 可安装包。安装 `personal-diet-pantry-0.7.3-installable.tgz` 时继续使用原专用 `dataDir`，启动后运行 `self_check` 并以只读查询检查库存包装、临期日期、餐食和体重。

若必须回退，应再次停止实例，恢复升级前数据库备份，再安装 v0.7.2 可安装包。不能只降级代码并继续使用已经执行 migration 021 的数据库。在线 `diet_system backup` 适合同版本恢复，不能替代跨版本回退所需的升级前冷备份。

## 非目标

v0.7.3 不包含 Docker 或远程主机部署，不接管 OpenClaw/Telegram 网关，不管理模型 API 密钥，不自动识别图片，也不修改生产数据。创建发布包、复制离线镜像、安装插件、启用插件和迁移生产数据仍是彼此独立的授权步骤。
