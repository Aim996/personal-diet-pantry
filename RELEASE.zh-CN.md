# 食序管家（Personal Diet Pantry）v0.7.5.3

食序管家是一套面向单个 OpenClaw 用户、本地优先的饮食、饮水、体重与家庭食材管理 Skill。产品版本为 `0.7.5.3`，npm/OpenClaw/Python 技术包版本为 `0.9.3`。

## 当前状态

这是本地开发候选说明，不代表 Git Tag、GitHub Release、路由器安装或真实数据验收已经完成。只有完整测试、敏感信息扫描、可复现构建和人工批准全部通过后，才可创建 `v0.7.5.3` Release；发布也不会自动部署到任何 OpenClaw 实例。

## 核心变化

- 修改营养目标改为真实的“预览 → 一次确认提交”凭证链；预览不改账本，重复确认不会重复修改。
- “刚吃了一个玉米”等已经完成、时间就是当前的摄入不再为了钟点二次询问；Skill 只提供判断边界，不规定普通正向口语的固定工具路线。
- 固体食物的化学含水量不计入饮水进度；纯水保持短回执，豆浆、牛奶等有明确饮用体积的营养饮料仍可贡献饮水。
- 用户明确给出的过去到期日可按事实入库；过期食品仍从推荐候选中硬过滤。
- 同一 `dataDir` 的正式写入在进程内串行执行，避免批量操作争抢 SQLite 写锁；只读查询继续并发。

玉米可食部说明、普通餐食六项双行回执、SQLite 事实源、库存联动事务、跨时间范围查询、查询零写入、否定/未来/他人行为零写入和跨域部分提交阻断保持不变。

## 发布制品

正式 Release 只上传以下五个文件；`GitHub文档/` 仅保留在本地候选目录：

| 文件 | 用途 |
| --- | --- |
| `personal-diet-pantry-0.7.5.3-installable.tgz` | OpenClaw npm-pack 可安装包。 |
| `personal-diet-pantry-0.7.5.3-source.tar.gz` | 干净提交的源码快照，不可交给插件安装器。 |
| `release-manifest.json` | 提交、环境、测试、可复现性和制品哈希证据。 |
| `TEST-SUMMARY-v0.7.5.3.zh-CN.md` | 当次构建生成的测试总计、通过、跳过和失败数。 |
| `SHA256SUMS` | 覆盖前四个文件的 SHA-256。 |

## 数据迁移与回退

本版新增 migration 023，只为目标更新预览增加合法操作类型；它不会改写既有餐食、饮水、体重、库存或目标。migration 022 的库存批次位置来源、到期来源、旧值保留及 `legacy_unknown` 兼容规则保持不变。

从 v0.7.5.2 更新前必须停止实例并创建、校验位于 `dataDir` 外的 SQLite 冷备份。回退时必须停止实例、恢复这份升级前冷备份，再安装 v0.7.5.2；不能只换回旧代码后继续使用已应用 migration 023 的数据库。在线 `diet_system backup` 仅用于同版本恢复，不能替代升级前冷备份。

完整变化见 [v0.7.5.3 更新说明](UPDATE-v0.7.5.3.zh-CN.md)，安装和升级入口分别见 [docs/INSTALL.md](docs/INSTALL.md) 与 [docs/UPGRADING.md](docs/UPGRADING.md)。
