# 食序管家 v0.7.4.19 更新说明

产品版本：`0.7.4.19`
技术包版本：`0.8.19`

## 为什么开发这一版

v0.7.4.18 的自动化、构建和安装门禁曾经通过，但真实 OpenClaw 口语化 UAT 证明它仍不能作为产品验收基线：

- 库存已经明确登记 `2盒×250ml` 和每盒营养，用户说“喝了一盒”时仍会因 `per_serving requires servings` 失败。
- “2盒，每盒250毫升”的自然中文入库可以生成预览，但确认提交又夹带数量和单位，造成确认循环或失败。
- “吃了个玉米”已保留 `1个` 和可食部约 90 克，但当模型没有重复提交营养字段时，底层会真实落成营养未知，Agent 却误说已使用标准估算。
- 餐食安全删除成功后只返回一行结果，没有继承用户已经锁定的六项双行回执。

这四个现象实际归于两条边界：“包装事实如何进入正式账本”与“餐食提交如何返回可信最终结果”。本版只收口这两条边界，不引入联网营养查询、新数据库表或新业务域。

## 本版修复

### 1. 包装事实只解析一次

- 工具边界把明确的中文 `克/毫升` 规范化为账本基础单位 `g/ml`。
- 用户原话中唯一、明确的“每盒/瓶/包 X 克/毫升”会生成基础总量和自然展示量；冲突或不完整的关系不猜测。
- `commit_add` 只消费预览凭证中的不可变事实。用户修改了数量或单位时必须重新预览；纯“确认/记上”不再覆盖旧预览。

### 2. 库存包装、份数和营养同源

- 库存搜索凭证已证明 `1盒=250ml` 时，餐食服务使用同一关系得到 `250ml=1份`。
- 每份标签营养按该份数计算，不再把 250ml 当作 250 份。
- 一次餐食事务同时提交餐食、标签营养和扣库；事务撤销后餐食消失、库存返回原数量。
- 没有统一包装证据的按份标签不猜份数，继续返回结构化缺失信息。

### 3. 玉米营养与可食部证据闭环

- 本地种子库增加煮甜玉米可食部的每 100 克常见估算数据，标记为 C 级证据并保留不确定性。
- 当餐食 item 已明确 `可食部（玉米粒）约90克（估算）` 却没有直接营养字段时，现有本地营养解析器可得到 90 kcal 及对应宏量营养。
- 玉米芯不计入营养重量；用户紧接改为 80 克时，按同一可食部对象重算，明确 80 克不再标估算。

### 4. 删除也返回正式餐食回执

- 安全删除完成后，从已提交的删除结果重新聚合当日进度。
- 回执顺序继续为热量、蛋白、脂肪、碳水、纤维、饮水，每项两行；本次变化使用负增量。
- 目标尚未确认时仍不伪造进度条，保留原有“目标尚未确认”提示。

## 明确保持不变

- 明确摄入一次直接记录；不为常见可食部估算增加写入前确认。
- “吃了点花生/尝了几口”仍只问一个有用问题，回答前业务零写入。
- 纯水仍只显示饮水确认和饮水双行，不展开五项营养。
- 时间窗、过期推荐硬过滤、查询零写入、否定/计划/他人零写入和失败熔断不改。
- 不修改 OpenClaw WebUI 会话按钮问题；本版没有新增 migration、数据库字段或公开 action，继续使用 migrations 001–021。
- 0.7.4.18 及更早的正式目录、包、校验值和历史证据保持不变。

## 更新与回退

本版没有新增 migration，与 v0.7.4.18 共用并继续使用 migrations 001–021。更新前必须停止实例，在 `dataDir` 之外创建并校验 SQLite **升级前冷备份**；在线 `diet_system backup` 只用于同版本恢复，不能替代升级前冷备份。安装失败且数据完整时，可重新安装已校验的 v0.7.4.18 包恢复技术运行。v0.7.4.18 的真实 UAT 结论仍是失败，回退不等于产品验收通过。

## 候选制品契约

本地候选目录顶层必须恰好包含：

- `personal-diet-pantry-0.7.4.19-source.tar.gz`
- `personal-diet-pantry-0.7.4.19-installable.tgz`
- `release-manifest.json`
- `TEST-SUMMARY-v0.7.4.19.zh-CN.md`
- `SHA256SUMS`
- 本地审阅留痕目录 `GitHub文档`

正式 GitHub Release 只上传前五个文件；`GitHub文档` 不上传。

## 重点口语化验收

1. `吃了个玉米`：一次直接落账，回执明确 `1个｜可食部（玉米粒）约90克（估算）`，有完整营养与六项双行进度。
2. `其实是80克`：唯一定位上一条并一次更新，改成可食部 80 克，不再标估算。
3. `把刚才整条玉米删掉`：用可信餐食凭证安全删除，返回六项双行进度与负增量。
4. `买了2盒UAT19原味燕麦奶，每盒250毫升，放冰箱`：一次预览和纯确认后入库为 `500ml（2盒）`。
5. `刚喝了一盒库存里的UAT19原味燕麦奶`：一次 summary 搜索加一次 meal record；餐食为 `1盒/250ml/1份/120kcal`，库存从 2 盒变 1 盒。
6. 撤销该燕麦奶餐食后，餐食消失、库存恢复 2 盒；再清理 UAT 库存。
7. 全量回归纯水短回执、模糊花生、查询零写入、否定/计划/他人、等价跨午夜查询、过期过滤和库存建议。

## 发布状态

当前是开发候选。只有全仓自动化、Skill 压力测试、可重复构建、安装包审计、软路由冷备份与安装、真实 OpenClaw 全保护矩阵、UAT 数据清理和数据库核验全部通过后，才能标记为可用候选。本项目不会自动推送 GitHub、创建 Tag 或 Release。
