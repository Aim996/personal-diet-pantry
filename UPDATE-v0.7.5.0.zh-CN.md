# 食序管家 v0.7.5.0 更新说明

## 本版定位

v0.7.5.0 不是继续堆补丁，而是把日常主链路重新收敛到四件事：记录摄入、记录体重、管理家庭库存、结合目标与可用库存回答“该吃什么”。产品版本为 `0.7.5.0`，技术包版本为 `0.9.1`。

本版保留 SQLite 账本、事务、库存联动、营养折算、跨时段查询、过期食品硬过滤、六项双行餐食回执和纯水短回执；重写的是 Skill 与智能体的职责边界：Skill 提供能力地图、证据要求和安全边界，智能体理解普通正向口语并选择能力，插件只拦截不安全写入。

## 用户可见变化

- “吃了个玉米”“刚啃了根玉米”“炫了两根火腿肠”等明确已经发生、数量可执行的单域事实由智能体直接选择记录，不再由封闭短语表规定唯一路线，也不再先问一次、确认后又问一次。
- 玉米保留用户的自然单位 `1个`，同时明确显示营养按“可食部（玉米粒）约 90 克（估算）”计算；用户随后说“其实 80 克”时，唯一目标直接原子更新。
- “一点、一些、几口、几粒、一小把”等开放数量不直接写库：有可信范围时只展示一次估算预览，没有可信范围时只问一个具体问题。
- “刚称了下 106.8”属于明确称重语境，默认单位为 kg 并直接记录；孤立数字 `106.8` 仍不会被擅自当作体重。
- 普通入库不再强制生产日期或保质期。食物、数量和单位明确时直接入库；未给位置时按品类推断冷藏、冷冻或常温，未给到期日时按入库日期和品类估算。
- 推断的位置和估算的到期日都有来源标记；用户明确给出的位置或日期始终优先，不把系统估算伪装成用户事实。
- 普通查询仍为只读；否定、计划、他人行为和已取消事件继续保持零写入。
- 跨域复合写入在真正的后端原子事务完成前继续整句拒绝，避免出现体重和饮水写入成功、餐食失败的部分提交。
- 正向短语路由器已删除。插件不会因为匹配到某个动词就预先决定领域；智能体第一次选择安全写入后，插件才锁定该轮领域和动作，防止后续游移和重复写入。

## 回执不变项

- 普通餐食继续使用热量、蛋白质、脂肪、碳水、纤维、饮水六项，每项固定两行，并展示本次增量。
- 纯水继续只显示饮水确认和饮水进度，不展开热量等五项。
- 成功回执只采用最终成功工具返回的 `rendered_receipt`，不重复拼接、压缩或补写另一套总量。

## 数据迁移

本版新增 migration 022，为库存批次增加：

- `storage_location_source`：`user`、`inferred` 或 `legacy_unknown`；
- `expiry_source`：`user`、`estimated` 或 `legacy_unknown`。

升级不会重算或改写旧库存。旧记录的数量、位置和到期时间原样保留，新增来源统一标为 `legacy_unknown`。新记录才会区分用户事实和系统默认值。

## 升级与回退

从 v0.7.4.28 升级前必须停止实例，并在 `dataDir` 外创建、校验一致的 SQLite 冷备份。v0.7.5.0 首次启动会应用 migration 022。

由于旧版本不认识 migration 022，不能仅替换回 v0.7.4.28 程序包后继续使用已升级数据库。需要回退时，应停止实例、恢复升级前冷备份，再安装已校验的 v0.7.4.28 可安装包。在线 `diet_system backup` 仅用于同版本恢复，不能替代这份升级前冷备份。

正式候选顶层包含 `personal-diet-pantry-0.7.5.0-source.tar.gz`、`personal-diet-pantry-0.7.5.0-installable.tgz`、`release-manifest.json`、`TEST-SUMMARY-v0.7.5.0.zh-CN.md`、`SHA256SUMS` 和本地审阅用 `GitHub文档`；只有前五项上传 Release。

## 边界

- 本地开发完成不等于已经部署到 OpenClaw；当前尚未部署，安装、启用、重启和真实数据验收必须由单独操作明确执行。
- 本版不修改 OpenClaw WebUI 的会话按钮、消息队列或会话标题行为。
- 本版不新增菜谱平台、健康诊断、家庭多人账户或与核心账本无关的功能。
- CORE-01～10、SAFE-01～07、GOV-01～02、UI-01 和 GF-001～006 继续作为发布门禁。
