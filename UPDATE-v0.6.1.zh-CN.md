# 食序管家 v0.6.1 更新说明

v0.6.1 是一次“数据正确性与写入可靠性”版本。它不扩张六类工具的数量，而是统一餐食营养的计量口径、写入确认、重复提交、库存事实、时区和可追溯性，让已经存在的能力产生可信数据。

## 本次解决的核心问题

### 1. 营养不再二次放大

每个直接营养输入现在必须声明一种明确口径：

| `nutrition_basis` | 输入含义 | 必须同时提供 | 缩放方式 |
| --- | --- | --- | --- |
| `per_100g` | 每 100 克营养 | `consumed_weight_g` | `重量 ÷ 100` |
| `per_100ml` | 每 100 毫升营养 | `consumed_volume_ml` | `体积 ÷ 100` |
| `per_serving` | 每份营养 | `consumed_servings` | `份数` |
| `consumed_total` | 本次吃掉的整份总营养 | 无额外缩放维度 | 固定为 `1` |

工具不再猜测“传入的是每百克还是整份”。缺少匹配维度、口径冲突或水分超过明确体积/质量时会拒绝写入。回归用例固定验证：

- 500ml 豆浆按每 100ml 数据计算为约 165 kcal、475ml 隐性水分；
- 已经是整份总营养的小红薯不会再按重量放大；
- “两个小红薯 + 500ml 豆浆”的组合不会回到 1494/1594 kcal 或 2375/2530ml 饮水的错误结果。

### 2. 明确已发生的行为直接记录

“吃了、喝了、买了、入库了”等清楚且已发生的行为直接提交。家庭库存匹配不确定时只放弃或延后库存扣减，不阻止已经发生的餐食事实。只有关键数量/单位缺失、营养输入无效或明确的摄入量低置信度才进入预览/确认。

预览仍然存在，但只是歧义流程和旧调用兼容入口。未消费的预览至少保留 24 小时，降低短暂重连或重启后确认失效的概率。

### 3. 重试不会重复记一餐

餐食会根据稳定的业务事实生成 `intake_fingerprint`：时间归一到 UTC 分钟，名称和单位经过 NFKC/大小写规范化，数值使用规范十进制，条目排序后再计算摘要；用户原话不进入指纹。相同已完成事件因确认、网络重试或模型重放再次到达时，返回原有结果而不是创建第二餐。请求级和语义级回执继续生效，餐食指纹补上跨调用形式的最后一道幂等保护。

### 4. 库存只以正式 movement 为准

餐项只有在同一事务真正产生 `pantry_movements(movement_type='consume')` 时，才保存库存扣减重量并在回复中显示库存变化。没有 movement 就等于没有扣库存，不能依据模型计划值或餐项字段宣称已经扣减。

做饭场景把“整道菜原料扣减”和“本次吃掉比例”分开计算。测试覆盖 6 个鸡蛋和 10ml 油做菜、吃 2 份、保存 4 份，以及撤销/重做后的原料、剩菜和营养守恒。

### 5. 数据可追溯但更少保存敏感上下文

迁移 013–014 新增：

- 餐食的事件时区与本地日期；
- 业务幂等指纹；
- 会话标识的 SHA-256 摘要，不再保存完整会话键；
- 计算状态与来源可追溯状态；
- 餐项的体积和份数；
- 一餐项一条的营养计算证据，包括口径、输入事实、缩放系数、来源等级、数据/规则版本、份量证据和警告。

置信度只保留到 3 位小数。结构化目标字段不会被误学成个人偏好。

### 6. 历史数据可以只读审计

新增只读营养审计并接入 `self_check`，检测：

- 隐性水分超过已知体积或按克计食品质量；
- 缺少营养口径或缩放系数；
- 声称“完整”但计算未经验证；
- 餐食总计与叶子餐项之和不一致；
- 餐项声称扣库存但没有对应 movement；
- 餐型与保存的本地时间明显不匹配。

审计不会擅自修改历史餐食，也不会在普通用户回复中暴露数据库 ID。`diet_report(progress)` 新增 `nutrition_quality`，分别呈现字段完整性、计算状态和来源状态；“字段齐全”不再等同于“计算可信”。

## 数据文件与派生文件

正式业务状态仍只有运行数据目录中的 `diet.sqlite`。库存、餐食、饮水、目标、偏好、事务和营养证据都在同一个 SQLite 数据库中。

以下内容不是额外事实来源：

- `reports/`：可从 SQLite 重建的日报、周报和月报；
- `backups/`：运维备份；
- `exports/`：显式导出的副本；
- `cache/`：可失效缓存；
- 健康检查与发布报告：诊断或构建证据。

源码目录和可安装包不包含个人数据库、报告、备份、凭据或完整会话键。

## 从 v0.6.0 升级

1. 停止目标 OpenClaw 实例的写入。
2. 完整备份原 `dataDir`，尤其是 `diet.sqlite`、`diet.sqlite-wal` 和 `diet.sqlite-shm`。
3. 使用 `personal-diet-pantry-0.6.1-installable.tgz` 安装，保留原 `dataDir`。
4. 启动后等待迁移 013–014 完成。
5. 运行 `diet_system(action="self_check")`。
6. 只读核对今日进度、最近餐食、饮水和库存；关注营养历史审计汇总。

迁移只增加字段、证据表和索引，不会自动重算或覆盖历史营养。既有历史数据因此可能显示 `unverified`/`untraceable`，这是如实表达旧数据证据不足，不是迁移失败。

## 回滚

迁移 013–014 后不可只把代码降回 v0.6.0 并继续使用已经迁移的数据库。正确回滚方式是：

1. 停止写入；
2. 恢复升级前完整数据目录备份；
3. 恢复 v0.6.0 软件包；
4. 启动并执行自检。

不要手工删除列、修改 `schema_migrations` 或伪造迁移 checksum。

## 非目标

本版本没有自动修正历史餐食、没有把估算升级为医疗级营养结论、没有新增图片识别、Docker、远程托管或多用户能力，也没有改动用户的线上数据目录。历史问题应先审计，再由后续受控工具逐项处理。
