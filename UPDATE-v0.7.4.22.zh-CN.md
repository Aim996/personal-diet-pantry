# 食序管家 v0.7.4.22 更新说明

产品版本：`0.7.4.22`
技术包版本：`0.8.22`

## 为什么开发这一版

v0.7.4.21 修住了跨会话把查询候选误当操作凭证的问题，但完整真实 OpenClaw UAT 又暴露出四个共用入口问题：自然说“刚称了”没有进入体重写入域；饮水写入后顺便查询总量会被整轮降成只读；历史餐食里的“碳水”被误识别成饮水域；退货确认时模型漏传当前原句，导致参数错误和重复确认。

这些问题都发生在自然语言进入工具之前的同一个意图与授权边界，所以本版只修这个中央入口，不重做业务事务、回执模板或数据库。修复目标是减少无意义确认和重试，同时保持查询零写入、跨域阻断和跨会话安全边界。

## 本版修复

- “刚称了106.66公斤”识别为明确已发生的体重事实，可直接调用一次体重记录。
- “刚喝了149毫升水，帮我记一下并告诉我今天一共喝了多少”识别为单一饮水域的写入加查询；先成功记录，再读取今日总量。
- 餐食营养中的“碳水”不再触发饮水域，带明确时间的历史餐食可以保持单域处理。
- 同会话说“其实可食部是80克”时，允许在“其实/实际上”和“是”之间保留短食物部位描述，并继续使用可信餐食句柄完成一次纠正。
- 已授权的库存调整、扣减、退货、丢弃、启封、冷冻、解冻和元数据预览若漏传 `source_text`，宿主入口补入当前用户原句；不会把这项补齐扩展到不接受该字段的其他工具。

## 授权边界

- 只有“明确写入命令、已发生事实、唯一业务域”同时成立时，句中附带的同域查询才不会把整轮降为只读。
- “刚才记上了吗？”等状态查询保持只读；“如果我删掉会怎样？”等假设表达不获得写权限。
- 一句话同时写体重、饮水和餐食仍在首次提交前整体阻断，不允许部分成功。
- “刚才那个”之类依赖上下文的餐食纠正或删除仍必须使用当前会话可信句柄；新会话查询到的候选不能直接升级为写权限。
- 失败熔断、句柄作用域和查询零写入规则继续生效。

## 明确保持不变

- 明确摄入继续直接记录；玉米回执明确显示 `1个｜可食部（玉米粒）约90克（估算）`，玉米芯不计入营养。
- 普通餐食继续使用六项双行进度回执；纯水继续只显示饮水确认和饮水双行，不展开热量与营养。
- 模糊开放份量、包装换算、营养证据、库存扣减与撤销返库、时间窗、过期推荐、否定/计划/他人隔离均不修改。
- 不修改 Meal/SQLite 事务，不新增 action、数据库字段或 migration，不处理 OpenClaw WebUI“新会话”按钮问题。
- v0.7.4.21 及更早目录、包、哈希和记录保持不可变。

## 更新与回退

本版没有新增 migration，继续使用 migrations 001–021，schema 与 v0.7.4.19 相同。更新前必须停止实例，并在外部 `dataDir` 之外创建和校验**升级前冷备份**；在线 `diet_system backup` 只用于同版本恢复，不能替代升级前冷备份。只安装已校验的 `personal-diet-pantry-0.7.4.22-installable.tgz`。如果安装、自检或真实授权 UAT 失败，保持数据不变并重新安装已校验的 v0.7.4.19 包恢复技术运行。

## 重点口语化验收

1. “刚称了106.66公斤”：直接记录一次；随后“撤销刚才”只撤销该体重。
2. “刚喝了149毫升水，帮我记一下并告诉我今天一共喝了多少”：一次写入、一次查询，最终纯水回执保持简洁；随后撤销测试数据。
3. “补记昨天22:00吃了1个UAT22测试饭团，200千卡，蛋白质5克，脂肪4克，碳水35克，纤维2克”：不得误判为饮水或多域；按明确时间记录后可精确删除。
4. 同会话“吃了个玉米”后说“其实可食部是80克”：直接纠正；新会话说“把刚才那个玉米删了”不得删除候选。
5. 入库两盒临时燕麦奶后明确“确认退货”：一次库存处置成功，不因漏传 `source_text` 进入重试循环。
6. 复核“刚才记上了吗？”保持只读、跨域复合写入保持零写入、时间窗等价、过期食品推荐硬过滤、否定/计划/他人零写入。

## 候选制品契约

本地候选目录顶层必须恰好包含：

- `personal-diet-pantry-0.7.4.22-source.tar.gz`
- `personal-diet-pantry-0.7.4.22-installable.tgz`
- `release-manifest.json`
- `TEST-SUMMARY-v0.7.4.22.zh-CN.md`
- `SHA256SUMS`
- 本地审阅留痕目录 `GitHub文档`

当前仍是开发候选。只有总约束保护矩阵、全量自动化、可重复构建、安装包审计、停机冷备份、软路由安装、真实口语化 UAT、测试数据清理和数据库核验全部通过，才能标记为可用；本项目不会自动推送 GitHub、创建 Tag 或 Release。
