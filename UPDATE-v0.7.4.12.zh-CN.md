# 食序管家 v0.7.4.12 更新说明

产品版本：`0.7.4.12`  
技术包版本：`0.8.12`

## 为什么开发这一版

0.7.4.11 在真实 OpenClaw 验收中证明，明确饮食、精确纠错、纯饮水、只读查询、模糊份量零写入和时间窗查询已经基本守住；但库存入库在“用户先说包装数量，随后补充到期日”时仍会陷入确认死循环。

根因不是库存数据库不能写，而是公开工具面没有把已有的库存预览/提交协议提供给模型：补充信息后模型只能再次调用直接 `add`，安全守卫又必须拒绝跨消息拼接写入。0.7.4.12 不放宽守卫，而是接通已有的安全协议。

## 本版修复

- 公开 `diet_pantry preview_add` 与 `diet_pantry commit_add` 两个已有工作流动作；七类业务工具不变，公开日常动作由 40 个调整为 42 个。
- 库存事实不完整时只追问缺失项；用户补充后，使用当前消息和已收集草稿生成一次完整预览，并返回短期有效确认句柄。
- 用户回复“确认记上”“就记上吧”等纯确认短语时，只能用上一轮有效句柄调用 `commit_add`；不得重新调用 `add`，也不得只靠旧聊天文本补写。
- 句柄缺失、过期、被取消或内容不一致时保持零写入，并重新展示或追问，不降低查询/写入隔离。
- 最终答复必须逐字段使用本轮最后一次成功工具回执；不得把纠错前的热量、蛋白质、脂肪、碳水、纤维或饮水数值拼回最终答复。

## 明确没有扩展

- 没有新增数据库表、字段或 migration；没有新增 migration，继续使用 migrations 001–021。
- 没有增加通用 Patch、跨域大事务或新的饮食业务范围。
- 明确摄入直接记录、精确纠错直接原子修改、模糊份量零写入、普通餐食六项两行、纯饮水只显示饮水、库存账本、时间窗查询、过期食物硬过滤和只读建议均保持不变。
- “吃了个玉米”仍直接记录并明确显示 `1个｜可食部（玉米粒）约90克（估算）`；用户随后明确“其实是80克”时直接原子纠正并移除估算标记。
- 0.7.4.11 及更早版本的提交、候选目录、安装包和校验值保持不可变。

## 更新、回退与候选制品

本版与 v0.7.4.7 使用相同的数据库 schema。更新前必须保留实例停止时取得并校验过的冷备份；在线 `diet_system backup` 只用于同版本恢复。安装、自检或口语化验收失败且数据未受损时，停止实例并重新安装已经验证的 v0.7.4.7 可安装包；若数据库或记录数量异常，先从升级前冷备份恢复，再做代码回退。

候选目录顶层契约为：

- `personal-diet-pantry-0.7.4.12-source.tar.gz`
- `personal-diet-pantry-0.7.4.12-installable.tgz`
- `release-manifest.json`
- `TEST-SUMMARY-v0.7.4.12.zh-CN.md`
- `SHA256SUMS`
- 本地审阅留痕目录 `GitHub文档`

## 重点口语化验收

1. “刚买了两盒测试用原味燕麦奶，每盒250毫升，放冰箱里。”
2. “2026年8月20日到期。”
3. “确认记上。”

预期：第一句只追问缺失到期日；第二句只生成一份完整预览；第三句通过同一有效句柄提交一次。全链路不得重复追问确认、不得重复入库，最终库存必须是 2 盒 / 500 毫升。

还必须复测：玉米直接记录与80克纠错、纯饮水双行、只读查询零写入、模糊花生零写入、否定/未来/他人语义、等价时间窗、过期推荐过滤，以及纠错后六项进度是否全部来自最新回执。

## 发布状态

当前内容仍是开发候选。只有自动化、可复现构建、软路由安装、广泛真实 UAT、测试数据清理和基线复核全部通过后，才能将 0.7.4.12 标记为可用候选。仓库脚本不会自动推送 GitHub、创建 Tag/Release。
