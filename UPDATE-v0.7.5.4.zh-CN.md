# 食序管家 v0.7.5.4 更新说明

技术包版本：`0.9.4`。基线为已发布的 `v0.7.5.3 / 0.9.3`；旧版本目录、安装包、校验值和 Release 均未修改。

## 本次只修四处

- 紧接成功餐食说“其实可食部是80克”时，整轮始终使用提示阶段捕获的稳定会话身份；工具钩子的临时 `sessionId` 不再覆盖它。目标唯一时直接执行一次原子更新，不再先查询、再二次确认。
- 调用方提供的 C/D 级营养估算可以让纤维、钠和食物含水等未知字段保持缺失；未知会作为未知保存，不能为了通过校验伪造为零。固体食物仍不会增加饮水累计。
- 用户陈述家中实际存在某种食品就是库存事实，不需要额外说“入库”。已过期项照实记录并标记过期，同时继续从所有食用建议中硬过滤。
- 安装文档补上 Docker 前台 Gateway 的真实重启验证：安装命令成功不等于运行时已换新，必须确认 Gateway 进程或容器启动时间发生变化，再检查七类工具和版本。

## 明确保留

- 明确已摄入直接记录；玉米保留 `1个`，营养采用的可食部与估算标记保持可见。
- 普通餐食固定六项双行回执；纯水只显示饮水短回执。
- 真正模糊份量只澄清一次，确认前零写入。
- 查询、否定、未来计划和他人行为零写入；停止任务不在下一轮复活。
- 推荐自动排除全部已过期食品；相对时间范围、库存联动和删除安全边界不变。
- Skill 继续提供能力地图、证据和边界，不增加面向某一句口语的封闭关键词路线。

## 数据、更新与回退

本版没有新 migration，继续使用 migrations 001–023，不改写既有业务事实。更新前仍应停止目标实例并创建、校验冷备份；在线 `diet_system backup` 不能替代升级前冷备份；安装时保留原外部 `dataDir`。回退到 v0.7.5.3 时可以恢复旧安装包并复用同一 023 schema，但如果更新后出现数据完整性异常，应先停止实例并恢复升级前冷备份。

正式发布目录包含 `personal-diet-pantry-0.7.5.4-source.tar.gz`、`personal-diet-pantry-0.7.5.4-installable.tgz`、`release-manifest.json`、`TEST-SUMMARY-v0.7.5.4.zh-CN.md`、`SHA256SUMS` 和本地审阅用 `GitHub文档/`；正式 GitHub Release 只上传前五个文件。OpenClaw 只能安装 `installable.tgz`。
