# 食序管家 v0.6.2-dev.5（用户版本 6.1.5）更新说明

本版本在 0.6.1 数据正确性主线之上增加一个刻意精简的体重记录闭环。它不部署到任何现有 OpenClaw 实例，也不读取、迁移或修改现有运行数据；只有安装本版本并让目标实例使用原 `dataDir` 后，迁移 015 才会创建体重表。

## 新增能力

- 新增第七个公开工具 `diet_weight`，支持 `record`、`query`、`update`、`delete` 四个动作。
- “105”“105 空腹”“记录体重105”“我105kg，睡前”等明确体重表达可直接记录；未写单位时默认千克。
- 记录时间只取服务端可信系统时间，接口不接收用户提供的测量时间。
- 状态是可选自由文本，例如“空腹”“睡前”“早餐后”；不建立固定枚举，不做医学解释。
- 成功回执可显示本次体重、状态、7 日滚动均值，以及当前 7 日均值相对前 7 日均值的趋势。
- 体重记录支持查询、修改、删除，并纳入已有撤销/重做事务机制。

## 数据与计算口径

体重正式事实保存在 SQLite 的 `body_weight_logs` 表中。重量以整数克保存，公开结果按千克显示，从而避免浮点累计误差。允许范围为 5–500 kg；状态去除首尾空白后最多 80 个字符。

统计窗口以当前系统时间为边界：

- 当前窗口：`(now - 7 天, now]`
- 对比窗口：`(now - 14 天, now - 7 天]`

7 日均值和差值统一四舍五入到 0.1 kg。当前窗口无数据时不显示均值；前一窗口无数据时不显示趋势，不用“0 kg”伪造变化。

趋势输出为：

- 当前均值低于前一窗口：`⬇️ x.xkg`
- 当前均值高于前一窗口：`⬆️ x.xkg`
- 差值四舍五入为 0.0kg：`➡️ 持平`

## 明确不做

- 不显示月平均、15 日平均、BMI、目标体重或预测。
- 不让用户覆盖系统记录时间，也不解析“昨天”“饭后两小时”等时间描述。
- 不把状态固定成枚举，不根据状态判断体重好坏。
- 不把体重混入饮食营养日报或医学建议。

## 工具与迁移变化

- 公开工具：6 类增至 7 类。
- 公开动作：51 个增至 55 个。
- 新增迁移：`015_body_weight_logs.sql`。
- 新增业务模块：`python/personal_diet_pantry/body_weight.py`。
- npm/Python 共同使用有效版本 `0.6.2-dev.5`；面向用户的功能版本仍称 6.1.5。

迁移 015 创建体重记录表和时间索引，同时扩展操作预览的引用类型。迁移保留已有预览记录，已有迁移文件及校验和不作改写。

## 升级步骤

1. 停止目标实例并完整备份现有 `dataDir`。
2. 安装 `personal-diet-pantry-0.6.2-dev.5-installable.tgz`，继续使用原 `dataDir`。
3. 启动后运行 `diet_system self_check`，确认迁移 001–015、外键和完整性检查均通过。
4. 先执行一次只读体重查询，再用测试记录验证新增能力。
5. 若需回滚，必须同时恢复旧安装包和升级前完整数据备份；不要手工删除表或修改 `schema_migrations`。

## 回复示例

用户：

```text
105 空腹
```

首次记录且没有前一窗口数据时：

```text
已记录：105kg（空腹）
7日均值：105.0kg
```

存在足够对比数据时：

```text
已记录：105kg（空腹）
7日均值：105.0kg
趋势：⬇️ 0.5kg
```

所有数值必须来自工具正式结果；内部编号、操作句柄和数据库路径不得出现在普通回复中。

## 验证范围

发布门禁覆盖重量单位换算、边界、系统时间、自由状态、滚动窗口、趋势舍入、查询/修改/删除、撤销/重做、迁移升级、备份恢复、TypeScript schema、桥接端到端、实际 npm 安装包、Skill 行为契约和发布一致性。最终测试计数和归档哈希以构建生成的 `release-manifest.json` 与测试摘要为准。
