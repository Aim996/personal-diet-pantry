# 食序管家 v0.7.4.4 更新说明

产品版本：`0.7.4.4`
技术包版本：`0.8.4`

## 本版目标

恢复 v0.7.4.3 被共用写入授权层误伤的日常写入，同时保留纯查询零写入、跨域原子阻断、
精确删除和受保护六项两行回执。本版不扩展公开动作，不新增 migration。

## 已修复

- 写入授权改在 OpenClaw 同步 `before_prompt_build` 门禁中读取当前用户消息并绑定 `runId`，
  不再依赖异步 `message_received`，也不要求普通用户开启第三方插件原始会话访问权限；WebUI、
  普通频道和其他 Agent 运行路径使用同一上下文。
- “刚喝了300毫升水”“刚吃了一个苹果”“把两盒酸奶放冰箱”等字段完整的单域事实恢复
  直接写入，不再因为缺失授权上下文陷入确认循环。
- “记吧”与既有“确认、没问题、1”等纯确认表达都只能消费当前有效句柄；没有句柄仍然
  失败关闭，完整事实重述仍按事实本身重新判断。
- 同一轮两次终止性失败后只熔断后续写操作；查询和操作状态核验继续可用，且成功查询不会
  重置失败计数。
- 刚发生的饮水可以省略 `occurred_at`，由服务端可信 UTC 时钟记录；“早上”等只有时段、
  没有可落库时间点的历史事实必须追问，不能替换成当前晚上。
- 没有饮品名称的“早上喝了250ml”不再猜成餐食或饮水；一次问题同时补齐饮品和大致时间。
- 已过期库存继续永不进入计划、菜谱或推荐；但用户明确报告已经吃掉时，宿主会为本轮注入
  不可由模型声明的“已发生摄入”授权，再用精确库存选择如实记录餐食并产生 `consume` 变动，
  不再伪装成丢弃或损耗。未来计划、假设或普通创建命令不能取得该授权。

## 保持不变

- 七类公共工具与 40 个日常公开动作不变。
- 一条消息要求两个或更多业务域写入时，在没有跨域原子事务以前仍整体零写入并请用户拆分。
- 餐食和饮水成功后的热量、蛋白、脂肪、碳水、纤维、饮水六项两行回执保持不变。
- 模糊份量仍先展示重量、范围、依据和营养估算，确认前零业务写入。
- 体重删除仍需精确记录预览与一次性确认句柄。
- SQLite 继续是正式事实来源；本版没有新增 migration，继续使用 migrations 001–021，
  schema 与 v0.7.4.3 相同。

## 升级与回退

更新前停止目标实例并创建、校验升级前冷备份；安装时保留外部 `dataDir`。更新后先核对七类
工具和版本，再运行 `self_check` 与只读数量检查。若候选失败，停止实例并重新安装已校验的
v0.7.4.3 可安装包；在线 `diet_system backup` 只用于同版本恢复，不能替代升级前冷备份。

## 本地候选制品

完整候选目录只允许包含以下顶层内容，不得用源码包代替可安装包：

- `personal-diet-pantry-0.7.4.4-source.tar.gz`
- `personal-diet-pantry-0.7.4.4-installable.tgz`
- `release-manifest.json`
- `TEST-SUMMARY-v0.7.4.4.zh-CN.md`
- `SHA256SUMS`
- `GitHub文档/`

本地构建、打包和测试不会自动推送 GitHub、创建 Tag/Release、安装到软路由或重启 OpenClaw。
