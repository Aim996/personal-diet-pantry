# 食序管家 v0.7.4.9 更新说明

产品版本：`0.7.4.9`  
技术包版本：`0.8.9`

## 本版解决什么

0.7.4.8 的全部自动化与插件自检虽然通过，但真实 OpenClaw 冷会话输入“吃了个玉米”仍会零写入失败。真实工具轨迹证明，公开参数允许模型提交自然计数、有界克重估算和部分营养，内部入口却要求重复填写消费克重、把钠强制为必填，并误把 B 级食物成分表证据送入只允许 C/D 的估算通道。

0.7.4.9 只修复这一组公开参数到内部契约的衔接问题：

- `1个 + 约90克` 会确定性补全为同一消费克重/体积，再执行原有一致性校验；调用方同时提供冲突值时仍然拒绝。
- A/B 级证据作为 `nutrition_facts`，C/D 级证据作为 `nutrition_estimate`，避免因字段名选错进入无意义重试。
- 钠或含水量未知时保存为未知，不伪造为 0；已有 partial nutrition 状态继续表示缺失字段。
- 更新中若新明确消费量与旧估算冲突，只移除旧估算并交给现有原子更新路径；新建数据不会吞掉冲突。
- Skill 明确要求完整份量表达携带自然计数、计量对象、可食部、重量和估算标记，例如 `1个｜可食部（玉米粒）约90克（估算）`。

## 明确没有改变

- 没有新增 migration，也没有修改既有 migration；继续使用 migrations 001–021。
- 没有新增公开工具、action 或数据库表。
- 明确已摄入仍直接记录；真正模糊份量仍只做一次零写入澄清。
- 普通餐食仍固定显示热量、蛋白、脂肪、碳水、纤维、饮水六项双行回执。
- 纯水仍只显示饮水确认和饮水双行进度。
- 库存、时间窗、过期推荐过滤、撤销、删除安全和 WebUI 均不在本轮修改范围。

## 更新与回退

本版与 0.7.4.8 使用同一数据库 schema。更新前仍须停止实例并保留经过校验的升级前冷备份；安装时只使用 `personal-diet-pantry-0.7.4.9-installable.tgz`。若安装、自检或真实口语验收失败，停止实例并重新安装已验证的 v0.7.4.7 制品；数据库异常时再使用升级前冷备份恢复。在线 `diet_system backup` 只用于同版本恢复，不能替代升级前冷备份。不得覆盖或修改 0.7.4.8 的失败候选制品。

## 候选制品合同

正式候选目录顶层必须包含以下内容，且 `SHA256SUMS` 覆盖四个正式文件：

- `personal-diet-pantry-0.7.4.9-source.tar.gz`
- `personal-diet-pantry-0.7.4.9-installable.tgz`
- `release-manifest.json`
- `TEST-SUMMARY-v0.7.4.9.zh-CN.md`
- `SHA256SUMS`
- 本地审阅留痕目录 `GitHub文档`

## 发布状态

当前仓库内容仍是候选。只有自动化全矩阵、可复现构建、安装包审计、软路由安装、真实 OpenClaw 多场景 UAT 和测试数据清理全部通过后，才可把 0.7.4.9 标记为完成版本或上传 GitHub。仓库脚本不会自动推送 GitHub、创建 Tag/Release。
