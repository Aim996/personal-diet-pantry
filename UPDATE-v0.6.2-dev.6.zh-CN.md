# 食序管家 v0.6.1.5.1 更新说明

内部包版本：`0.6.2-dev.6`  
完整基线：v0.6.1.5 / `0.6.2-dev.5`  
数据库迁移：仍为 001–015  
公共工具：仍为 7 个  
公共动作：仍为 55 个

## 本次增加

v0.6.1.5.1 把自然口语自动触发设计纳入完整食序管家版本。用户无需输入
Skill 名称、斜杠命令或“帮我记录”，普通中文口语、省略句、做饭剩菜、
库存临期、进度报告、撤销和合理裸数字体重都能进入同一套正式 Skill 规则。

触发后继续执行 v0.6.1.5 的全部约束：

- 食物和营养饮品使用 `diet_meal`；
- 纯水使用 `diet_water`；
- 体重使用 `diet_weight`；
- 库存使用 `diet_pantry`；
- 撤销重做使用 `diet_transaction`；
- 进度与报告使用 `diet_report`；
- 初始化、自检、备份、恢复、目标和偏好使用 `diet_system`；
- SQLite 仍是唯一正式事实来源；
- 营养口径仍只缩放一次；
- 工具失败时不伪造成功。

## 裸数字体重

继承 v0.6.1.5 已完成规则：

- 单独合理正数且没有其他更强领域语义时视为体重；
- 裸数字默认 kg；
- 时间只取系统时间；
- 状态保留为可选自由文本；
- 直接记录一次，不预览、不确认。

`105 个鸡蛋`、`订单编号 105`、`预算 105`、`验证码 105`、代码和测试示例
都不能被当作体重。

## 排除边界

以下内容不会因为出现食物、饮水或数字而写入个人数据：

- 广告、文案、翻译和文章写作；
- 泛营养或食品百科；
- 图片生成；
- 软件代码、测试数据和引用示例；
- 订单号、验证码和其他标识符；
- 明确表示没有发生的事件。

## 兼容与数据

本版本不新增或修改数据库表，不改迁移 001–015，不改变工具 action 和公共参数，
也不创建第二份事实数据。原有餐食、饮水、库存、目标、偏好、事务和体重记录
继续保存在原 `dataDir/diet.sqlite`。

升级前仍需停止目标实例并完整备份旧软件包和整个 `dataDir`。升级时安装
`personal-diet-pantry-0.6.2-dev.6-installable.tgz`，保留原 `dataDir`，
启动后执行 `diet_system self_check` 和只读核对。

## 本次不包含

- 软路由部署或 OpenClaw 实例更新；
- Telegram 配对、令牌或频道配置；
- 15 日/月度体重均值、BMI、目标体重或医疗评价；
- 图片识别；
- 对用户现有数据库的读取、迁移或改写。

## 验证

发布门禁覆盖自然口语正反例、七领域路由、体重优先级、版本一致性、
干净工作区 build-before-test 顺序、Python/TypeScript 全量测试、Skill 校验、
升级、安装、发布审计、依赖审计和两次独立可复现构建。

详细设计与执行计划：

- `docs/development/0.6.1.5.1-NATURAL-LANGUAGE-TRIGGER-DESIGN.zh-CN.md`
- `docs/development/0.6.1.5.1-NATURAL-LANGUAGE-TRIGGER-IMPLEMENTATION-PLAN.zh-CN.md`

