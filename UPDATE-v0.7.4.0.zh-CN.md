# 食序管家 v0.7.4.0 更新说明

技术包版本：`0.8.0`。

## 本版目标

本版不是继续堆叠规则，而是把日常饮食账本收敛为一个可验证的核心闭环：更少的公开动作、更少的工具调用、确定性回执，以及不会把未知值写成零的数据边界。

## 用户可见变化

- 记录饮食或饮水后，由插件直接生成受保护的六项两行进度回执；模型只能原样输出一次。
- “一个玉米”等估算记录必须同时显示份量依据，例如“1个（估算可食部约90g）”，不能只显示热量。
- “一点、一些、几口、几粒”等模糊份量先给出建议值、范围和营养估算，确认前零写入。
- 餐食纠错统一为一次预览、一次确认、一次提交；库存回执显示整笔纠错的净变动。
- “昨晚到今天凌晨”“昨儿夜里到天亮前”等自然时间统一交给 `Asia/Shanghai` 时间窗解析器；查询包含该时段内的正餐、加餐、营养饮料和补剂。
- 命中库存包装但缺少营养标签时，营养保持未知；可以补标签或明确确认按未知记录，不再生成静默的零营养餐。
- 过期库存不会进入食用和推荐候选。过期批次仍可被查询并丢弃，但会明确标记为 `expired_only`，餐食入口会拒绝使用。

## 运行与性能边界

- OpenClaw 公开面保持 7 个工具，并收敛为 40 个日常公开动作；兼容动作仍保留在内部。
- Skill 运行时只读主 `SKILL.md`，reference、源码、日志、Memory Search、Exec、Shell 和文件遍历均不参与普通业务路由。
- 简单读写最多一次业务调用；查找后操作最多两次；预览与提交分属两个用户回合，每回合一次。
- 失败是终态：相同失败不会立即重复；写入超时只允许一次只读状态核验，绝不重放写入。

## 兼容性与回退

本版没有新增 migration，继续使用 migration 001–021，数据库 schema 与 v0.7.3.6 相同。升级前冷备份必须在实例停止时用 SQLite backup API 制作并校验；在线 `diet_system backup` 只用于同版本恢复，不能替代升级前冷备份。需要回退时，可停止实例后安装 v0.7.3.6 可安装包并复用同一 `dataDir`。

本版不会自动部署、安装、停止或重启 OpenClaw。

## 发布制品

正式发布目录顶层只包含以下六项：

- `personal-diet-pantry-0.7.4.0-source.tar.gz`
- `personal-diet-pantry-0.7.4.0-installable.tgz`
- `release-manifest.json`
- `TEST-SUMMARY-v0.7.4.0.zh-CN.md`
- `SHA256SUMS`
- `GitHub文档`
