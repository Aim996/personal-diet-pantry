# 食序管家 v0.7.4.5 更新说明

产品版本：`0.7.4.5`
技术包版本：`0.8.5`

## 本版目标

修复 v0.7.4.4 在真实 OpenClaw WebUI 中出现的“明确写入被拒绝”和“预览确认断链”，让
日常饮水、清晰餐食以及模糊份量的预览确认恢复可用。本版只修写入闭环，不扩展业务范围、
不新增 migration，也不重写已经通过的查询、库存、时区、推荐和六项进度回执。

## 已修复

- 当前轮意图仍由同步 `before_prompt_build` 中宿主提供的用户消息和 `runId` 建立；当 OpenClaw 拒绝把插件
  `runContext` 持久化时，插件使用同一实例内、按 `runId` 隔离、30 分钟过期且有数量上限
  的短期副本继续完成该轮授权。它不会跨 `runId`、跨会话或跨重启赋予写权限。
- “刚喝了137毫升水，帮我记一下”等字段完整的单域事实可以在当前轮直接写入；没有可信
  `runId`、纯查询、跨域复合写入和未授权动作仍然失败关闭。
- 模糊餐食继续先做零业务写入预览。纯“确认、记上吧、就按这个记吧”只提交可见会话中
  未过期的原凭证一次，不会改成新的直接记录，也不会再索要第二次同义确认。
- “刚刚吃的、其实是半根、是原味那盒”等补充事实不会被误当确认。它会与当前可见事实
  合并，生成一份新的完整零写入预览；旧凭证被替代，用户只需对新预览确认一次。
- 当前轮的两次终止性失败计数在宿主 `runContext` 不可用时仍然有效；查询不会重置计数，
  不同 `runId` 不能复用任何写入授权。

## 保持不变

- 七类公共工具与 40 个日常公开动作不变。
- 一条消息要求两个或更多业务域写入时，在没有跨域原子事务以前仍整体零写入并请用户拆分。
- 餐食和饮水成功后的热量、蛋白、脂肪、碳水、纤维、饮水六项两行回执保持不变。
- 模糊份量仍展示具体重量、范围、依据和营养估算，确认前零业务写入。
- 查询不写入、过期库存不进入推荐、营养未知不写成零、体重精确删除和事务幂等均保持不变。
- SQLite 继续是正式事实来源；本版没有新增 migration，继续使用 migrations 001–021，
  schema 与 v0.7.4.4 相同。

## 升级与回退

更新前停止目标实例并创建、校验升级前冷备份；安装时保留外部 `dataDir`。更新后先核对七类
工具和版本，再执行只读自检与记录数量核对。若候选失败，停止实例并重新安装已校验的
v0.7.4.4 可安装包；在线 `diet_system backup` 只用于同版本恢复，不能替代升级前冷备份。

## 本地候选制品

完整候选目录只允许包含以下顶层内容，不得用源码包代替可安装包：

- `personal-diet-pantry-0.7.4.5-source.tar.gz`
- `personal-diet-pantry-0.7.4.5-installable.tgz`
- `release-manifest.json`
- `TEST-SUMMARY-v0.7.4.5.zh-CN.md`
- `SHA256SUMS`
- `GitHub文档/`

本地构建、打包和测试不会自动推送 GitHub、创建 Tag/Release、安装到软路由或重启 OpenClaw。
