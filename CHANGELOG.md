# Changelog

## [0.7.5.3] - 2026-08-08

技术包版本：`0.9.3`

- 新增目标方案预览与一次确认提交凭证，消除裸确认死循环。
- 固体食物含水不再污染饮水，明确体积的营养饮料继续计入。
- 允许如实记录已过期库存，同时保持推荐硬过滤。
- 同一 `dataDir` 的正式写入串行执行，降低批量入库的 SQLite 写锁冲突。
- 新增 migration `023_goal_update_preview.sql`；纯水短回执、餐食六项双行和普通正向输入由智能体选路保持不变。

## [0.7.5.2] - 2026-08-08

技术包版本：`0.9.2`

- 修复普通 Linux/OpenClaw 环境只有 `python3` 时，Node bridge 因固定查找 `python` 而无法启动服务的问题。
- 保留显式解释器和 `PYTHON` 环境变量优先级；Windows 默认行为不变。
- 不新增 migration，不修改 v0.7.5.0 的 Skill 业务行为和回复契约。

## [0.7.5.0] - 2026-08-07

技术包版本：`0.9.1`

### Changed

- 删除确定性的正向短语路由层；`SKILL.md` 改为开放的能力地图、证据要求和安全边界，由智能体为普通正向输入选择能力。
- 插件保留负向安全护栏，并在智能体第一次选择非只读动作后锁定该轮领域与动作，防止跨域部分提交、破坏性误写和工具游移。
- 明确发生且数量可执行的餐食、饮水、体重和入库仍应直接记录；开放数量只预览一次；否定、计划、他人行为和查询保持零写入。
- 明确称重语境中的无单位合理数值默认按 kg 记录；孤立数字仍不写体重。
- 普通库存入库不再要求生产日期或到期日；位置按品类推断，到期日按入库日期和品类估算，显式用户值优先。
- 新增 `storage_location_source` 和 `expiry_source`，区分用户事实、系统默认和无法追溯的旧记录。
- Skill、工具描述和参数 schema 统一为同一决策，删除普通入库的日期确认循环与唯一餐食修正的二次预览。

### Preserved

- SQLite 真相源、营养基准、餐食与库存原子联动、相对时间范围、过期推荐硬过滤和查询零写入保持不变。
- 普通餐食保留六项双行进度回执；纯水保留只显示饮水的短回执。
- 已完成的 v0.7.4.28 目录和发布内容未修改。

Migration: `022_pantry_default_provenance.sql`。旧库存值原样保留，新增来源标为 `legacy_unknown`；降级必须恢复升级前冷备份，不能让 v0.7.4.28 直接复用已迁移数据库。

## [0.7.4.28] - 2026-08-07

技术包版本：`0.8.28`

### Changed

- 把公开仓库的 README、安装/更新/排障文档和 AI 操作提示词统一到正式 GitHub Release 的固定版本下载、SHA-256 校验、npm-pack 安装与零业务写入验收流程。
- Release 提供可安装包、源码快照、manifest、测试摘要和 SHA256SUMS 五项资产，明确源码包不可交给插件安装器。
- Node.js 支持范围与锁定 OpenClaw 2026.7.1-2 对齐，避免把 Node 23 或 24.0–24.14 误标为支持环境。
- 源码归档根目录改为取自已提交的 `package.json.name`，不再依赖维护者本地克隆目录名。

### Security

- 初始化全新账本必须取得用户明确授权；安装验收不写入餐食、饮水、体重、库存、目标或偏好。
- 外部 `dataDir`、升级前冷备份、失败关闭和运行时七类工具检查继续作为安装/更新边界。

Migration: none。数据库 schema 与 v0.7.4.27/v0.7.4.19 相同，继续使用 migrations 001–021；运行时业务行为没有变化。

## [0.7.4.27] - 2026-08-07

技术包版本：`0.8.27`

### Fixed

- 显式日历端点中的具体时刻优先于早上、傍晚、晚上等时段默认边界，并统一按 `Asia/Shanghai` 处理中文数字、跨日、跨月和跨年。
- 同一轮唯一、无冲突的包装换算可确定性补齐 per-100ml/per-100g 消费量，避免部分标签第一次 Meal 调用因缺字段失败；分类词不匹配或换算冲突时仍不猜测。
- 完整、只读的餐食历史清单通过同步 `before_message_write` 钩子写入 WebUI transcript，并继续通过出站钩子发送；两条通道都逐条保留相同内容的独立记录。

### Safety

- transcript 接管仅限当前会话、完整历史查询和最终 assistant 文本；用户/工具/中间消息、写入回执、不完整查询、分析、推荐与删除候选均不受影响。
- 没有新增业务域、数据库迁移或确认流程；玉米直接写入与纠正、纯水短回执、六项双行餐食回执、模糊量预览、未知营养、Meal/Pantry 原子事务和过期推荐硬过滤保持原行为。

Migration: none。数据库 schema 与 v0.7.4.26/v0.7.4.19 相同，继续使用 migrations 001–021；本版不会自动推送或创建 GitHub Release。

## [0.7.4.26] - 2026-08-07

技术包版本：`0.8.26`

### Fixed

- 完整、只读的“某个时间段吃了什么”查询改为确定性逐条呈现：最终条数与 Meal 工具返回数组严格一致，每条独立记录单独编号，时间和内容相同也不合并；完整结果返回后同一轮禁止继续探测其他饮食工具。
- `per_100g` / `per_100ml` 营养证据在份量已经是精确兼容物理单位时，由宿主补齐消费重量或体积，避免因模型漏传等价字段而产生无意义失败。
- 用户为营养未知的库存食品补充部分包装标签时，只缩放已知字段；未提供的纤维、钠、饮水等继续保持未知，不再要求用户编造 0。库存包装换算在营养基准校验前完成，使餐食写入与库存扣减可在一个原子事务内成功，撤销同时返库。

### Safety

- 确定性清单只接管 `scope.complete: true` 的只读 Meal 历史查询，不接管当前未完整时间窗、删除候选、营养分析或饮食建议。
- 部分标签至少要包含一个真实已知营养字段；全空标签在进入 Python 前结构化拒绝。普通食品库、C/D 估算、Meal/Pantry 原子事务、纯水短回执、六项双行餐食回执、过期推荐硬过滤和 UI-01 均保持原行为。

Migration: none。数据库 schema 与 v0.7.4.25/v0.7.4.19 相同，继续使用 migrations 001–021；本版不会自动推送或创建 GitHub Release。

## [0.7.4.25] - 2026-08-07

技术包版本：`0.8.25`

### Fixed

- 把同会话尚未提交的餐食预览保存为短期、可替换的活动预览；用户紧接着说“实际是100克”等精确补充时，只生成一份替换预览，不再查询历史、修改旧记录或错误提交旧预览。
- 精确克数或毫升数纠正会清除与新事实冲突的“约”“估算”“一点”“10粒｜10克”等旧份量文字和估算证据，同时保留“1个”“可食部（玉米粒）”这类仍兼容的结构信息。
- 成功写入后的固定回执增加同会话短期兜底关联；即使宿主最终回复事件遗漏 `runId`，也会展示工具已生成的六项双行回执，不再退化成“今日进度见上”。新回合和新会话不能复用旧回执。
- 自然时间窗解析器直接支持“8月3号晚上到4号天亮前”及跨月、跨年的同类表达，统一按用户时区和时间段注册表构造半开区间；完整查询结果不得再按旧记录的餐次标签或原始描述二次删减。
- “算了、不记了、别记、取消”等口语统一判为零写入，并立即使当前活动餐食预览失效；后续精确补充不能复活已取消的提案。

### Safety

- 活动预览只在同会话短期有效；纯确认仍必须绑定有效预览句柄，查询、假设、计划、否定和他人行为保持零写入。
- 不修改 Meal/Pantry 数据库事务、schema、migrations 001–021、纯水短回执、六项双行餐食回执、过期推荐硬过滤或 OpenClaw WebUI。

Migration: none。数据库 schema 与 v0.7.4.24/v0.7.4.19 相同；本版不会自动推送或创建 GitHub Release。

## [0.7.4.24] - 2026-08-07

技术包版本：`0.8.24`

### Fixed

- 把“确认入库”“确认，按上面补记”和带业务摘要的自然确认统一识别为当前可见预览的句柄提交，只允许一次 `commit_record` 或 `commit_add`，不再误走新的 `record/add` 后陷入确认循环。
- 对“补充最终数值 + 明确要求记录”的同一条消息，例如“大概10粒，就按10克记吧”，合并当前可见摄入事实后只执行一次正式餐食写入，不再生成第二份预览或要求第二次确认。
- 库存已唯一命中但营养未知时，用户补充包装标签并明确要求记录，可在一次餐食事务中复用库存凭证和标签营养，原子记餐并扣库；不再尝试额外修改 Pantry，也不把未知营养静默写成零。

### Safety

- 纯“确认一下库存还有没有”仍是只读查询；普通补充事实但没有最终写入授权时仍保持零写入或替换预览，不会被放宽成直接提交。
- 不修改 Meal/Pantry 数据库事务、schema、migrations 001–021、时间窗、推荐、纯水短回执、六项双行餐食回执或 OpenClaw WebUI。

Migration: none。数据库 schema 与 v0.7.4.23/v0.7.4.19 相同；本版不会自动推送或创建 GitHub Release。

## [0.7.4.23] - 2026-08-07

技术包版本：`0.8.23`

### Fixed

- 把“刚才记上了吗？”这类纯操作结果查询收敛到一次最近事务读取；Meal、Pantry、Report 等旁路以及同一轮第二次重复读取会被宿主入口拦截，不再出现长时间工具探测或误补写。
- 只在模型可见的工具文本中优先保留 `occurred_at_local`、`measured_at_local` 和本地时间窗，隐藏同时存在的 UTC 伴随字段与审计时间，避免把上海凌晨记录显示成前一天晚上。

### Safety

- “没记就帮我补记”等当前消息明确授权的条件补记继续走写入判断，不会被纯状态查询规则吞掉；查询本身仍保持零写入。
- 原始结构化 `details`、数据库 UTC、Meal/SQLite 事务、schema、migrations 001–021、纯水短回执和六项两行餐食回执均未修改。

Migration: none。数据库 schema 与 v0.7.4.22/v0.7.4.19 相同；本版不会自动推送或创建 GitHub Release。

## [0.7.4.22] - 2026-08-07

技术包版本：`0.8.22`

### Changed

- 把明确已发生事实与同域查询统一交给同一个回合意图：例如“刚喝了149毫升水，帮我记一下并告诉我今天一共喝了多少”现在先记录、再查询，不再因为句末查询而把整轮错误降为只读。
- 补齐“刚称了106.66公斤”这类自然称重表达的体重写入域；“补记……碳水35克”不再因为“碳水”中的“水”被误判成饮水域。
- 同会话紧接上一餐说“其实可食部是80克”可复用可信餐食句柄直接纠正；已授权的退货、丢弃等库存处置若模型漏传 `source_text`，宿主入口会补入当前用户原句，避免无意义失败与重复确认。

### Security

- 只在“明确写入命令 + 已发生事实 + 唯一业务域”同时成立时允许同域写后查询；状态查询、假设表达、跨域复合写入和跨会话无句柄操作继续失败关闭。
- 不修改 Meal/SQLite 事务、数据库 schema、migrations 001–021、纯水短回执、六项两行餐食回执、过期推荐硬过滤或 OpenClaw WebUI。

Migration: none。数据库 schema 与 v0.7.4.21/v0.7.4.19 相同；本版不会自动推送或创建 GitHub Release。

## [0.7.4.21] - 2026-08-07

技术包版本：`0.8.21`

### Fixed

- “刚才那个/这条/那条”这类必须依赖当前会话才能定位的餐食删除或纠正，现在只能使用同会话最近一次成功操作的可信句柄；宽泛查询返回的候选句柄不能在同一轮直接升级为修改或删除权限。
- 同会话已有可信餐食句柄时继续一次直接删除/纠正；即使模型多做一次只读查询，插件也会覆盖为原可信句柄，不采用查询候选或模型猜测句柄。
- 带明确钟点和食物身份的历史删除不再误绑定同会话最近餐食，仍可通过窄查询取得精确句柄后执行。

### Safety

- 新会话没有可信目标时，最多只允许查询并展示最少候选，必须等待用户明确选择；不猜最新一条，不自动写入。
- Meal 事务、数据库 schema、migrations 001–021、营养、库存、时间窗、推荐、体重、WebUI 和所有回执模板均未修改。
- 0.7.4.20 及更早正式目录、制品和校验值保持不变；已确认不安全的 0.7.4.20 不作为回退版本，技术回退仍使用 0.7.4.19。

Migration: none。数据库 schema 与 v0.7.4.20/v0.7.4.19 相同；本版不自动推送或创建 GitHub Release。

## [0.7.4.20] - 2026-08-07

技术包版本：`0.8.20`

### Fixed

- 同会话明确说“把刚才那个玉米删了”时，主机写入门现在把这类陈述式口语识别为句柄绑定的定向删除，不再误判为未授权并要求二次确认。
- 删除状态问题“刚才那个玉米删了吗？”和假设问题“如果把刚才那个玉米删了会怎样？”稳定保持只读，不因补齐“删了”而放宽写权限。
- 即使模型在删除前多做了一次只读查询，当前消息已经获得的删除授权和同会话餐食句柄仍保持有效；Skill 预算仍要求有句柄时直接删除一次。

### Safety

- Meal 删除事务、数据库 schema、migrations 001–021、营养、库存、时间窗、推荐、体重、WebUI 和回执模板均未修改。
- 删除仍必须绑定不透明目标句柄；跨会话、无句柄、查询和假设表达继续失败关闭。
- 0.7.4.19 及更早正式目录、制品和校验值保持不变；0.7.4.19 因真实 UAT 阻断只作为代码起点和技术回退点。

Migration: none。数据库 schema 与 v0.7.4.19 相同；本版不自动推送或创建 GitHub Release。

## [0.7.4.19] - 2026-08-06

技术包版本：`0.8.19`

### Fixed

- 库存中已登记“2 盒×250ml”且标签为每盒营养的饮料，现在能把 `1盒` 精确换算为 `250ml` 和 `1份`，一次原子事务完成记餐、营养计算与扣库，撤销后返库。
- “2盒，每盒250毫升”这类明确中文包装事实在工具边界统一为 `500ml + 2盒`；纯确认提交只消费不可变预览凭证，不再夹带新数量导致确认死循环。
- 模型已给出玉米 `1个｜可食部（玉米粒）约90克（估算）` 却遗漏营养字段时，使用本地可追溯每 100 克可食部数据完成计算，不再以“营养待补充”提交。
- 普通餐食安全删除后返回与新增、纠正同源的六项双行进度回执，增量显示为删除的负变化。

### Safety

- 包装换算只采用已登记的统一包装关系或用户原话中唯一、明确的“每盒/瓶/包 X 克/毫升”；不根据常见规格猜测。
- 不修改数据库 schema、migrations 001–021、时间窗、推荐过滤、纯水短回执或 OpenClaw WebUI。
- 0.7.4.18 及更早的目录、提交、制品和校验值保持不变；0.7.4.18 因真实 UAT 阻断只作为技术回退点。

Migration: none。数据库 schema 与 v0.7.4.18 相同；本版不自动推送或创建 GitHub Release。

## [0.7.4.18] - 2026-08-06

技术包版本：`0.8.18`

### Fixed

- 明确“删除当前整条餐食”与历史事务撤销的边界：同会话可信餐食凭证直接执行一次 meal delete；跨会话或目标不唯一时只查询餐食候选，不再遍历旧 operation。
- 库存食品摄入的唯一搜索使用营养摘要凭证，并保留用户的自然包装数量与单位；不再额外查询库存、手工换算基础量或手填营养字段。
- 完成库存摄入的搜索成功后阻止同轮第二次库存读取，餐食事务继续独占营养快照、扣库和撤销返库。

### Safety

- 不修改数据库 schema、migrations 001–021、时间窗、推荐过滤、普通餐食回执、纯水短回执或 WebUI。
- 0.7.4.17 及更早提交、候选目录与制品保持不变；0.7.4.17 因真实 UAT 阻断只作为技术回退点。

Migration: none。数据库 schema 与 v0.7.4.17 相同；本版不自动推送或创建 GitHub Release。

## [0.7.4.17] - 2026-08-06

技术包版本：`0.8.17`

### Fixed

- 明确摄入缺少餐次或地点时，由执行边界使用 `other` / `unknown` 默认分析标签，不再向用户追问非关键字段。
- 当前会话存在可信最近餐食凭证时，紧接纠正直接走一次 `diet_meal update`，不先查询、搜索或确认。
- 库存食品摄入统一走库存搜索凭证加餐食事务，由餐食提交原子扣库和撤销返库，不再拆成独立 Pantry 扣减。
- 撤销/重做优先按事务路由，库存处置支持“丢弃”；只有明确后续动作才判定为多域操作。
- 未知营养字段保持未知，饮水营养只使用合法的 `hydration_ml` 字段。

### Safety

- 不修改数据库 schema、migrations 001–021、时间解析、回执渲染、纯水短回执、过期推荐硬过滤或 OpenClaw WebUI。
- 不放宽跨会话纠正、删除、只读、模糊份量和库存多候选边界；0.7.4.16 及更早版本保持不可变。

Migration: none。数据库 schema 与 v0.7.4.16 相同；本版不自动推送或创建 GitHub Release。

## [0.7.4.16] - 2026-08-06

技术包版本：`0.8.16`

### Fixed

- 同一 OpenClaw 会话中，紧接上一笔成功餐食的“其实是80克”“不对，是五粒”等上下文纠正，会复用该笔回执返回的真实 `meal_handle`，不再猜时间、时区或句柄。
- 纠正目标只在当前会话、短期有效且可信意图明确时绑定；不同会话、明确历史目标和删除操作继续要求独立定位。
- 无时区 selector 可以到达插件守卫以便被真实句柄替换；没有可信替换时，执行入口仍按原规则拒绝非带时区 ISO 时间并保持零写入。

### Safety

- 不放宽删除、只读或跨会话权限；错误目标仍被守卫拒绝，两次失败熔断继续保留。
- 不修改餐食营养、库存联动、回执格式、纯水、数据库 schema、migration 或公开 action；保留 0.7.4.15 的明确份量、包装规范化和正式回执边界。

Migration: none。数据库 schema 与 v0.7.4.15 相同；本版不自动推送或创建 GitHub Release。

## [0.7.4.15] - 2026-08-06

技术包版本：`0.8.15`

### Fixed

- 把与明确自然计数完全相同的零宽份量区间视为冗余字段，避免“吃了个玉米”被错误降级成确认预览；真正有上下界差异的模糊份量仍保持零写入澄清。
- 把“2盒、每盒200克”这类自然包装事实一次规范化为`400克`基础库存和`2盒`展示库存，预览生成的提交凭证可以直接完成入库，不再让Agent反复猜单位。
- 对同一轮、单一业务域的成功写入，最终发送内容直接采用工具生成的`rendered_receipt`，防止Agent把`1个｜可食部（玉米粒）80克`、六项双行进度或纯水双行回执缩写丢失。

### Safety

- 回执替换严格限定于同一`runId`、`write_committed`和存在正式回执的纯写入；读写混合问题、失败结果、跨轮消息和后续追加工具调用不替换。
- 不新增action、migration或数据库字段；保留0.7.4.14的纠错对象继承、库存联动、模糊份量、纯水、时间窗、推荐和只读边界。

Migration: none。数据库schema与v0.7.4.14相同；本版不自动推送或创建GitHub Release。

## [0.7.4.14] - 2026-08-06

技术包版本：`0.8.14`

### Fixed

- 修复紧接精确重量纠错只保留新克重、丢失上一条正式自然计数和计量对象的问题；`1个｜可食部（玉米粒）约90克（估算）`更新为80克后保持`1个｜可食部（玉米粒）80克`。
- 份量上下文仅从同一餐食内唯一同名的已提交项目继承，不再要求模型在纠错载荷中重复完整旧回执。
- 新增真实OpenClaw载荷形状回归：模型只传`portion_expression: 80克`且不重复旧营养不确定性时，服务端仍生成完整、无估算的正式回执并按80克重算营养。

### Safety

- 显式新计量对象（可食部、毛重、总重、净重、带芯/皮/骨/壳等）优先并阻止继承；服务端不按食物名称推断解剖部位。
- 保留0.7.4.13的库存凭证、原子扣库与撤销返库；不新增action、migration、数据库字段、跨域事务或WebUI修改。

Migration: none。数据库schema与v0.7.4.13相同；本版不自动推送或创建GitHub Release。

## [0.7.4.13] - 2026-08-06

技术包版本：`0.8.13`

### Fixed

- 修复已校验库存商品凭证在餐食地点未单独说明时只写餐食、不扣库存的问题；有效凭证与明确消费数量现在作为家庭库存来源证据参与同一事务。
- 新增真实蛋白粉链路回归：350g 库存记录半勺 15g 后必须为 335g，撤销该餐食后必须原子恢复到 350g。
- Skill 明确要求库存关联餐食只能依赖同一次餐食提交返回的 `inventory_effects`，禁止餐后再补调 Pantry 制造非原子联动。

### Safety

- 仅对已校验、未失效且商品身份与单位一致的 `inventory_match_handle` 生效；显式外食、无凭证猜测、过期库存和失效凭证仍保持原有安全边界。
- 不新增 action、数据库字段、migration、跨域大事务或 WebUI 修改；普通餐食六项两行、纯饮水双行、玉米可食部、精确纠错和库存补充确认协议保持不变。

Migration: none。数据库 schema 与 v0.7.4.12 相同；本版不自动推送或创建 GitHub Release。

## [0.7.4.12] - 2026-08-06

技术包版本：`0.8.12`

### Fixed

- 向公开 Pantry 工具面接通已有的 `preview_add` / `commit_add` 安全协议，使跨轮补充到期日等库存事实能够先形成完整预览，再用短期句柄提交。
- “确认记上”“就记上吧”等纯确认短语只在存在有效工作流句柄时提交；没有句柄时仍保持零写入，直接 `add` 不能借确认短语绕过当前消息授权。
- Skill 明确要求最终答复逐字段采用本轮最后一次成功工具回执，禁止把更新前的营养或进度数值混入纠错后的结果。

### Safety

- 七类业务工具、数据库 schema 与 migrations 001–021 保持不变；公开日常动作从 40 个变为 42 个，仅因为公开两个已有的 Pantry 预览/提交工作流动作。
- 不新增通用 Patch 或跨域大事务；明确摄入、精确纠错、模糊份量、普通餐食六项两行、纯饮水双行、只读隔离、时间窗、库存和过期推荐边界保持不变。

Migration: none。数据库 schema 与 v0.7.4.11 相同；本版不自动推送或创建 GitHub Release。

本文件按面向用户的产品版本记录变化；npm、OpenClaw 与 Python 共用的技术包版本单独列出。历史细节保留在各版本更新说明中。

## [0.7.4.11] - 2026-08-06

技术包版本：`0.8.11`

### Fixed

- 餐食纠错载荷保留用户本次 `source_text` 作为份量精度证据和审计原因，同时继续由服务端补未变化的时间、餐次与地点。
- 明确的克重/体积纠正会移除份量表达中复用的“约/大概/估算”标记并替换旧估算值；近似原话继续保留估算语义。

### Safety

- 不新增 action、数据库字段、migration 或通用 Patch；stale/歧义/并发目标、模糊份量、纯水、库存、时间和推荐边界均保持不变。

Migration: none。数据库 schema 与 v0.7.4.10 相同；本版不自动推送或创建 GitHub Release。

## [0.7.4.10] - 2026-08-06

技术包版本：`0.8.10`

### Fixed

- 已用安全句柄或唯一 selector 选中的普通餐食允许只提交完整的新餐食项；服务端从当前记录保留未变化的发生时间、餐次、原始事实文本和地点，避免模型为一个重量纠错反复补旧字段。
- 显式提交的新餐次、文本、地点或时间继续优先，旧记录只补缺失值；stale handle、歧义目标和并发变更仍按原安全规则拒绝。
- Skill 明确“完整替换 items、餐次级旧字段可省略”，使“其实是80克”走一次更新，而不是多次试错或删除重建。

### Safety

- 不新增 action、数据库字段或 migration，不引入通用 Patch；餐食项仍必须作为完整替换集合，避免多项餐食的局部目标歧义。
- 纯水双行、餐食六项双行、模糊份量零写入、只读隔离、库存、时间窗、推荐和历史版本均保持不变。

Migration: none。数据库 schema 与 v0.7.4.9 相同；本版不自动推送或创建 GitHub Release。

## [0.7.4.9] - 2026-08-06

技术包版本：`0.8.9`

### Fixed

- 修复“自然计数 + 有界克重/体积估算”在公开工具中合法、进入内部工具前却因缺少重复消费量而被拒绝的问题。
- A/B 级食物表或标签证据会归入正式营养事实，C/D 级估算保持估算；钠或含水量未知时保存为未知，不再要求模型编造 0。
- 餐食更新同时携带新明确消费量和旧份量估算时，更新入口移除失效估算，让现有原子纠正按新事实重算；新建中的真正冲突仍会拒绝。
- 明确模型必须在完整 `portion_expression` 中携带自然计数、可食对象、克重和估算标记，避免回执只剩含义不完整的计数。

### Safety

- 不新增 migration、公开工具或 action，不修改库存、时间窗、推荐、纯水回执与普通餐食六项双行结构。
- 0.7.4.8 的提交、制品和校验值保持不变；本版只有全矩阵与真实 OpenClaw UAT 通过后才可发布。

Migration: none。数据库 schema 与 v0.7.4.8 相同；本版不自动推送或创建 GitHub Release。

## [0.7.4.8] - 2026-08-06

技术包版本：`0.8.8`

### Fixed

- 让已存在的可食部名称、重量和估算依据稳定进入餐食草稿、持久化记录与最终回执。
- 用户明确纠正克重后保留自然数量和可食部对象，同时移除已经失效的估算标记。
- 纯饮水回执在渲染边界只接收饮水指标，防止误带餐食六项进度。

### Safety

- 不根据食物名称自行推断可食部，不改变账本 schema、工具协议、库存、时间窗、推荐或宿主 WebUI。
- 明确摄入直接记录、真正模糊份量先确认、餐食六项两行、纯饮水两行和旧版本不可变规则保持不变。

Migration: none。数据库 schema 与 v0.7.4.7 相同；本版不自动推送或创建 GitHub Release。

## [0.7.4.7] - 2026-08-06

技术包版本：`0.8.7`

### Changed

- 明确餐食继续直接记录；带芯、皮、壳、骨或核的食物会区分整物总重与可食部，营养只按可食部计算。
- “吃了个玉米”保留用户的 `1个` 表达，回执明确显示 `可食部（玉米粒）约90克（估算）`，不再省略重量口径。

### Fixed

- 修复“其实是80克”已被识别为上下文纠正、却没有获得定向更新动作授权的问题。
- 修复唯一餐食凭证删除仍因缺少重复 `source_text` 而失败的问题；目标绑定和查询零写入边界不变。
- 修复完整份量表达已写入营养证据、但刷新后的回执没有读取它，导致估算标记和可食部说明丢失的问题。

### Safety

- 模糊开放份量零写入预览、纯饮水短回执、餐食六项两行模板、两次失败熔断、过期推荐硬过滤和跨轮授权隔离均保持不变。

Migration: none。数据库 schema 与 v0.7.4.6 相同；本版不自动推送或创建 GitHub Release。

## [0.7.4.6] - 2026-08-06

技术包版本：`0.8.6`

### Changed

- “吃了个玉米”“吃了根火腿肠”等明确、已发生的常见计数餐食，会保留自然单位并采用显式标注的标准重量估算，在同一轮直接记录。
- 成功餐食回执同时显示自然单位与克重；刚写入的餐食返回不暴露给用户的纠错凭证，明确的唯一目标修正可单轮原子完成。
- 份量估算证据复用现有营养证据字段持久保存；未新增数据库迁移，不改变旧事务的撤销/重做兼容性。

### Fixed

- 修复 `1根 + 估算50g` 被误判为互斥数量单位的问题。
- 修复模型把相同估算同时放入 `nutrition_facts` 与 `nutrition_estimate` 时反复报错；仅在两者结构和值完全一致时保守归一为估算，冲突值仍拒绝。
- 修复餐食更新字段被平铺时缺少 `draft`、所有更新被强制二次预览，以及成功写入后必须额外查询才能修正的问题。

### Security

- 查询零写入、跨轮授权隔离、两次失败熔断、过期食品推荐硬过滤、模糊开放份量零写入预览和纯饮水短回执均保持不变。

Migration: none。数据库 schema 与 v0.7.4.5 相同；本版不自动推送或创建 GitHub Release。

## [0.7.4.5] - 2026-08-05

技术包版本：`0.8.5`

### Fixed

- 明确单域写入不再依赖可能被宿主拒绝的 `runContext` 持久化；同轮授权按可信 `runId` 隔离并有界过期。
- 模糊餐食的补充事实会替换零写入预览，纯确认只消费有效凭证一次，不再出现同义确认死循环。
- 宿主上下文不可用时，两次失败熔断、不同轮次隔离和查询零写入仍然有效。

### Safety

- 七类工具、40 个日常动作、跨域原子阻断、过期过滤、营养未知和六项两行回执均保持不变。

Migration: none。数据库 schema 与 v0.7.4.4 相同；本版不自动推送或创建 GitHub Release。

## [0.7.4.4] - 2026-08-05

技术包版本：`0.8.4`

### Fixed

- 当前轮次授权改由默认可用的同步提示构建门禁建立，恢复 WebUI 和普通频道的明确单域写入。
- 纯确认话术与有效句柄重新闭环；两次写失败后仍可完成只读查询和状态核验。
- 刚发生的饮水使用服务端可信时钟；缺少饮品或只有粗略历史时段时不再猜测。
- 已发生的过期食品摄入需宿主本轮授权后才会如实记录并按 `consume` 扣减，未来计划仍禁止。

### Safety

- 纯查询零写入、跨域原子阻断、精确删除、模糊份量确认和六项两行回执均保持不变。

Migration: none。数据库 schema 与 v0.7.4.3 相同；本版不自动安装、推送、发布或部署。

## [0.7.4.3] - 2026-08-05

技术包版本：`0.8.3`

### Fixed

- 纯查询和状态核对不再能够触发餐食、饮水、体重、库存或资料写入。
- 多域复合句在第一次业务写入前整体阻断，避免部分提交。
- 常见中文 Meal 餐次和地点会在下沉前转换为固定枚举，非法值返回字段级错误。
- 体重删除必须先展示精确体重与本地测量时间，再用一次性确认句柄提交。

### Safety

- 写入权限绑定宿主当前消息与 `runId`；上一轮已停止或失败的任务不能在后续复活。
- 同一轮连续两次终止性 Diet 失败后熔断，同时保留普通单域写入、模糊份量预览和带句柄纠错。

Migration: none。数据库 schema 与 v0.7.4.2 相同；本版不自动安装、推送、发布或部署。

## [0.7.4.2] - 2026-08-05

技术包版本：`0.8.2`

### Changed

- 新增根目录 `GITHUB-WORKFLOW.zh-CN.md`，作为维护者和执行型 AI 的 GitHub 更新、验证、发布与交接入口。
- 明确修改价值门槛、分阶段授权、双版本治理、失败停止条件和标准交接记录。
- README 增加维护者导航，普通 OpenClaw 用户优先结构和所有受保护业务行为保持不变。

### Security

- 上传、合并、Tag、Release、仓库可见性和生产部署不再被解释为同一项授权。
- GitHub 工作流继续排除真实数据库、备份、凭据、生产路径和个人饮食数据。

Migration: none。数据库 schema 继续使用 migrations 001–021；本版不自动推送、合并、发布或部署，也不会修改用户数据。

## [0.7.4.1] - 2026-08-05

技术包版本：`0.8.1`

### Changed

- 将仓库首页重构为普通 OpenClaw 用户优先的安装、验收和文档入口。
- 补齐许可证、安全报告、贡献、安装、升级、AI 操作提示词及维护者发布说明。
- 为 GitHub CI、候选制品和人工批准后的 Release 流程建立可验证边界。

### Security

- 仓库、测试、制品和问题模板继续排除真实饮食数据、数据库、备份、凭据、地址和主机信息。
- Release 发布路径按标签、分支、版本和既有 Release 状态 fail closed。

Migration: none。数据库 schema 继续使用 migrations 001–021；本版不自动部署，也不会修改用户数据。

## [0.7.4.0] - 2026-08-04

技术包版本：`0.8.0`。简化核心账本运行面，确定性生成受保护的六项两行回执，并收敛时间、份量、库存营养、纠错和过期食品边界。详见 [v0.7.4.0 更新说明](UPDATE-v0.7.4.0.zh-CN.md)。

## 历史版本

- [v0.7.3.6](UPDATE-v0.7.3.6.zh-CN.md)：通用时间范围、估算确认、报告完整性与调用预算。
- [v0.7.3.5](UPDATE-v0.7.3.5.zh-CN.md)：约束必读与受保护行为登记。
- [v0.7.3.4](UPDATE-v0.7.3.4.zh-CN.md)：强制双版本迭代和旧制品不可覆盖。
- [v0.7.3.3](UPDATE-v0.7.3.3.zh-CN.md)：恢复六项两行进度回执。
- 更早版本仍保留各自的 `UPDATE-*.zh-CN.md`，不会因首页精简而删除。
