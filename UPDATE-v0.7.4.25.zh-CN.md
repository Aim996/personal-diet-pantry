# 食序管家 v0.7.4.25 更新说明

产品版本：`0.7.4.25`

技术包版本：`0.8.25`

## 为什么开发这一版

v0.7.4.24 已修复确认循环，并在自动化与大部分软路由真实测试中通过；但同一轮广泛 UAT 还暴露出四个会直接破坏真实使用体验的问题：模糊餐食最终写入后可能只回复“今日进度见上”，精确纠正后仍残留旧粒数或“估算”文字，尚未提交的历史补记预览无法被下一句精确份量替换，以及显式日期口语时间窗需要错误重试并可能漏掉工具已经返回的记录。

这四项不是四套新工作流，而是两个状态边界没有闭合：宿主没有稳定关联“当前活动预览/当前成功回执”，时间解析器也没有把显式日期口语统一收进既有范围构造器。本版只补齐这两个边界，不扩展新的饮食业务，不修改底层账本事务。

## 本版修复

- 同会话成功生成但尚未提交的餐食预览，作为短期活动预览保存。用户紧接着说“实际是100克”等精确补充时，只能用当前可见事件生成一份替换预览；不得查询历史、修改已有餐食、提交旧预览或重复调用工具。
- 用户给出精确克数或毫升数后，展示份量与结构化估算证据必须同时收敛：去掉冲突的“一点”“一些”“几口”“10粒｜10克”“约”“估算”等旧证据；仍成立的“1个”“可食部（玉米粒）”可以保留。
- Meal 已成功写入并生成固定回执时，即使 OpenClaw 的最终回复事件没有携带 `runId`，仍可在同会话、短有效期内显示刚才那次写入的完整回执。进入新回合即清除未消费兜底，新会话不能继承，避免串回复。
- `natural_window` 直接解析“8月3号晚上到4号天亮前”、跨月和跨年同类表达，统一复用用户时区与时间段注册表生成半开区间；自然说法和等价显式本地范围必须返回同一记录集合。
- 工具已经完整返回范围内记录后，以返回结果和时间边界为准；不得再根据旧记录里的“早餐/晚餐”标签或原始描述自行漏掉记录，也不得为同一范围继续试探其他查询形式。
- “算了、不记了、别记、取消”等口语统一作为取消处理，保持零业务写入并立即清除同会话活动餐食预览；后续“实际是……”不能重新绑定已取消提案。

## 明确保持不变

- 用户明确说已经吃了玉米、火腿肠等，继续直接记录；不新增普遍确认步骤。玉米默认显示“1个｜可食部（玉米粒）约90克（估算）”，玉米芯不计营养；用户给出80克后直接纠正为精确可食部重量。
- 真正缺少可用份量时，只做一次有用估算并询问一个最小问题；得到最终值并明确要求记录后只写一次。
- 普通餐食继续使用六项双行进度回执；纯水继续只显示饮水确认与饮水双行，不附带热量等无关项目。
- Meal/Pantry 原子扣库与撤销返库、过期推荐硬过滤、计划/否定/他人行为隔离、查询零写入、复合跨域写入失败关闭均保持不变。
- 不处理 OpenClaw WebUI“当前会话无法新建会话”的独立 UI-01 问题。
- v0.7.4.24 及更早的正式目录、包、哈希与测试记录保持不可变。

## 数据、安装与回退

本版没有新增 migration，继续使用 migrations 001–021，schema 与 v0.7.4.24/v0.7.4.19 相同。更新前必须停止实例，并在外部 `dataDir` 之外创建和校验升级前冷备份；只安装已校验的 `personal-diet-pantry-0.7.4.25-installable.tgz`。安装、自检或真实 UAT 失败时保持数据不变，重新安装已校验的 v0.7.4.19 包恢复技术运行；在线 `diet_system backup` 不能代替升级前冷备份。

## 阻断性口语化验收

1. “刚才吃了点花生，帮我记一下”后回复“大概10粒，就按10克记吧”：只能正式写入一次，最终回复必须是完整六项双行回执。
2. 紧接着说“其实是5克，帮我改一下吧”：只能修改刚才那条；回执必须显示精确 `5克`，不得残留 `10粒`、`10克`、“约”或“估算”。
3. “昨天8月6日下午3点吃了点米饭，帮我补记一下”产生预览后回复“实际是100克”：只能产生一份精确100克的替换预览，不能查历史或写库；再确认后只提交一次并显示完整回执。
4. “8月3号晚上到4号天亮前吃了什么”：一次 Meal 范围查询必须覆盖所有处于该时间窗的餐食、加餐、饮料和补剂；与等价 `local_range` 返回的记录句柄集合完全一致。
5. 回归玉米新增/80克纠正/删除、137ml 纯水/撤销、Pantry 预览/确认/撤销、库存营养标签联动、过期推荐、计划/否定/他人行为、状态查询、今日查询和跨域复合句零写入。任一保护项失败均阻断验收。
6. 餐食预览后说“算了，不记了”，再说“实际是100克”：取消轮次和后续轮次都必须零写入，且后续不得进入替换预览路径。

## 候选制品契约

本地候选目录顶层必须恰好包含：

- `personal-diet-pantry-0.7.4.25-source.tar.gz`
- `personal-diet-pantry-0.7.4.25-installable.tgz`
- `release-manifest.json`
- `TEST-SUMMARY-v0.7.4.25.zh-CN.md`
- `SHA256SUMS`
- 本地审阅留痕目录 `GitHub文档`

当前仍是开发候选。只有总约束保护矩阵、全量自动化、可重复构建、安装包审计、停机冷备份、软路由安装、真实口语化 UAT、测试数据清理和数据库核验全部通过，才能标记为可用；本项目不会自动推送 GitHub、创建 Tag 或 Release。
