# 食序管家 v0.7.5.3 更新说明

技术包版本：`0.9.3`。基线为 `v0.7.5.2 / 0.9.2`，旧版本目录和制品未修改。

## 本次修复

- 目标方案现在使用真实的 `preview_update_goals → commit_update_goals` 凭证链。展示方案不改账本，用户回复一次“确认”即可提交；重复提交同一凭证不会重复修改。
- “吃了个玉米”这类刚刚完成的摄入可以直接使用可信当前时间，不再为了缺少钟点追问；仍由智能体依据公开能力选择安全动作，Skill 没有新增固定正向口语路线。
- 固体食物的化学含水量不再增加饮水进度；豆浆、牛奶等具有明确饮用体积的营养饮料仍保留饮水贡献。
- 用户明确提供的过去到期日可以作为真实库存事实保存。已过期库存仍会被推荐硬过滤，并与临期可食库存分开。
- 同一个 `dataDir` 的正式写入进入进程内串行队列，避免一句话多项入库时多个 Python/SQLite 写事务争抢写锁；只读调用不进入该队列。
- 新增 migration `023_goal_update_preview.sql`，只扩充目标确认凭证类型，不改餐食、饮水、体重和库存业务数据。

## 明确保留

- 纯水仍直接记录，并只显示饮水确认和饮水双行进度。
- 普通餐食仍固定显示热量、蛋白、脂肪、碳水、纤维、饮水六项，每项两行。
- 玉米等食物保留用户自然单位，营养只计算可食部；明确摄入不因常见份量估算进入确认循环。
- 模糊份量、查询零写入、否定/计划/他人行为零写入、过期推荐硬过滤、相对时间窗和安全删除边界保持不变。

## 更新与回退

更新前停止 OpenClaw 并校验外部 `dataDir` 的冷备份。在线 `diet_system backup` 只用于同版本恢复，不能替代升级前冷备份。首次启动 v0.7.5.3 会应用 migration 023。若需回退到 v0.7.5.2，必须停止实例、恢复升级前冷备份，再安装旧包；不能让旧代码直接复用已应用 migration 023 的数据库。

## 发布制品

正式候选目录包含 `personal-diet-pantry-0.7.5.3-source.tar.gz`、`personal-diet-pantry-0.7.5.3-installable.tgz`、`release-manifest.json`、`TEST-SUMMARY-v0.7.5.3.zh-CN.md`、`SHA256SUMS` 和本地审阅用 `GitHub文档/`。GitHub Release 只上传前五个文件，OpenClaw 只能安装 `installable.tgz`。
