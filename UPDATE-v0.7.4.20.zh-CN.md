# 食序管家 v0.7.4.20 更新说明

产品版本：`0.7.4.20`
技术包版本：`0.8.20`

## 为什么开发这一版

v0.7.4.19 已经闭合自然包装、每份营养、玉米可食部和删除后正式回执，但真实 OpenClaw UAT 发现一处核心阻断：用户在刚记完玉米后自然说“把刚才那个玉米删了”，系统能查到正确餐食，却被主机授权层判为“当前消息没有明确授权”，必须再说一次“确认删除”才能完成。

根因不是 Meal 事务或数据库，而是授权分类器的重复删除词表不一致：目标模式识别“删了”，明确命令与定向写入模式却没有识别。v0.7.4.20 只修这一个已经证实的边界。

## 本版修复

- 带“刚才、刚刚、这条、那条、整条”等最近上下文的陈述式“删了”，现在复用既有 `targeted + opaque handle` 安全删除路径。
- 同会话有最近成功餐食句柄时可直接删除；如果模型先做了一次只读查询，当前消息的授权不会因此丢失。
- “删了吗、删除没有”等状态问题稳定分类为只读。
- “如果/假如/要是删了会怎样”等假设问题不获得写权限。

## 明确保持不变

- 删除仍必须绑定不透明餐食句柄；跨会话或无句柄时不得猜测并删除。
- 明确摄入直接记录、玉米自然单位与可食部、紧接纠正、模糊份量澄清、普通餐食六项双行全部保持。
- 纯水仍只显示饮水确认和饮水双行，不展开五项营养。
- 包装换算、每份营养、扣库/返库、时间查询、过期推荐、否定/计划/他人隔离均不修改。
- 不修改 OpenClaw WebUI“新会话”按钮问题；本版没有新增 migration，继续使用 migrations 001–021。
- 玉米行为不变：“吃了个玉米”仍直接记录，并诚实展示 `1个｜可食部（玉米粒）约90克（估算）`。
- v0.7.4.19 及更早正式目录、包和校验值保持不可变。

## 更新与回退

本版与 v0.7.4.19 使用相同 schema。更新前停止实例，在外部 `dataDir` 之外创建并校验升级前冷备份；在线 `diet_system backup` 只能用于同版本恢复，不能替代这份冷备份。只安装已校验的 `personal-diet-pantry-0.7.4.20-installable.tgz`。若插件、自检或真实删除 UAT 失败，保持数据不变并重新安装已校验的 v0.7.4.19 包恢复技术运行；v0.7.4.19 的产品 UAT 仍是失败，技术回退不等于验收通过。

## 重点口语化验收

1. 先说“吃了个UAT20玉米”，成功后说“把刚才那个玉米删了”：第二句只调用一次 Meal delete，直接返回六项双行负增量，不再确认或先查询。
2. 冷会话问“刚才那个玉米删了吗？”：只读，不触发 delete。
3. 冷会话问“如果把刚才那个玉米删了会怎样？”：只解释，不触发 delete。
4. 另一个会话说“把刚才那个玉米删了”：不得复用原会话句柄或删除真实记录。
5. 复核普通玉米新增/80克纠正、纯水137ml、库存燕麦奶、模糊花生、时间窗、推荐过滤和否定/计划/他人零写入。

## 候选制品契约

本地候选目录顶层必须恰好包含：

- `personal-diet-pantry-0.7.4.20-source.tar.gz`
- `personal-diet-pantry-0.7.4.20-installable.tgz`
- `release-manifest.json`
- `TEST-SUMMARY-v0.7.4.20.zh-CN.md`
- `SHA256SUMS`
- 本地审阅留痕目录 `GitHub文档`

当前仍是开发候选。只有全量自动化、可重复构建、安装包审计、停机冷备份、软路由安装、真实口语化 UAT、测试数据清理和数据库核验全部通过，才能标记为可用；本项目不会自动推送 GitHub、创建 Tag 或 Release。
