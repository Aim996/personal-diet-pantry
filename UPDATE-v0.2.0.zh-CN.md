# 食序管家 v0.2.0 更新说明

## 这次更新是什么

`v0.2.0` 是 Personal Diet Pantry（食序管家）Skill 的功能升级版本。
它只更新 Skill 包及其自带工具，不更新 OpenClaw 系统，也不会自动部署到
Docker。当前 `v0.1.0` 源码与正式 SQLite 数据不会在制作发布包时被修改。

## 新增功能

### 库存商品可以保存实际营养表

包装营养表会写入结构化营养档案，并与具体库存批次建立关联。库存批次还会
保存当时的营养快照，因此产品以后更换配方，也不会改变旧库存和历史饮食的
计算结果。

一次库存查询即可返回：

- 商品名称、批次、剩余数量和单位；
- 总重量、平均单件重量和重量基准；
- 保质期、存放位置和备注；
- 该批次关联的实际营养表。

营养表不再以普通文字塞入库存备注。标签没有提供的营养项目保持“未知”，
不会擅自填成零。

### 自动计算单件平均重量

计数商品可以同时保存数量和总重量。例如：

```text
鸡蛋：30枚
包装总重：1800克
平均单枚重量：约60克
重量基准：带壳重量
```

以后记录“吃了两个鸡蛋”时，系统可以：

1. 把两枚换算为约120克；
2. 使用该库存批次关联的实际营养表计算；
3. 在同一笔饮食提交中扣减两枚库存。

如果包装重量包含蛋壳、外包装或其他不可食部分，系统会保留重量基准。未获得
可食比例时，结果应作为估算说明，不能把毛重伪装成精确的可食重量。

### 正式的营养表和库存元数据工具

`diet_pantry` 新增以下动作：

- `preview_update_metadata`
- `commit_update_metadata`
- `preview_link_nutrition`
- `commit_link_nutrition`

库存重量和营养表都先预览、后提交。预览不写数据库；提交只执行一次正式事务。
重复提交同一个有效句柄会返回原结果，不会重复新增库存或营养档案。

## 已修复问题

- 修复营养表只能写入 `notes`、无法被后续营养计算调用的问题。
- 修复使用 `adjust` 动作尝试写营养表并反复出现 `INVALID_INPUT` 的问题。
- 修复库存查询错误字段可能被忽略、最终返回全部库存的问题。
- 修复新 Windows 工作区找不到项目 Python，导致跨进程恢复测试误报的问题。
- 固定 SQLite 迁移文件的原始字节，避免 Windows 换行转换触发错误的迁移校验。
- 工具参数在入口严格校验；稳定错误不会盲目重复调用。
- `INVALID_INPUT` 只允许修正指定字段后重试一次。
- 工具未就绪时停止写入，只返回一条可操作的安装提示。
- 禁止绕过正式工具直接修改 SQLite。

图片识别逻辑本身没有在本版本中重做。已识别出的订单或营养表只作为结构化
候选，最终仍由食序管家的正式工具校验并写入。

## 运行条件

- OpenClaw 使用支持本插件格式的版本；
- Node.js 与 npm；
- Python 3.11 或更高版本；
- SQLite；
- 一个独立、可持久化的 `dataDir`；
- 以下六个工具全部加载：
  - `diet_meal`
  - `diet_water`
  - `diet_pantry`
  - `diet_transaction`
  - `diet_report`
  - `diet_system`

安装或升级后应先调用 `diet_system.self_check`。自检未通过时不要执行库存或
饮食写入。

## 全新安装顺序

以下是部署人员应遵循的顺序，不代表本源码包会自动执行安装：

1. 准备 Node.js、Python、SQLite 和持久化目录。
2. 安装或注册 `v0.2.0` 插件包。
3. 确认插件清单中的六个工具全部被 OpenClaw 加载。
4. 配置独立的 `dataDir`，Docker 环境必须映射到持久卷。
5. 加载 `personal-diet-pantry` Skill。
6. 执行 `diet_system.initialize`。
7. 执行 `diet_system.self_check`。
8. 自检通过后再测试一次库存预览和提交。

如果六个工具没有全部出现，先修复插件加载，不要让图片识别结果继续尝试写库。

## 从 v0.1.0 升级并保留 SQLite

1. 停止对旧实例执行新的饮食或库存写入。
2. 使用 `diet_system.backup` 创建升级前备份。
3. 另外保留一份原始 `diet.sqlite` 文件副本。
4. 保留原 `v0.1.0` 安装包，不要覆盖删除。
5. 安装独立的 `v0.2.0` Skill 包。
6. 继续配置原来的持久化 `dataDir`。
7. 运行数据库迁移和 `diet_system.self_check`。
8. 查询现有库存，核对商品数量、单位、备注和流水。
9. 对旧备注中疑似营养表的内容只生成候选，不自动转换；确认后再通过正式营养
   工具写入。

数据库迁移是增量式的：

- 不删除现有库存；
- 不清空饮食记录；
- 不覆盖原备注；
- 不删除事务和撤销记录；
- 新增字段对旧数据默认为空。

## 升级后建议验证

部署人员可在测试数据或数据库副本上验证：

1. 新增30枚、总重1800克的鸡蛋，查询结果显示约60克/枚。
2. 给某批香干上传实际营养表，查询库存时一次返回营养信息。
3. 标签未提供膳食纤维时，该字段显示未知而不是零。
4. 记录吃了两枚鸡蛋，饮食显示约120克且库存减少两枚。
5. 只询问营养或计划下一顿时，不新增饮食、不扣库存。
6. 故意传入错误的库存查询字段，只出现一次明确参数错误。
7. 工具缺失时停止写入，不出现连续红色工具调用。

## 回退

如果升级后需要回退：

1. 停止 `v0.2.0` 的新写入。
2. 保留升级后的数据库副本，避免丢失排查信息。
3. 恢复升级前创建的 SQLite 备份。
4. 重新启用保留的 `v0.1.0` 包。
5. 运行旧版自检并核对库存。

不要让 `v0.1.0` 直接继续写入已经迁移且产生新营养档案的数据库。最安全的
回退方式是恢复升级前备份。

## 已知限制

- 包装总重不一定等于净可食重量，带壳鸡蛋尤其需要注意。
- OCR 未识别出的营养字段保持未知，需要用户补充或人工确认。
- 同名但不同品牌、规格或配方的商品应分别建立营养档案。
- `v0.1.0` 中已经写进备注的营养文字不会自动转成正式档案。
- 本包不会自动修改 OpenClaw、重启 Gateway、安装 Docker 或操作正式数据库。
