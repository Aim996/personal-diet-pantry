# 食序管家 v0.7.2 更新说明

## 本版目标

v0.7.2 把食序管家继续收敛为一套可复制、可安装、可迁移的个人饮食与库存 Skill：为模型提供由机器契约校验的首选能力路线，在错误恢复中要求每一步都增加证据或修复明确问题，并把简单库存场景从宽泛浏览改为有界的定向搜索。本版保留七个公共工具、现有营养与事务语义，以及餐食最多八层原料结构。

## Skill 导航与后台自救

主 Skill 根据当前意图只加载会影响本次动作的最小 reference，并通过生成的能力路线选择首选工具和 action。首选能力确实缺失或改名时，只有当前可见说明和 Schema 能证明输入、输出与安全语义等价，才可使用替代能力。

失败后的每个动作必须取得新事实、修正结构化错误指出的字段、消除歧义、核验不确定写入，或采用契约验证的等价能力。相同能力、工具、action、规范化参数和错误特征不得原样重复；库存不足、低置信度、不可撤销等业务结果不能通过换工具绕过。调试过程留在后台，用户只收到最终结果或清楚的阻塞说明。

## 库存定向搜索

`diet_pantry(search)` 接受用户原始 `search_text`，以及可选的 `unit`、`statuses`、`storage_location`、`limit` 和 `nutrition_mode`。`limit` 默认 5 且最大为 5，因此每次最多 5 个紧凑商品候选；系统依次使用规范名、静态或已学习别名和受索引支持的关键词匹配，不把完整库存交给模型扫描。

同一商品的多个批次聚合为一个候选，不同商品才形成歧义。候选携带不透明的 `inventory_match_handle`；用户或模型选定后，`diet_meal(record)` 复用该句柄完成身份校验、确定性批次扣减和营养缩放，不再次按名称搜索。只有用户明确要求浏览完整库存时，才使用 `query` 分页读取。

## 库存与营养一次组合返回

营养数据仍规范化、分层保存在同一个 `diet.sqlite`：`nutrition_profiles` 保存版本化档案，`pantry_nutrition_links` 保存各批次独立的营养快照，缓存仍是可重建辅助数据；不会把营养字段复制到每个库存批次。

`nutrition_mode` 只允许 `none | summary | full`，默认 `none`。普通定位不读取或展开营养；主要营养问题使用 `summary`，在同一次定向搜索中返回有界摘要；只有用户明确请求完整营养标签时才使用 `full`。餐食消费已关联库存时，服务在一次 `diet_meal` 操作内读取批次快照，不要求模型先额外查询营养档案。

## 餐食 Schema 压缩

餐食公共 Schema 将重复展开的嵌套原料改为根级 `$defs/$ref` 复用，仍接受八层原料并拒绝第九层，所有既有公共 action 和闭合对象校验保持不变。序列化 `MealParametersSchema` 的回归预算为小于 125,000 字节，避免把约一百万字节的重复结构长期带入工具上下文。

## 数据迁移与兼容

`migrations/020_inventory_search.sql` 为库存定向搜索增加索引，并把 `pantry_product_reference` 加入操作预览类型；迁移继续按 checksum 验证并在一个事务中应用。营养档案、批次链接、快照和缓存仍在同一 SQLite 数据库内分层保存。

可携带导入保留既有 `0.6.7`、`0.7.0` 兼容范围，并新增接受 `0.7.1` 与 `0.7.2` 生成的导出。升级迁移是向前边界：旧版本程序不应直接打开已经由 v0.7.2 完成迁移的数据库。

## 验证结果

本版的版本合同、发布输入、工具契约生成、库存搜索、句柄复用、迁移 020、餐食 Schema 体积与八层边界均由自动化测试覆盖。发布构建从 JUnit 与 Vitest 机器报告分别记录总计、通过、跳过和失败数；合法跳过允许发布，任何失败、错误或计数不一致都会阻止构建。最终完整门禁的测试数量、发布包哈希和复现结果只由发布构建生成，不在本说明中手填或提前宣称。

正式发布路径必须位于 Git 工作树和项目根目录之外，且构建前不得存在；构建后的顶层恰好包含 `personal-diet-pantry-0.7.2-source.tar.gz`、`personal-diet-pantry-0.7.2-installable.tgz`、`release-manifest.json`、`TEST-SUMMARY-v0.7.2.zh-CN.md`、`SHA256SUMS` 和 `GitHub文档/`。其中 `SHA256SUMS` 覆盖前四个文件；旧名校验文件不属于 v0.7.2 发布合同。

## 升级与回退

v0.7.2 不会自动部署、安装、启用、重启或修改任何 OpenClaw/Telegram/Docker/软路由实例。升级前必须停止目标实例，按[安装手册](docs/INSTALLATION.zh-CN.md#5-备份用途与降级冷备份)使用随源码发布的 helper 保存包含已提交 WAL 数据的一致 SQLite 升级前冷备份；随后安装 `personal-diet-pantry-0.7.2-installable.tgz`，保留原 `dataDir`，等待迁移 020 完成，再执行 `self_check` 和只读业务核验。

回退必须同时具备 **v0.7.1 可安装包** 和这份**升级前数据库备份**。在线 `diet_system backup` 仅用于同版本恢复，不能替代降级冷备份；v0.7.2 的 restore 会重施 migration 020。停止实例后验证并恢复冷备份，再安装 `personal-diet-pantry-0.7.1-installable.tgz`；不得只降级代码后继续使用已迁移数据库，也不得删除迁移记录或手工逆改 schema。

## 非目标

- 不自动部署或覆盖现有实例，不配置 OpenClaw、Telegram、Gateway、Docker、Lucky、WireGuard 或软路由。
- 不增加固定调用次数上限，不允许为强行成功而绕过业务错误。
- 不把完整库存、完整营养库、动态会话内容或调试日志写进主 Skill。
- 不引入云搜索、向量数据库、外部模型 API Key 或自动图片识别。
- 不降低八层餐食原料能力，也不把规范化营养数据反规范化复制进库存批次。
