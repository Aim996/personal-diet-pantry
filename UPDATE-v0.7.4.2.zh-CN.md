# 食序管家 v0.7.4.2 更新说明

产品版本：`0.7.4.2`
技术包版本：`0.8.2`
上一产品版本：`0.7.4.1`

## 本版目标

本版只解决一个维护问题：把分散的 GitHub 更新、授权、版本、验证、发布和交接规则整理成项目根目录的单一入口。后续维护者或执行型 AI 可以先读取 `GITHUB-WORKFLOW.zh-CN.md`，再进入产品行为约束、发布手册和安装文档，不需要从历史对话猜测流程。

本版不增加新的日常饮食功能，也不改变既有业务语义。受保护的六项两行进度回执、七类工具、公开动作、时间范围、份量确认、库存营养、纠错、过期食品过滤和 SQLite 事实来源全部保持不变。

## 用户与维护者可见变化

- 项目根目录新增详细的 `GITHUB-WORKFLOW.zh-CN.md`。
- 文档明确要求先判断修改是否有真实用户价值，避免为了修复或发版而机械迭代。
- 文档分别定义本地修改、提交、推送、Draft PR、合并、Tag、Release、仓库可见性和 OpenClaw 上线的授权边界。
- 文档提供版本同步清单、失败停止条件、任务输入模板、执行型 AI 简版指令和交接记录模板。
- README 增加维护者入口，但普通 OpenClaw 用户优先的首页结构保持不变。

## 兼容性与数据

- 本版没有新增 migration，继续使用 migrations 001–021，schema 与 v0.7.4.1 相同。
- 本版不会修改用户数据，不读取生产 `dataDir`，也不改变七类工具或公开行为。
- v0.7.4.2 导出可被当前导入器识别，所有既有导入兼容版本继续保留。
- 更新前仍需停止目标实例并创建、校验升级前冷备份；在线 `diet_system backup` 只用于同版本恢复，不能替代升级前冷备份。
- 若更新失败，可停止实例并重新安装 v0.7.4.1 包；数据或环境损坏时以升级前冷备份作为恢复边界。

## GitHub 与发布边界

本地完成文档和版本同步不等于已经上传 GitHub；推送分支不等于合并或发布；创建 Release 也不等于已经安装到任何 OpenClaw 实例。

本文件描述的是本地候选和后续 GitHub 审阅内容。仓库在本轮开始时保持 Private，尚未创建 Git Tag 或 GitHub Release；目标版本为 `v0.7.4.2`，也不会自动部署。只有完整门禁通过、用户明确批准且同名 Tag/Release 均不存在时，才能进入正式发布。

本地候选目录顶层只包含 `personal-diet-pantry-0.7.4.2-source.tar.gz`、`personal-diet-pantry-0.7.4.2-installable.tgz`、`release-manifest.json`、`TEST-SUMMARY-v0.7.4.2.zh-CN.md`、`SHA256SUMS` 和 `GitHub文档/`。其中 `GitHub文档/` 仅供本地审阅，不作为第六个 GitHub Release 附件。
