# 食序管家 v0.7.4.24 更新说明

产品版本：`0.7.4.24`

技术包版本：`0.8.24`

## 为什么开发这一版

v0.7.4.23 的直接记餐、库存联动事务、撤销、时间查询、过期过滤和回执生成均已有可用实机证据，但真实 OpenClaw 复测发现一个集中在“澄清后的下一句话”的阻断问题：模型生成的预览本身正确，用户回复“确认入库”“确认，按上面补记”或“大概10粒，就按10克记吧”后，回合守卫却把它们误判为普通新增或无授权文本，导致提交被拒绝并反复确认。

库存营养未知时也受同一问题影响：用户补充包装标签并明确要求记录，系统仍可能再次预览，或错误尝试修改 Pantry 后要求拆开操作。底层 Meal 与 Pantry 原子事务没有损坏，因此本版只修自然确认和带最终数据的补充消息路由，不增加新的工作流缓存或数据库结构。

## 本版修复

- “确认入库”“确认，2盒……按预览入库”“确认，按上面补记”等自然确认统一进入 `workflow_confirmation`，只能使用当前可见、未变化的有效预览句柄执行一次 `commit_add` 或 `commit_record`。
- 裸确认不能创建新的 `add`、`record` 或重建预览；当前会话没有有效预览句柄时保持零写入，只询问最小缺失事实。
- 同一条补充消息既给出可用最终数据、又明确说“就按……记”或“直接记录”时，合并仍可见的已发生摄入事件后执行一次 `diet_meal record`，不再生成替换预览或要求二次确认。
- 库存已唯一命中但营养明确未知时，用户补充标签并授权记录，可以把原库存 `inventory_match_handle` 与用户标签营养一起交给同一笔 Meal 事务；不得另调 Pantry、单独扣库或写入零营养餐。
- “确认一下库存还有没有燕麦奶”等包含“确认”字样的查询仍保持只读；只有确认预览或带最终数据且明确授权记录的陈述才进入写入路由。

## 明确保持不变

- 明确已摄入的玉米、火腿肠等继续直接记录，不新增普遍确认步骤；玉米仍显示 `1个｜可食部（玉米粒）约90克（估算）`，玉米芯不计营养。
- 真正模糊、尚未给出可用最终值的份量继续保持零业务写入，只给一次有用估算和一个问题。
- 普通餐食继续使用六项双行进度回执；纯水继续只显示饮水确认和饮水双行。
- Meal/Pantry 原子扣库与撤销返库、相对时间查询、过期推荐硬过滤、计划/否定/他人行为隔离均不修改。
- 不处理 OpenClaw WebUI“当前会话无法新建会话”的独立 UI-01 问题。
- v0.7.4.23 及更早的正式目录、包、哈希和测试记录保持不可变。

## 数据、安装与回退

本版没有新增 migration，继续使用 migrations 001–021，schema 与 v0.7.4.23/v0.7.4.19 相同。更新前必须停止实例并创建、校验位于外部 `dataDir` 之外的升级前冷备份；只安装已校验的 `personal-diet-pantry-0.7.4.24-installable.tgz`。安装、自检或真实 UAT 失败时保持数据不变，重新安装已校验的 v0.7.4.19 包恢复技术运行；在线 `diet_system backup` 不能代替升级前冷备份。

## 阻断性口语化验收

1. “吃了点花生”得到一次估算后回复“大概10粒，就按10克记吧”：只能正式写入一次，不再预览或确认；随后修改、撤销并核对数据。
2. 完整 Pantry 预览后分别回复“确认入库”和“确认，2盒……按预览入库”：必须各只调用一次 `commit_add`；无有效句柄时必须零写入。
3. 历史餐食预览后回复“确认，按上面补记”：必须只调用一次 `commit_record`，本地时间与原预览一致。
4. 库存营养未知的饮料在用户补充包装标签并说“就按整盒记，直接记录”后：只能调用一次 Meal 写入，营养非零、库存原子扣减，撤销后营养和库存一并恢复。
5. “确认一下库存还有没有燕麦奶”：只能读取，不得提交任何预览或新增。
6. 继续复测玉米新增/80克纠正/删除、137ml 纯水/撤销、体重/撤销、跨午夜时间查询、过期推荐和复合句零写入，任一保护项失败均阻断安装。

## 候选制品契约

本地候选目录顶层必须恰好包含：

- `personal-diet-pantry-0.7.4.24-source.tar.gz`
- `personal-diet-pantry-0.7.4.24-installable.tgz`
- `release-manifest.json`
- `TEST-SUMMARY-v0.7.4.24.zh-CN.md`
- `SHA256SUMS`
- 本地审阅留痕目录 `GitHub文档`

当前仍是开发候选。只有总约束保护矩阵、全量自动化、可重复构建、安装包审计、停机冷备份、软路由安装、真实口语化 UAT、测试数据清理和数据库核验全部通过，才能标记为可用；本项目不会自动推送 GitHub、创建 Tag 或 Release。
