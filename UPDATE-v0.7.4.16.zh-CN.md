# 食序管家 v0.7.4.16 更新说明

产品版本：`0.7.4.16`
技术包版本：`0.8.16`

## 为什么开发这一版

0.7.4.15 已能一次记录“吃了个玉米”，并生成完整可食部与六项双行回执；但真实 OpenClaw 中紧接“其实是80克”仍会猜无时区时间、错误时区和不存在的餐食句柄。即使查询已经返回正式记录，Agent 仍可能抄错长句柄，最后触发熔断。数据库会安全保留原记录，但自然纠正不可用。

本版只修一个边界：同一会话的紧接纠正必须复用上一笔成功餐食回执返回的真实操作凭证。它不增加提醒、菜谱、图片、多人协作或新的健康功能，也不靠放宽安全校验换取表面成功。

## 本版修复

### 1. 紧接纠正复用真实餐食凭证

- 餐食新增或更新成功后，插件在当前 OpenClaw 会话内短期保存工具返回的真实 `meal_handle`。
- 下一轮只有被可信意图分类为“其实是……”“不对，是……”“刚才那个改成……”的餐食更新，才使用该凭证定位上一笔。
- 模型即使提交无时区 selector 或格式像真但不存在的句柄，工具边界也会在执行前替换成当前会话快照中的真实句柄。
- 更新成功后保存新句柄，便于用户继续紧接纠正；网关重启、凭证过期或上下文无法唯一定位时，回退为查询选择，不猜记录 ID。

### 2. 保留原有安全校验

- 不同会话绝不共享凭证；明确指定昨天午餐等历史目标时，不自动套用最近餐食。
- 删除操作不使用这条便捷路径，仍需真实目标凭证或安全查询定位。
- selector 的宿主 schema 只接收形状，以便安全守卫有机会替换；没有被真实句柄替换的无时区或无效时间，仍在执行入口严格失败并保持零写入。
- 两次失败熔断、只读隔离、原子更新和错误目标拒绝全部保留。

## 保持不变

- 普通餐食仍固定热量、蛋白、脂肪、碳水、纤维、饮水六项，每项两行。
- 单独喝纯水仍只显示饮水确认和饮水双行进度，不展开营养六项。
- `吃了个玉米`保留 `1个`，营养按可食部计算，玉米芯不计入；紧接“其实是80克”仍直接原子更新为明确的可食部80克。
- 真正模糊份量仍给出可用范围并只问一个问题，确认前零写入。
- 查询只读，否定、计划和他人行为零写入；停止任务不跨轮重放。
- 库存联动和撤销返库、等价相对时间窗、过期食品推荐硬过滤、目标与库存建议保持原行为。
- 不修改 OpenClaw WebUI 新会话按钮问题。
- 本版没有新增 migration、数据库字段、公开 action 或跨域大事务；继续使用 migrations 001–021。
- 0.7.4.15 及更早版本的目录、提交、制品和校验值保持不可变。

## 更新、回退与候选制品

本版与 v0.7.4.15 使用相同数据库 schema。更新前仍需停止实例并创建、校验位于 `dataDir` 之外的升级前冷备份；在线 `diet_system backup` 不能替代升级前冷备份。若安装或验收失败且数据未受损，停止实例并重新安装已校验的 0.7.4.15 可安装包；若数据库或数量异常，先恢复冷备份再回退代码。

候选目录顶层契约为：

- `personal-diet-pantry-0.7.4.16-source.tar.gz`
- `personal-diet-pantry-0.7.4.16-installable.tgz`
- `release-manifest.json`
- `TEST-SUMMARY-v0.7.4.16.zh-CN.md`
- `SHA256SUMS`
- 本地审阅留痕目录 `GitHub文档`

## 重点口语化验收

1. 新会话说“吃了个玉米。”，必须一次直接落账并显示 `1个｜可食部（玉米粒）约90克（估算）`。
2. 紧接说“其实是80克。”，必须一次更新为 `1个｜可食部（玉米粒）80克`；最终回执不能缩写掉自然计数、计量对象或六项双行进度。
3. 说“吃了点花生。”，必须给出非零估算范围，只问一个问题并保持零写入。
4. 另开会话后说“其实是75克”，不得借用前一个会话的玉米凭证；明确说“把昨天午餐改成……”也不得自动套最近一条。
5. 回归纯水短回执、蛋白粉扣库与撤销返库、等价跨午夜查询、过期食品过滤、否定/未来/他人语义和查询零写入。
6. 删除全部测试数据，复核升级前后账本数量、SQLite 完整性和 migration 状态。

## 发布状态

当前内容是开发候选。只有完整自动化、可复现构建、安装包审计、软路由冷备份与安装、真实 OpenClaw 全保护矩阵、测试清理和数据基线全部通过后，才能标记为可用候选。仓库脚本不会自动推送 GitHub、创建 Tag/Release。
