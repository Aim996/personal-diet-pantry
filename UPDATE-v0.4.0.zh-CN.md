# 食序管家 v0.4.0 更新说明

## 这次更新解决什么

v0.4.0 重点解决新商品入库反复确认、工具红色报错、营养表没有结构化保存，以及
“豆浆”等简称无法自动命中实际库存的问题。

- 明确的“买了、入库”使用一次 `diet_pantry add`，库存、包装重量和营养表在
  同一事务内完成；任一步失败都会整笔回滚。
- 明确的“吃了、喝了”使用一次 `diet_meal record`，不再把预览句柄交给模型
  拼接，也不会在工具已经表示无需确认后重复询问。
- 当前库存中只有一款匹配商品时，“豆浆”可以自动命中
  “5.0蛋白浓醇豆浆”；存在多款合理候选时仍会让用户选择。
- 液体库存保留 `ml` 单位，扣减结果返回实际商品名、扣减量和剩余量。

## 营养与饮水

上传的包装营养表会与具体库存批次建立结构化快照。后续明确表示吃了或喝了该
商品时，系统先从命中的库存批次读取实际营养表，再按本次摄入量换算；没有实际
营养表时依次使用个人缓存、内置数据和模型 C/D 级估算，不会仅因缺少营养匹配
再次询问确认。

牛奶、豆浆、咖啡、汤和粥等液体或流质可以携带 `hydration_ml`。这部分水分随
餐食一同保存，并与显式喝水记录合并到当天饮水进度；更正、删除或撤销餐食时，
对应水分也会同步调整，不会额外伪装成一条白水记录。

## 用量估算

用户已经明确吃了或喝了、但没有说具体数量时，由模型结合原话、容器、包装规格、
上下文和个人习惯估算一个具体值。例如“喝了一口豆浆”应估算为一小口的毫升数，
不再固定写成 250ml 或一整杯。回复只简洁标记“用量估算”或“估算”。

## 回复格式

餐食成功写入后，回复固定为：

1. `已记录！内容`；
2. 热量、蛋白、脂肪、碳水、纤维、饮水六项，每项固定两行；
3. 本次增量只显示在对应指标第二行；
4. 只有实际发生库存变化时才追加 `📦 库存变动：`。

不再额外输出横向营养摘要、“今天的进度更新”标题、完整库存、内部编号、句柄或
置信度。

## 数据安全与升级

本版本新增 `010_meal_hydration.sql`，只给餐食及餐食项目增加可为空的水分字段。
历史餐食的水分保持未知，不会按名称批量猜测。现有 SQLite 数据、库存数量、
营养快照和撤销日志全部保留。

升级前必须：

1. 备份当前 SQLite 并验证备份可读；
2. 保留 v0.3.0 源码副本；
3. 替换 Skill 与插件源码后执行迁移；
4. 重新加载六个工具并运行 `diet_system self_check`。

如需回退，必须同时恢复升级前的 SQLite 备份，不能让旧版本直接使用已经迁移到
v0.4.0 的数据库。

## 现有数据修复

升级完成后，通过正式库存元数据和营养关联工具补齐已经存在但字段缺失的批次，
不重新创建库存、不改变数量。包括：

- 余同乐素食北京烤鸭：补齐每包 76g、4 包共 304g 及包装营养表；
- 已在备注中保存营养表的豆浆、牛奶、香干等商品：解析后关联到原批次。

修复前后都要核对每个批次的剩余数量，禁止直接编辑 SQLite。
