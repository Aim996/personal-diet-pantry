# 食序管家 v0.7.4.26 更新说明

产品版本：`0.7.4.26`

技术包版本：`0.8.26`

## 为什么开发这一版

v0.7.4.25 已经稳定了“模糊预览→精确补充→确认写入→纠正/删除”的主要链路，也通过了玉米、纯水、Pantry、过期推荐和安全语义测试；但软路由真实 UAT 仍发现两个会破坏核心账本可信度的问题。

第一，Meal 工具对“8月3号晚上到4号天亮前”已经正确返回 5 条完整记录，最终自由文本回答却把两条内容相同的独立餐食合并成 4 条。第二，库存饮料营养未知时，用户补充每 100ml 的热量和三大营养素，系统会因消费体积字段与缺失纤维字段连续失败，并错误要求用户把未知纤维说成 0。

本版只关闭这两个根因，不增加新的饮食业务、数据库表、迁移或普遍确认流程。

## 本版修复

- 对 `scope.complete: true` 且意图为只读“这个时间段吃了什么”的 Meal 查询，宿主直接生成确定性清单：条数严格等于工具返回的 `meals` 数组长度，每条记录按返回顺序单独编号；内容、时间或热量相同也不去重，不展示工作流句柄。
- 完整清单成功返回后，同一轮阻止第二个饮食工具调用，避免模型拿到正确结果后继续搜索、改写范围或二次删减。
- 当营养基准是 `per_100ml` / `per_100g`，且结构化份量已经是精确兼容的 ml/L 或 g/kg 时，宿主确定性补齐 `consumed_volume_ml` / `consumed_weight_g`；包装单位、模糊量和不兼容单位不在这里猜测。
- 库存凭证包含“一盒＝180ml”等正式换算时，后端先解析该库存换算，再校验用户提供的每 100ml/100g 标签，因此可以保留一次 Meal 原子写入，不再为了补字段反复调用。
- A/B 包装标签可以只包含用户真正提供的营养项。已知项正常缩放，未提供的纤维、钠、饮水或其他项保持 `未知`；不得补成 0，也不得要求用户编造 0。
- 全部营养字段都为空的“标签”会在宿主入口被结构化拒绝，避免无意义 Python 调用；至少一个真实营养字段才构成部分标签证据。

## 明确保持不变

- “吃了个玉米”“吃了根火腿肠”等明确已发生摄入继续首句直接记录，不增加普遍预览或确认。
- 玉米继续显示自然计数、玉米粒可食部和估算克数，例如 `1个｜可食部（玉米粒）约90克（估算）`；紧接 80 克仍一次原子纠正并移除冲突估算。
- “吃了点花生”等真正模糊量继续只做一次有用预览，回答前零业务写入；取消后旧预览不能复活。
- 普通餐食继续使用热量、蛋白、脂肪、碳水、纤维、饮水六项双行回执；纯水仍只显示饮水双行。
- Pantry 入库/撤销、餐食原子扣库/返库、查询零写入、计划/否定/他人行为隔离、跨域复合写入失败关闭和过期食品推荐硬过滤保持不变。
- 不修改 schema、migrations 001–021 或独立的 OpenClaw WebUI UI-01 问题。
- v0.7.4.25 及更早版本目录、提交、安装包和哈希保持不可变。

## 数据、安装与回退

本版没有新增 migration，继续使用 migrations 001–021，schema 与 v0.7.4.25/v0.7.4.19 相同。更新前必须停止目标实例，在外部 `dataDir` 之外创建并校验升级前冷备份，只安装已校验的 `personal-diet-pantry-0.7.4.26-installable.tgz`。在线 `diet_system backup` 只用于日常辅助备份，不能替代升级前冷备份。安装、自检、真实 UAT 或数据审计失败时，恢复安装前备份与最近可启动包；不得回写旧版本。

## 阻断性口语化验收

1. “8月3号晚上到4号天亮前吃了什么？”与等价显式范围必须返回同一个完整记录集合；工具返回 5 条时最终回复必须明确显示 5 条，两个相同内容的独立记录仍是两条。
2. 库存中存在一盒 180ml、营养未知的豆奶时，先说“刚喝了一盒”，再补充“每100ml：70千卡、蛋白3克、脂肪2克、碳水10克；整盒180ml，直接记录”：只能发生一次 Meal 写入，已知营养按 1.8 倍缩放，纤维和钠保持未知，库存扣 180ml；撤销同时删除餐食并返库。
3. 相同场景不提供纤维/钠时，不得再询问“是否按0记录”，不得产生第二、第三次 Meal 失败调用，也不得扫描源码或数据目录。
4. 回归玉米新增/80克纠正/删除、纯水新增/撤销、花生模糊预览/确认/纠正/删除、Pantry 预览/确认/撤销、过期推荐、状态查询、计划/否定/他人行为、查询零写入和跨域复合句零写入。
5. 任一总约束保护项失败，或安装后存在未清理测试数据、数据库完整性异常、版本/哈希不符，均阻断验收。

## 候选制品契约

本地候选目录顶层必须恰好包含：

- `personal-diet-pantry-0.7.4.26-source.tar.gz`
- `personal-diet-pantry-0.7.4.26-installable.tgz`
- `release-manifest.json`
- `TEST-SUMMARY-v0.7.4.26.zh-CN.md`
- `SHA256SUMS`
- 本地审阅留痕目录 `GitHub文档`

当前仍是开发候选。只有总约束保护矩阵、全量自动化、可重复构建、安装包审计、停机冷备份、软路由安装、真实口语化 UAT、测试数据清理和数据库核验全部通过，才能标记为可用；本项目不会自动推送 GitHub、创建 Tag 或 Release。
