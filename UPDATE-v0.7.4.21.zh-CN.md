# 食序管家 v0.7.4.21 更新说明

产品版本：`0.7.4.21`
技术包版本：`0.8.21`

## 为什么开发这一版

v0.7.4.20 已经让同会话“把刚才那个玉米删了”可以一次完成，但真实 OpenClaw UAT 发现了更严重的授权边界：全新会话说同一句时，Agent 先查询一天内的餐食，得到三条玉米后擅自选择最新一条并删除。插件没有泄漏另一个会话的缓存；真正问题是查询候选句柄被错误当成了同会话操作凭证。

本版只修这个已经复现并可能误删真实账本的缺口，不扩大业务范围。

## 本版修复

- “刚才那个、刚刚那条、这条、那条、整条”等无法脱离当前会话唯一定位的删除或纠正，只能使用本会话最近一次成功餐食操作的可信句柄。
- 新会话没有可信句柄时，查询结果只能用于展示最少候选；同一轮不得从候选中猜一条并修改或删除，必须等待用户明确选择。
- 同会话已有可信句柄时仍然直接执行一次；即使 Agent 多做了一次只读查询，插件也只使用原可信句柄。
- “删除昨晚22:00那条UAT饭团”这类带明确钟点和食物身份的历史操作不再绑定当前会话最近餐食，保留窄查询后精确操作的能力。

## 明确保持不变

- 明确摄入直接记录，不增加预览或确认；玉米继续保留 `1个`，标准示例仍明确显示 `可食部（玉米粒）约90克（估算）`。
- 紧接“其实是80克”继续一次原子更新；普通餐食继续六项双行回执。
- 纯水继续只显示饮水确认和饮水双行，不展开营养。
- 模糊份量、包装换算、营养证据、库存扣减与撤销返库、时间窗、过期推荐、否定/计划/他人隔离均不修改。
- 不修改 Meal/SQLite 事务，不新增 migration，不处理 OpenClaw WebUI“新会话”按钮问题。
- v0.7.4.20 及更早目录、包、哈希和记录保持不可变。

## 更新与回退

本版没有新增 migration，继续使用 migrations 001–021。更新前必须停止实例，并在外部 `dataDir` 之外创建和校验**升级前冷备份**；在线 `diet_system backup` 只用于同版本恢复，不能替代升级前冷备份。只安装已校验的 `personal-diet-pantry-0.7.4.21-installable.tgz`。如果安装、自检或真实授权 UAT 失败，保持数据不变并重新安装已校验的 v0.7.4.19 包恢复技术运行；已确认会跨会话误删的 v0.7.4.20 不能作为回退目标。

## 重点口语化验收

1. 同一会话先说“吃了个UAT21玉米”，再说“把刚才那个玉米删了”：只调用一次 Meal delete，直接返回六项双行负增量。
2. 另开新会话只说“把刚才那个玉米删了”：允许一次窄查询，但不得删除任何候选；应展示最少候选并等待选择。
3. 新会话说“其实是80克”：不得用查询到的任意餐食句柄修改真实记录。
4. 新会话说“删除昨晚22:00那条UAT21饭团”：能够按明确钟点查询和精确删除，不得改成当前会话最近一餐。
5. 复核玉米新增/80克纠正、纯水137ml、模糊花生、库存燕麦奶、时间窗、推荐过滤与否定/计划/他人零写入。

## 候选制品契约

本地候选目录顶层必须恰好包含：

- `personal-diet-pantry-0.7.4.21-source.tar.gz`
- `personal-diet-pantry-0.7.4.21-installable.tgz`
- `release-manifest.json`
- `TEST-SUMMARY-v0.7.4.21.zh-CN.md`
- `SHA256SUMS`
- 本地审阅留痕目录 `GitHub文档`

当前仍是开发候选。只有总约束保护矩阵、全量自动化、可重复构建、安装包审计、停机冷备份、软路由安装、真实口语化 UAT、测试数据清理和数据库核验全部通过，才能标记为可用；本项目不会自动推送 GitHub、创建 Tag 或 Release。
