# 食序管家 v0.7.3.1 更新说明

## 本版目标

v0.7.3.1 是 v0.7.3 的兼容性增量发布，重点修复 DeepSeek/OpenAI 兼容模型看到的餐食工具 Schema 过大、液体营养证据难以稳定提交，以及用户未说时间时模型被迫猜测时间的问题。本版不新增业务数据库表，不改变现有库存、餐食、饮水、体重或目标数据。

## 模型侧 Schema 兼容

根因不是豆浆营养算法，而是 OpenClaw 的模型侧 Schema 归一化会递归展开餐食条目的 `allOf/anyOf` 组合，将工具参数放大到约 813 KB。部分模型因此拒绝或截断工具定义，表现为参数反复试错、液体字段丢失或根本没有调用 Skill。

本版把模型公开的餐食条目改成单层、封闭且有界的结构。按宿主实际归一化路径计算，模型侧参数预算保持 `<160000 bytes`，同时保留 `per_100g`、`per_100ml`、`per_serving`、`consumed_total`、`consumed_volume_ml` 和营养证据字段。嵌套食材仍有数量上限，不允许无限递归扩张。

## 确定性营养校验

跨字段关系移到确定性运行层校验，并同时在 TypeScript 入口和 Python 业务边界执行：

- 营养事实与估算不能同时提交；
- 直接营养证据必须带 basis，basis 也必须带证据；
- `per_100g`、`per_100ml`、`per_serving` 必须分别带正数的重量、体积或份数；
- `consumed_total` 不会再次按重量或体积缩放。

这不是降低验证强度，而是避免让模型 Schema 承担会被宿主指数展开的关系表达。错误会返回稳定字段路径和原因，失败请求不会写库。

## 可信系统时间

`diet_meal record`、`preview_record` 和 `record_cooking` 现在允许省略 `occurred_at`。用户没有明确说时间时，插件使用可信系统时间；用户明确给出时间时仍原样尊重。模型不需要也不应猜测时区或伪造 `context.now`。

## 液体、饮水与库存闭环

包装液体按一个独立体积维度处理。以 250ml 豆浆、标签 basis 为 `per_100ml` 为例，缩放因子只计算一次，即 2.5；33 kcal、3.5g 蛋白、95ml hydration 分别得到 82.5 kcal、8.75g 蛋白和 237.5ml hydration。hydration 仍受已饮用体积上限约束，不会超过 250ml。

当库存为 2 盒 × 250ml 时，定向搜索返回的真实商品句柄用于餐食扣减：喝 1 盒后基础库存从 500ml 变为 250ml，包装展示从 2 盒变为 1 盒。通过公开撤销操作可同时移除餐食与营养证据，并把库存恢复为 500ml/2 盒。

## 数据迁移与兼容

本版没有新增 migration，继续使用 v0.7.3 已包含的 migration 001–021，也继续复用同一个 `diet.sqlite` 和原 `dataDir`。营养档案仍保持规范化存储，不复制进库存批次。数据导入新增接受产品版本 `0.7.3.1`，并继续兼容既有受支持版本。

技术包版本使用合法 SemVer `0.7.4`，面向用户的产品版本为 `0.7.3.1`。发布文件名、导出 manifest 和文档统一使用产品版本，包管理器与插件元数据使用技术版本。

## 发布制品

正式发布目录顶层恰好包含：

- `personal-diet-pantry-0.7.3.1-source.tar.gz`；
- `personal-diet-pantry-0.7.3.1-installable.tgz`；
- `release-manifest.json`；
- `TEST-SUMMARY-v0.7.3.1.zh-CN.md`；
- `SHA256SUMS`；
- `GitHub文档/`。

源码包只用于审阅和复现，安装时只使用可安装包。

## 升级与回退

升级前仍建议停止实例并保存升级前冷备份，然后安装 `personal-diet-pantry-0.7.3.1-installable.tgz`，保留原 `dataDir`，启动后运行 `diet_system self_check`。因为没有新增 migration，可直接回退到 v0.7.3 代码；但数据库和发布包仍应成套冷备份，避免把运行中未落盘状态误当作可回退数据。在线 `diet_system backup` 仅用于同版本恢复，不能替代升级前冷备份。

## 非目标

构建发布不等于部署。本版不会自动部署，也不会自动安装、启用、停止或重启 OpenClaw，不会修改软路由、Docker、Telegram、模型配置、凭据或生产数据，也不会替用户补录历史记录。
