# 食序管家 v0.3.0 更新说明

## 这次更新解决什么

v0.3.0 重点解决“做了多少、吃了多少、剩了多少”混在一起造成的库存与营养
错乱，同时缩短日常回复和工具读取结果。

例如：

- “煎了6个蛋，吃了4个，剩2个冷藏”：生鸡蛋扣除6枚，饮食记录4枚煎蛋，
  新增冷藏煎蛋2枚。
- “一盘辣椒炒牛肉吃了一半”：制作使用的牛肉和辣椒全部出库；一半营养计入
  本次饮食，另一半作为带营养快照的熟食库存保存。

这些动作在同一事务中完成。中途任一步失败，都不会留下只扣原料、没有剩菜，
或只有饮食记录、库存没变化的半成品状态。

## 新增与优化

- 区分 `prepared`（制作）、`consumed`（实际摄入）和 `leftover`（剩余熟食）。
- 新增迁移 `009_prepared_food_profiles.sql`，把熟食库存与
  `portion_total` 营养快照关联。
- 后续吃剩菜时直接读取熟食营养并扣熟食库存，不再重复扣原始食材。
- 餐食提交直接返回 `daily_progress` 和 `inventory_effects`，不需要再查询完整
  日报或完整库存。
- 普通库存查询默认最多20项，只返回名称、余量、单位、状态和必要的到期信息。
- 只有指定商品并设置 `include_details: true` 时，才读取营养、备注、重量信息
  和后续操作句柄。
- Skill 内置统一进度格式，固定使用 `✅ 已记录`、六项营养 Emoji 和可选的
  `📦 库存变动`。
- 不确定用户是否吃了某项食物时，不再默认写成已经摄入。

## 数据安全

升级会保留现有 SQLite 数据，只新增兼容字段和表。v0.3.0 不会自动改写历史记录，
也不会主动修正过去可能存在的鸡蛋、牛肉或其他库存差异。

升级前必须备份当前 SQLite 数据库，并保留 v0.2.0 的源码包或安装包。数据库经过
新迁移后，如果需要回退到 v0.2.0，应同时恢复升级前的 SQLite 备份，不能让旧版本
直接继续使用已经迁移到 v0.3.0 的数据库副本。

## 升级步骤

1. 停止正在进行的饮食或库存写入。
2. 使用现有备份功能备份 SQLite，并确认备份文件可见。
3. 保存当前 v0.2.0 Skill 和插件包，以便回退。
4. 替换为 v0.3.0 Skill 和插件源码。
5. 让 OpenClaw 重新加载插件及六个饮食工具。
6. 调用 `diet_system migrate`，应用
   `009_prepared_food_profiles.sql`。
7. 调用 `diet_system self_check`，确认数据库、迁移、工具和目录状态正常。

本源码包本身不替用户部署，也不直接修改 OpenClaw 系统文件。

## 回退

1. 停止 v0.3.0 的写入。
2. 保存当前数据库副本用于排查。
3. 恢复升级前的 SQLite 备份。
4. 恢复保留的 v0.2.0 Skill 和插件包。
5. 重新加载 OpenClaw，并执行 v0.2.0 自检。

## 建议上线验收

- 用30枚且带总重量的鸡蛋测试“煎了6个、吃4个、剩2个冷藏”。
- 测试辣椒炒牛肉整盘制作、吃一半、另一半冷藏。
- 再吃一部分冷藏剩菜，确认只扣熟食库存，并按熟食营养快照计算。
- 记录一顿不涉及家庭库存的外食，确认没有库存区。
- 查看全部库存，确认默认最多20项且不返回备注和完整营养。
- 指定一个上传过营养表的商品查看详情，确认能返回该商品的营养快照。
- 观察回复顺序是否为“✅ 已记录 → 进度块 → 可选 📦 库存变动”。

这些上线验收由用户在真实 OpenClaw 环境中完成；源码侧只提供有针对性的自动检查。
