# 食序管家 v0.7.4.27 更新说明

产品版本：`0.7.4.27`

技术包版本：`0.8.27`

## 为什么开发这一版

v0.7.4.26 已让玉米直接记录与纠正、纯水简洁回执、模糊量预览、部分标签写入、库存原子扣减/返库、过期推荐过滤和查询零写入在真实 OpenClaw 中通过，但仍有三个可复现缺口阻断放行。

第一，用户明确说“8月3号傍晚六点到4号早上六点”时，结束时刻会被“傍晚”的默认段落边界覆盖成 23:00。第二，同一轮已经说明“一盒就是180毫升”并提供每 100ml 标签时，第一次 Meal 调用仍缺消费体积，要失败后重试。第三，完整历史清单只接管出站回复，没有接管 WebUI transcript，页面最终文字仍可能漏记或把两条独立餐食说成重复。

本版只修这三处现有闭环，不增加业务域、数据库表、迁移、普通确认步骤或额外回复内容。

## 本版修复

- 显式日期端点附近的具体时刻优先于“早上、傍晚、晚上”等时段默认边界。阿拉伯数字和常用中文数字时刻统一换算到 `Asia/Shanghai`，并保留跨日、跨月和跨年推导。
- 没有具体端点时刻的“昨晚到今天凌晨”“晚上到天亮前”等表达继续使用原有时段策略，不改变既有自然窗口含义。
- 同一用户消息中唯一、无冲突且包装单位匹配的精确换算（如“一盒就是180毫升”“每袋80克”）会在宿主入口确定性补齐 per-100ml/per-100g 所需消费量；多盒/多袋按数量相乘。
- 分类词不匹配、存在冲突换算、缺少数值或开放模糊量时不猜测，继续返回原有结构化缺失信息并保持零写入。
- 使用 OpenClaw 官方同步 `before_message_write` 钩子，把完整、只读“某时段吃了什么”查询的确定性逐条清单写入 WebUI transcript；出站通道和 WebUI 页面现在使用同一个 Meal 工具结果。
- WebUI 接管只作用于没有工具调用的最终 assistant 文本。用户消息、工具结果、中间工具调用、写入回执、不完整时间窗、营养分析、推荐和删除候选均不修改。
- 完整历史清单严格保留工具返回的每一条独立记录。内容和时间相同也不去重；用户未要求查重时，不追加“可能重复”判断。

## 明确保持不变

- “吃了个玉米”“吃了根火腿肠”等明确摄入继续首句直接记录，不增加确认。
- 玉米继续显示 `1个｜可食部（玉米粒）约90克（估算）`；紧接 80 克仍一次原子纠正，不新增餐次。
- 真正开放模糊量继续先展示有用估算并只问一次，确认前零写入。
- 普通餐食继续使用热量、蛋白、脂肪、碳水、纤维、饮水六项双行回执；纯水继续只显示饮水双行。
- Pantry 批次和位置管理、Meal 与唯一库存原子扣减/撤销返库、未知营养不补 0、计划/否定/他人隔离、跨域复合写入失败关闭、删除安全和过期推荐硬过滤均保持不变。
- 不修改 schema、migrations 001–021 或独立的 WebUI “新会话”按钮 UI-01。
- v0.7.4.26 及以前版本目录、提交、安装包和哈希保持不可变。

## 数据、安装与回退

本版没有新增 migration，继续使用 migrations 001–021，schema 与 v0.7.4.26/v0.7.4.19 相同。更新前必须停止目标实例，在外部 `dataDir` 之外建立并校验升级前冷备份，只安装已校验的 `personal-diet-pantry-0.7.4.27-installable.tgz`。在线 `diet_system backup` 只用于同版本辅助恢复，不能替代升级前冷备份。安装、自检、真实 UAT 或数据审计失败时，停止实例并恢复安装前冷备份及最近可启动包；不得回写旧版本。

## 阻断性口语化验收

1. “8月3号傍晚六点到4号早上六点，我有吃过啥吗？”必须只查询 18:00–次日 06:00；等价阿拉伯数字说法返回相同记录集合。
2. WebUI 最终消息必须逐条显示该时间窗全部记录；两条相同内容的独立餐次仍显示两条，不得暗示重复。
3. 唯一库存中有两盒营养未知豆奶时，用户在同一轮明确“一盒180毫升”和每 100ml 部分标签，第一次 Meal 调用必须直接成功；只扣本次盒数，未知纤维/钠保持未知，撤销同时返库。
4. 包装分类词冲突或换算数值冲突时不得猜测、不得先失败后扫描源码，也不得产生部分写入。
5. 回归玉米新增/80克纠正/删除、纯水新增/删除、花生模糊预览/取消、Pantry、过期推荐、查询零写入、计划/否定/他人、跨域复合零提交和删除安全。
6. 任一总约束保护项失败，或安装后存在未清理测试数据、数据库完整性异常、版本/哈希不符，均阻断验收。

## 候选制品契约

本地候选目录顶层必须恰好包含：

- `personal-diet-pantry-0.7.4.27-source.tar.gz`
- `personal-diet-pantry-0.7.4.27-installable.tgz`
- `release-manifest.json`
- `TEST-SUMMARY-v0.7.4.27.zh-CN.md`
- `SHA256SUMS`
- 本地审阅留痕目录 `GitHub文档`

当前仍是开发候选。只有总约束保护矩阵、全量自动化、可重复构建、安装包审计、停机冷备份、软路由安装、真实口语化 UAT、测试数据清理和数据库核验全部通过，才能标记为可用；本项目不会自动推送 GitHub、创建 Tag 或 Release。
