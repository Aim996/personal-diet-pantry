# 食序管家 v0.4.2 更新说明

这是 v0.4.1 的小版本可靠性修复，只更新 Skill 与配套插件，不更新
OpenClaw 系统，也不改变现有 SQLite 数据结构。

## 本次修复

- 输入“吃了两个玉米饼，喝了330ml豆浆”时，按 100g 标注的液体包装营养可按
  `330ml ≈ 330g` 计算，不再出现豆浆营养为空。
- 液体标签未提供含水量时，将本次实际饮用体积计入隐性饮水；例如 330ml
  豆浆为饮水进度增加 330ml，但不会重复创建一条纯饮水记录。
- 同一商品存在多个物理批次时，不再误判为商品歧义，而是按库存策略先进先出
  自动扣减；多个不同商品都可能命中时仍保留一次简短选择。
- 模型首次漏传餐食 `items` 时，工具返回 `meal_items_required` 的安全重试提示，
  不写数据，也不再把这类可恢复遗漏显示为红色工具错误。
- 明确的“吃了/喝了”记录如果误用了预览动作，会按 `intent: record` 归一为一次
  原子写入，避免重复确认。
- 普通记录回执固定为三段：已记录内容、六项两行制进度条、可选的本次库存变动；
  不返回完整库存。
- 插件清单显式声明随包提供的 `skills` 目录，安装或重启后会自动加载食序管家
  的记录、库存与回复格式规则，不需要另外复制 Skill。
- 进度条直接使用 Skill 内嵌规则计算，不再尝试读取 NAS 上的外部模板或验证
  脚本，避免只为排版产生红色 `Read` 工具错误。

## 查询防护

- 饮食记录与进度回复只使用 `diet_meal`、`diet_report` 返回的结构化结果，不读取旧版
  `reports/daily` 日报、Markdown、JSON 导出或其他文件型报告。
- 当 `daily_progress` 已包含饮水进度时，不再额外调用 `diet_water` 重复拼装同一组数据。

## 数据、备份与回退

v0.4.2 没有新增数据库迁移，SQLite 仍保持 schema 10，现有库存、营养表、
饮食记录和个人偏好均原样保留。部署前会生成 SQLite 备份并校验完整性。

需要回退时，可恢复部署前备份，并将插件源码切回 v0.4.1。回退不会要求重建
库存，也不会删除现有营养数据。
