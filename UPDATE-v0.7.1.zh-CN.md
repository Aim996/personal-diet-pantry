# 食序管家 v0.7.1 更新说明

v0.7.1 是基于 v0.7.0 的可信写入与调用效率升级。它继续把项目定位为可复制、可安装的 OpenClaw Skill；源码构建不等于部署，本轮不会自动修改软路由、网关、现有 OpenClaw 状态或个人 `dataDir`。

## 本版完成内容

### 1. 信息不完整时保持零写入

- 每条输入先判定为 `ready`、`needs_one_fact` 或 `supplement_or_correction`。
- 清晰且完整的已发生事件直接正式写入；只缺一个关键事实时只问一个短问题。
- “鸭腿是库存里的”“是小象那瓶”等补充事实不再被当作纯确认。补充后必须重新检查整份记录是否完整，不能沿用旧猜测直接落库。
- 带骨食物缺重量、煎制缺用油、剔脂只给范围而没有可用值时均保持零写入。

### 2. 模糊库存商品不再被规范名掩盖

库存匹配先保留并查询用户原始商品说法。若“小瓶牛奶”等说法同时命中多个不同 SKU，工具返回候选并等待一次选择；模型提前填写的规范名不能绕过歧义。多个属于同一商品的物理批次仍按既定批次策略扣减，不制造无意义确认。

### 3. 做饭修正成为完整原子替换

`diet_meal update` 现在接受普通餐食草稿或完整做饭草稿。修正做饭记录时会在同一事务中恢复旧原料扣减、撤下未被后续使用的旧派生剩菜、写入新做饭事实并生成新剩菜。如果旧剩菜已经发生食用或其他后续变动，修正以 `STALE_PREVIEW` 失败，旧餐食、旧库存和旧剩菜全部保持不变，不留下“幽灵剩菜”。

### 4. 明确加工损耗进入营养重算

餐项可携带最多 8 条结构化 `preparation_losses`，类型为 `bone`、`shell`、`skin`、`fat` 或 `other`，单位固定为克，并要求完整的损耗营养事实。营养先按实际可食重量和声明口径缩放，再扣除明确损耗；库存仍按原料实际扣减量处理。损耗内容会写入份量证据，便于复核与重放。

### 5. 混合液体和超标值可直接信任

- 同一餐里的牛奶、咖啡等液体各自保留体积与 `per_100ml` 口径，只在一个餐食事务中各缩放一次；不会因为批量记录相互污染，也不会重复生成白水记录。
- 进度指标新增 `over_by`。仅在用户目标已确认、指标属于 maximum goal 且确实超标时返回非负精确差值。例如脂肪 `77.25g / 55g` 返回 `over_by: 22.25` 与 `percent: 140`；40 个百分点不再被写成 40g。

### 6. DeepSeek 缓存友好且减少无效调用

- 主 Skill 的稳定规则放在固定前缀，动态会话事实留在宿主请求后部；不靠重复提示词或填充文本追求缓存。
- 普通单领域消息只加载一个最相关 reference；真正跨领域最多两个；同一轮不重复读取同一文件。
- 清晰完整的单领域事件不先做 pantry、report、self-check 或 preview 热身，一次正式工具动作即可完成。
- 歧义查询返回的候选与句柄在有效期内直接复用，不再次按同一名称查询；日报/周报等使用一个聚合报告调用，不扇出为多次餐食、饮水、体重和库存读取。
- 必要的歧义定位、状态读取、完整性保护和聚合查询仍保留，不能为了减少调用而降低正确性。

## 兼容与升级

v0.7.1 未新增数据库迁移，继续兼容 v0.7.0 的 SQLite 数据。升级前仍需停止目标实例并完整备份当前软件包和整个 `dataDir`；安装 v0.7.1 后保留原 `dataDir`，启动完成后运行一次 `diet_system self_check`，再用只读查询核对目标、库存、进度和最近记录。回滚应同时恢复旧软件包与升级前数据备份。

## 已验证边界

快速核心门禁固定为不超过 25 个高风险节点，新增覆盖：模糊牛奶 SKU、牛奶与咖啡混合记录、做饭修正回收旧剩菜、已消费剩菜阻断修正、明确剔脂重算和精确 `over_by`。完整发布仍以 `ci/verify.ps1` 与可复现发布构建的实际机器结果为准，不在文档中手填通过数。

## 本轮明确延期

- 前端同一回复重复渲染需要 OpenClaw 宿主事件日志才能区分“重复展示”与“重复写入”；本版已保留业务幂等和单回复 Skill 规则，但不把宿主 UI 问题伪装为已修复。
- 不加入供应商专属 DeepSeek 代码、伪造缓存命中统计或提示词填充；真实缓存指标应由部署侧 API usage/日志验证。
- 不新增 Docker、远程托管、Tailscale/WireGuard、Lucky、凭据管理、多用户协作或图片识别能力。
- 本轮不自动部署到任何现有 OpenClaw、软路由或个人数据目录。

## 回退

如果实际体验不符合预期，可停止实例，恢复升级前软件包和对应的完整 `dataDir` 备份。不要只替换代码后继续使用已被新版本写过的数据，也不要手工编辑 `schema_migrations`、SQLite 业务表或派生 Markdown 报告。
